//
// Generated by JSON Model Compiler version 2
// see https://github.com/clairey-zx81/json-model
//

import json_model.*;
import java.util.Map;
import java.util.Set;
import java.util.HashMap;
import java.util.HashSet;
import java.util.Iterator;
import java.util.regex.Pattern;
import java.util.regex.Matcher;

@SuppressWarnings("unchecked")
public class prefix_03 extends ModelChecker
{
    static public final String VERSION = "2";

    public Map<String, Checker> prefix_03_map_pmap;

    // check $ (.)
    public boolean json_model_1(Object val, Path path, Report rep)
    {
        // just 1 or 2 strings
        // .
        boolean res = json.isArray(val);
        if (res)
        {
            long size_0 = json.arrayLength(val);
            if (size_0 >= 1 && size_0 <= 2)
            {
                // unrolled prefix type check
                Object item_0 = json.arrayItem(val, 0);
                res = json.isString(item_0);
                if (! res)
                {
                    if (rep != null) rep.addEntry("unexpected array item type [.'@'.1]", path);
                }
                // optional remaining item
                if (res && size_0 == 2)
                {
                    Object item_1 = json.arrayItem(val, 1);
                    res = json.isString(item_1);
                    if (! res)
                    {
                        if (rep != null) rep.addEntry("unexpected array item type [.'@'.1]", path);
                    }
                }
            }
            else
            {
                if (rep != null) rep.addEntry("unexpected array size [.]", path);
                res = false;
            }
        }
        else
        {
            if (rep != null) rep.addEntry("expecting an array [..@]", path);
            res = false;
        }
        return res;
    }


    public void init(JSON json)
    {
        if (!initialized)
        {
            try {
            prefix_03_map_pmap = new HashMap<String, Checker>();
            prefix_03_map_pmap.put("", new Checker() { public boolean call(Object o, Path p, Report r) { return json_model_1(o, p, r);} });
                super.init(json);
            }
            catch (Exception e) {
                throw new Error("cannot initialized model checker: " + e);
            }
        }
    }

    public void free()
    {
        if (initialized)
        {
            super.free();
            prefix_03_map_pmap = null;
        }
    }

    public Checker get(String name)
    {
        return prefix_03_map_pmap.get(name);
    }

    public Set<String> models()
    {
        return prefix_03_map_pmap.keySet();
    }

    static public void main(String[] args) throws Exception
    {
        ModelChecker checker = new prefix_03();
        Main.main("prefix_03", checker, VERSION, args);
    }
}
