//
// Generated by JSON Model Compiler version 2
// see https://github.com/clairey-zx81/json-model
//

import json_model.*;
import java.util.Map;
import java.util.Set;
import java.util.HashMap;
import java.util.HashSet;
import java.util.Iterator;
import java.util.regex.Pattern;
import java.util.regex.Matcher;

@SuppressWarnings("unchecked")
public class const_one extends ModelChecker
{
    static public final String VERSION = "2";

    public Map<String, Checker> const_one_map_pmap;

    // check $ (.)
    public boolean json_model_1(Object val, Path path, Report rep)
    {
        // JSON_MODEL_LOOSE_NUMBER
        // .
        // .'@'
        boolean res = ((json.isInteger(val) || (json.isDouble(val) && json.asDouble(val) == ((long) json.asDouble(val))))) && json.asNumber(val) == 1;
        if (! res)
        {
            if (rep != null) rep.addEntry("unexpected value for model \"=1\" [.'@']", path);
        }
        return res;
    }


    public void init(JSON json)
    {
        if (!initialized)
        {
            try {
            const_one_map_pmap = new HashMap<String, Checker>();
            const_one_map_pmap.put("", new Checker() { public boolean call(Object o, Path p, Report r) { return json_model_1(o, p, r);} });
                super.init(json);
            }
            catch (Exception e) {
                throw new Error("cannot initialized model checker: " + e);
            }
        }
    }

    public void free()
    {
        if (initialized)
        {
            super.free();
            const_one_map_pmap = null;
        }
    }

    public Checker get(String name)
    {
        return const_one_map_pmap.get(name);
    }

    public Set<String> models()
    {
        return const_one_map_pmap.keySet();
    }

    static public void main(String[] args) throws Exception
    {
        ModelChecker checker = new const_one();
        Main.main("const_one", checker, VERSION, args);
    }
}
