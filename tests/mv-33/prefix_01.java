//
// Generated by JSON Model Compiler version 2
// see https://github.com/clairey-zx81/json-model
//

import json_model.*;
import java.util.Map;
import java.util.Set;
import java.util.HashMap;
import java.util.HashSet;
import java.util.Iterator;
import java.util.regex.Pattern;
import java.util.regex.Matcher;

@SuppressWarnings("unchecked")
public class prefix_01 extends ModelChecker
{
    static public final String VERSION = "2";

    public Map<String, Checker> prefix_01_map_pmap;

    // check $ (.)
    public boolean json_model_1(Object val, Path path, Report rep)
    {
        // .
        boolean res = json.isArray(val);
        if (res)
        {
            long size_0 = json.arrayLength(val);
            if (size_0 == 3)
            {
                // unrolled prefix type check
                Object item_0 = json.arrayItem(val, 0);
                Object item_1 = json.arrayItem(val, 1);
                Object item_2 = json.arrayItem(val, 2);
                res = json.isInteger(item_0) && json.isInteger(item_1) && json.isInteger(item_2);
                if (! res)
                {
                    if (rep != null) rep.addEntry("unexpected array item type [.'@'.1]", path);
                }
            }
            else
            {
                if (rep != null) rep.addEntry("unexpected array size [.]", path);
                res = false;
            }
        }
        else
        {
            if (rep != null) rep.addEntry("expecting an array [..@]", path);
            res = false;
        }
        return res;
    }


    public void init(JSON json)
    {
        if (!initialized)
        {
            try {
            prefix_01_map_pmap = new HashMap<String, Checker>();
            prefix_01_map_pmap.put("", new Checker() { public boolean call(Object o, Path p, Report r) { return json_model_1(o, p, r);} });
                super.init(json);
            }
            catch (Exception e) {
                throw new Error("cannot initialized model checker: " + e);
            }
        }
    }

    public void free()
    {
        if (initialized)
        {
            super.free();
            prefix_01_map_pmap = null;
        }
    }

    public Checker get(String name)
    {
        return prefix_01_map_pmap.get(name);
    }

    public Set<String> models()
    {
        return prefix_01_map_pmap.keySet();
    }

    static public void main(String[] args) throws Exception
    {
        ModelChecker checker = new prefix_01();
        Main.main("prefix_01", checker, VERSION, args);
    }
}
