//
// Generated by JSON Model Compiler version 2
// see https://github.com/clairey-zx81/json-model
//

import json_model.*;
import java.util.Map;
import java.util.Set;
import java.util.HashMap;
import java.util.HashSet;
import java.util.Iterator;
import java.util.regex.Pattern;
import java.util.regex.Matcher;

@SuppressWarnings("unchecked")
public class exmax extends ModelChecker
{
    static public final String VERSION = "2";

    public Map<String, Checker> exmax_map_pmap;

    // check $ (.)
    public boolean json_model_1(Object val, Path path, Report rep)
    {
        // JsonModel 1 (JSON_MODEL_LOOSE_INT JSON_MODEL_LOOSE_FLOAT)
        // .
        // .'|'.0
        // .'|'.0.'@'
        boolean res = (json.isInteger(val) || (json.isDouble(val) && json.asDouble(val) == ((long) json.asDouble(val))));
        if (res)
        {
            long ival_0 = json.asLong(val);
            res = ival_0 <= 2;
            if (! res)
            {
                if (rep != null) rep.addEntry("constraints failed [.'|'.0]", path);
            }
        }
        else
        {
            if (rep != null) rep.addEntry("not a -1 loose int [.'|'.0.'@']", path);
        }
        if (! res)
        {
            // .'|'.1
            // .'|'.1.'@'
            res = json.isNumber(val);
            if (res)
            {
                double fval_0 = json.asNumber(val);
                res = fval_0 < 3.0;
                if (! res)
                {
                    if (rep != null) rep.addEntry("constraints failed [.'|'.1]", path);
                }
            }
            else
            {
                if (rep != null) rep.addEntry("not a -1.0 loose float [.'|'.1.'@']", path);
            }
        }
        if (res)
        {
            if (rep != null) rep.clearEntries();
        }
        else
        {
            if (rep != null) rep.addEntry("no model matched [.'|']", path);
        }
        return res;
    }


    public void init(JSON json)
    {
        if (!initialized)
        {
            try {
            exmax_map_pmap = new HashMap<String, Checker>();
            exmax_map_pmap.put("", new Checker() { public boolean call(Object o, Path p, Report r) { return json_model_1(o, p, r);} });
                super.init(json);
            }
            catch (Exception e) {
                throw new Error("cannot initialized model checker: " + e);
            }
        }
    }

    public void free()
    {
        if (initialized)
        {
            super.free();
            exmax_map_pmap = null;
        }
    }

    public Checker get(String name)
    {
        return exmax_map_pmap.get(name);
    }

    public Set<String> models()
    {
        return exmax_map_pmap.keySet();
    }

    static public void main(String[] args) throws Exception
    {
        ModelChecker checker = new exmax();
        Main.main("exmax", checker, VERSION, args);
    }
}
