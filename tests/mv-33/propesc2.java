//
// Generated by JSON Model Compiler version 2
// see https://github.com/clairey-zx81/json-model
//

import json_model.*;
import java.util.Map;
import java.util.Set;
import java.util.HashMap;
import java.util.HashSet;
import java.util.Iterator;
import java.util.regex.Pattern;
import java.util.regex.Matcher;

@SuppressWarnings("unchecked")
public class propesc2 extends ModelChecker
{
    static public final String VERSION = "2";

    public Map<String, Checker> propesc2_map_pmap;

    // check $ (.)
    public boolean json_model_1(Object val, Path path, Report rep)
    {
        // one mandatory prop with an escaped character
        // .
        // check open must/may only props
        if (! json.isObject(val))
        {
            if (rep != null) rep.addEntry("not an object [.]", path);
            return false;
        }
        if (! json.objectHasProp(val, "foo\nbar"))
        {
            if (rep != null) rep.addEntry("missing mandatory prop <foo\\nbar> [.]", path);
            return false;
        }
        return true;
    }


    public void init(JSON json)
    {
        if (!initialized)
        {
            try {
            propesc2_map_pmap = new HashMap<String, Checker>();
            propesc2_map_pmap.put("", new Checker() { public boolean call(Object o, Path p, Report r) { return json_model_1(o, p, r);} });
                super.init(json);
            }
            catch (Exception e) {
                throw new Error("cannot initialized model checker: " + e);
            }
        }
    }

    public void free()
    {
        if (initialized)
        {
            super.free();
            propesc2_map_pmap = null;
        }
    }

    public Checker get(String name)
    {
        return propesc2_map_pmap.get(name);
    }

    public Set<String> models()
    {
        return propesc2_map_pmap.keySet();
    }

    static public void main(String[] args) throws Exception
    {
        ModelChecker checker = new propesc2();
        Main.main("propesc2", checker, VERSION, args);
    }
}
