//
// Generated by JSON Model Compiler version 2
// see https://github.com/clairey-zx81/json-model
//

import json_model.*;
import java.util.Map;
import java.util.Set;
import java.util.HashMap;
import java.util.HashSet;
import java.util.Iterator;
import java.util.regex.Pattern;
import java.util.regex.Matcher;

@SuppressWarnings("unchecked")
public class prefix_00 extends ModelChecker
{
    static public final String VERSION = "2";

    public Map<String, Checker> prefix_00_map_pmap;

    // check $ (.)
    public boolean json_model_1(Object val, Path path, Report rep)
    {
        // list type check unrolled prefix
        // .
        boolean res = json.isArray(val);
        if (res)
        {
            long size_0 = json.arrayLength(val);
            if (size_0 >= 2 && size_0 <= 4)
            {
                // unrolled prefix type check
                Object item_0 = json.arrayItem(val, 0);
                Object item_1 = json.arrayItem(val, 1);
                res = json.isString(item_0) && json.isString(item_1);
                // optional remaining items
                if (res)
                {
                    for (int index_0 = 2; index_0 < size_0; index_0++)
                    {
                        Object item_2 = json.arrayItem(val, index_0);
                        res = json.isString(item_2);
                        if (! res)
                        {
                            break;
                        }
                    }
                }
                // other constraints
                if (res)
                {
                    res = rt.array_is_unique(val, path, rep);
                }
                if (res)
                {
                    res = size_0 != 3;
                    if (! res)
                    {
                        if (rep != null) rep.addEntry("unexpected array size [.]", path);
                    }
                }
            }
            else
            {
                if (rep != null) rep.addEntry("unexpected array size [.]", path);
                res = false;
            }
        }
        else
        {
            if (rep != null) rep.addEntry("expecting an array [.]", path);
            res = false;
        }
        return res;
    }


    public void init(JSON json)
    {
        if (!initialized)
        {
            try {
            prefix_00_map_pmap = new HashMap<String, Checker>();
            prefix_00_map_pmap.put("", new Checker() { public boolean call(Object o, Path p, Report r) { return json_model_1(o, p, r);} });
                super.init(json);
            }
            catch (Exception e) {
                throw new Error("cannot initialized model checker: " + e);
            }
        }
    }

    public void free()
    {
        if (initialized)
        {
            super.free();
            prefix_00_map_pmap = null;
        }
    }

    public Checker get(String name)
    {
        return prefix_00_map_pmap.get(name);
    }

    public Set<String> models()
    {
        return prefix_00_map_pmap.keySet();
    }

    static public void main(String[] args) throws Exception
    {
        ModelChecker checker = new prefix_00();
        Main.main("prefix_00", checker, VERSION, args);
    }
}
