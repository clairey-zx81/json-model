{
  "type": "object",
  "properties": {
    "l": {
      "type": "string",
      "pattern": "(?s)^[a-z][0-9a-z_-]+$"
    },
    "u": {
      "type": "string",
      "pattern": "(?s)^[A-Z][-0-9A-Z_]*$"
    },
    "i": {
      "type": "string",
      "pattern": "(?is)^[a-zA-Z][-_0-9a-z]{1,8}$"
    }
  },
  "additionalProperties": false
}
