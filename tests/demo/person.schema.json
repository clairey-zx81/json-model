{
  "description": "A person with a birth date",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "pattern": "(?i)^[a-z]+$"
    },
    "born": {
      "type": "string",
      "format": "date"
    }
  },
  "required": [
    "name",
    "born"
  ],
  "additionalProperties": false
}
