//
// Generated by JSON Model Compiler version 2
// see https://github.com/clairey-zx81/json-model
//
import 'dart:convert';
import 'dart:io';
import 'dart:core';
// Import the helper library generated nearby
import 'package:json_model_runtime/json_model.dart' as runtime;
import 'package:json_model_runtime/main.dart';

//
// Generated JSON Model Validator
//
const String JSON_MODEL_VERSION = "2";

final _jm_re_0_re = RegExp("^[0-9a-zA-Z]+(\\.[0-9a-zA-Z]+)*\$", caseSensitive: true, multiLine: false);
final _jm_re_1_re = RegExp("^rfc\\d+\$", caseSensitive: true, multiLine: false);
final _jm_re_2_re = RegExp("^iso\\d+\$", caseSensitive: true, multiLine: false);
var check_model_map = <String, Function>{};

bool _jm_re_0(String s, List<dynamic>? path, List<dynamic>? rep) => _jm_re_0_re.hasMatch(s);

// check $Version (.'$Version')
bool json_model_2(dynamic val, List<dynamic>? path, List<dynamic>? rep)
{
    // .'$Version'
    // "/^[0-9a-zA-Z]+(\\.[0-9a-zA-Z]+)*$/"
    bool res = (val is String) && _jm_re_0(val, path, rep);
    if (! res)
    {
        rep?.add(["unexpected /^[0-9a-zA-Z]+(\\.[0-9a-zA-Z]+)*\$/ [.'\$Version']", path]);
    }
    return res;
}

// check $Test (.'$Test')
bool json_model_3(dynamic val, List<dynamic>? path, List<dynamic>? rep)
{
    // A single test
    // .'$Test'
    if (! (val is Map))
    {
        rep?.add(["not an object [.'\$Test']", path]);
        return false;
    }
    bool res;
    int must_count = 0;
    for (var entry in val.entries)
    {
        var prop = entry.key;
        var pval = entry.value;
        List<dynamic>? lpath_0 = (path != null ? [...?path, prop] : null);
        if (prop.compareTo("data") == 0)
        {
            // handle must data property
            must_count += 1;
            // .'$Test'.data
            res = true;
            continue;
        }
        else if (prop.compareTo("valid") == 0)
        {
            // handle must valid property
            must_count += 1;
            // .'$Test'.valid
            res = (pval is bool);
            if (! res)
            {
                rep?.add(["not a bool [.'\$Test'.valid]", (path != null ? lpath_0 : null)]);
                rep?.add(["invalid mandatory prop value [.'\$Test'.valid]", (path != null ? lpath_0 : null)]);
                return false;
            }
            continue;
        }
        else if (prop.compareTo("description") == 0)
        {
            // handle must description property
            must_count += 1;
            // .'$Test'.description
            res = (pval is String);
            if (! res)
            {
                rep?.add(["unexpected string [.'\$Test'.description]", (path != null ? lpath_0 : null)]);
                rep?.add(["invalid mandatory prop value [.'\$Test'.description]", (path != null ? lpath_0 : null)]);
                return false;
            }
            continue;
        }
        if (prop.compareTo("comment") == 0)
        {
            // handle may comment property
            // .'$Test'.comment
            res = (pval is String);
            if (! res)
            {
                rep?.add(["unexpected string [.'\$Test'.comment]", (path != null ? lpath_0 : null)]);
                rep?.add(["invalid optional prop value [.'\$Test'.comment]", (path != null ? lpath_0 : null)]);
                return false;
            }
            continue;
        }
        rep?.add(["unexpected prop [.'\$Test']", (path != null ? lpath_0 : null)]);
        return false;
    }
    if (must_count != 3)
    {
        if (rep != null)
        {
            if (! val.containsKey("data"))
            {
                rep?.add(["missing mandatory prop <data> [.'\$Test']", path]);
            }
            if (! val.containsKey("description"))
            {
                rep?.add(["missing mandatory prop <description> [.'\$Test']", path]);
            }
            if (! val.containsKey("valid"))
            {
                rep?.add(["missing mandatory prop <valid> [.'\$Test']", path]);
            }
        }
        return false;
    }
    return true;
}

bool _jm_re_1(String s, List<dynamic>? path, List<dynamic>? rep) => _jm_re_1_re.hasMatch(s);

bool _jm_re_2(String s, List<dynamic>? path, List<dynamic>? rep) => _jm_re_2_re.hasMatch(s);

// object .'$Specification'.'@'
bool _jm_obj_0(dynamic val, List<dynamic>? path, List<dynamic>? rep)
{
    if (! (val is Map))
    {
        rep?.add(["not an object [.'\$Specification'.'@']", path]);
        return false;
    }
    bool res;
    for (var entry in val.entries)
    {
        var prop = entry.key;
        var pval = entry.value;
        List<dynamic>? lpath_1 = (path != null ? [...?path, prop] : null);
        if (prop.compareTo("core") == 0)
        {
            // handle may core property
            // .'$Specification'.'@'.core
            res = json_model_2(pval, (path != null ? lpath_1 : null), rep);
            if (! res)
            {
                rep?.add(["unexpected \$Version [.'\$Specification'.'@'.core]", (path != null ? lpath_1 : null)]);
                rep?.add(["invalid optional prop value [.'\$Specification'.'@'.core]", (path != null ? lpath_1 : null)]);
                return false;
            }
            continue;
        }
        else if (prop.compareTo("perl5") == 0)
        {
            // handle may perl5 property
            // .'$Specification'.'@'.perl5
            res = (pval is String);
            if (! res)
            {
                rep?.add(["unexpected string [.'\$Specification'.'@'.perl5]", (path != null ? lpath_1 : null)]);
                rep?.add(["invalid optional prop value [.'\$Specification'.'@'.perl5]", (path != null ? lpath_1 : null)]);
                return false;
            }
            continue;
        }
        else if (prop.compareTo("quote") == 0)
        {
            // handle may quote property
            // .'$Specification'.'@'.quote
            res = (pval is String);
            if (! res)
            {
                rep?.add(["unexpected string [.'\$Specification'.'@'.quote]", (path != null ? lpath_1 : null)]);
                rep?.add(["invalid optional prop value [.'\$Specification'.'@'.quote]", (path != null ? lpath_1 : null)]);
                return false;
            }
            continue;
        }
        else if (prop.compareTo("ecma262") == 0)
        {
            // handle may ecma262 property
            // .'$Specification'.'@'.ecma262
            res = json_model_2(pval, (path != null ? lpath_1 : null), rep);
            if (! res)
            {
                rep?.add(["unexpected \$Version [.'\$Specification'.'@'.ecma262]", (path != null ? lpath_1 : null)]);
                rep?.add(["invalid optional prop value [.'\$Specification'.'@'.ecma262]", (path != null ? lpath_1 : null)]);
                return false;
            }
            continue;
        }
        else if (prop.compareTo("validation") == 0)
        {
            // handle may validation property
            // .'$Specification'.'@'.validation
            res = json_model_2(pval, (path != null ? lpath_1 : null), rep);
            if (! res)
            {
                rep?.add(["unexpected \$Version [.'\$Specification'.'@'.validation]", (path != null ? lpath_1 : null)]);
                rep?.add(["invalid optional prop value [.'\$Specification'.'@'.validation]", (path != null ? lpath_1 : null)]);
                return false;
            }
            continue;
        }
        if (_jm_re_1(prop, path, rep))
        {
            // handle 2 re props
            // .'$Specification'.'@'.'/^rfc\\d+$/'
            res = json_model_2(pval, (path != null ? lpath_1 : null), rep);
            if (! res)
            {
                rep?.add(["unexpected \$Version [.'\$Specification'.'@'.'/^rfc\\\\d+\$/']", (path != null ? lpath_1 : null)]);
                return false;
            }
        }
        else if (_jm_re_2(prop, path, rep))
        {
            // handle 2 re props
            // .'$Specification'.'@'.'/^iso\\d+$/'
            res = json_model_2(pval, (path != null ? lpath_1 : null), rep);
            if (! res)
            {
                rep?.add(["unexpected \$Version [.'\$Specification'.'@'.'/^iso\\\\d+\$/']", (path != null ? lpath_1 : null)]);
                return false;
            }
        }
        else
        {
            rep?.add(["unexpected prop [.'\$Specification'.'@']", (path != null ? lpath_1 : null)]);
            return false;
        }
    }
    return true;
}

// check $Specification (.'$Specification')
bool json_model_4(dynamic val, List<dynamic>? path, List<dynamic>? rep)
{
    // .'$Specification'
    // .'$Specification'.'@'
    bool res = _jm_obj_0(val, path, rep);
    if (res)
    {
        int ival_0 = val.length;
        res = ival_0 >= 1;
        if (! res)
        {
            rep?.add(["constraints failed [.'\$Specification']", path]);
        }
    }
    else
    {
        rep?.add(["unexpected element [.'\$Specification'.'@']", path]);
    }
    return res;
}

// check $TestCase (.'$TestCase')
bool json_model_5(dynamic val, List<dynamic>? path, List<dynamic>? rep)
{
    // .'$TestCase'
    if (! (val is Map))
    {
        rep?.add(["not an object [.'\$TestCase']", path]);
        return false;
    }
    bool res;
    int must_count = 0;
    for (var entry in val.entries)
    {
        var prop = entry.key;
        var pval = entry.value;
        List<dynamic>? lpath_2 = (path != null ? [...?path, prop] : null);
        if (prop.compareTo("tests") == 0)
        {
            // handle must tests property
            must_count += 1;
            // .'$TestCase'.tests
            // .'$TestCase'.tests.'@'
            res = (pval is List);
            if (res)
            {
                for (var arr_0_idx = 0; arr_0_idx < pval.length; arr_0_idx++)
                {
                    var arr_0_item = pval[arr_0_idx];
                    List<dynamic>? arr_0_lpath = ((path != null ? lpath_2 : null) != null ? [...?(path != null ? lpath_2 : null), arr_0_idx] : null);
                    // .'$TestCase'.tests.'@'.0
                    res = json_model_3(arr_0_item, ((path != null ? lpath_2 : null) != null ? arr_0_lpath : null), rep);
                    if (! res)
                    {
                        rep?.add(["unexpected \$Test [.'\$TestCase'.tests.'@'.0]", ((path != null ? lpath_2 : null) != null ? arr_0_lpath : null)]);
                        break;
                    }
                }
            }
            if (res)
            {
                int ival_1 = pval.length;
                res = ival_1 >= 1;
                if (! res)
                {
                    rep?.add(["constraints failed [.'\$TestCase'.tests]", (path != null ? lpath_2 : null)]);
                }
            }
            else
            {
                rep?.add(["not array or unexpected array [.'\$TestCase'.tests.'@']", (path != null ? lpath_2 : null)]);
            }
            if (! res)
            {
                rep?.add(["invalid mandatory prop value [.'\$TestCase'.tests]", (path != null ? lpath_2 : null)]);
                return false;
            }
            continue;
        }
        else if (prop.compareTo("schema") == 0)
        {
            // handle must schema property
            must_count += 1;
            // .'$TestCase'.schema
            res = true;
            continue;
        }
        else if (prop.compareTo("description") == 0)
        {
            // handle must description property
            must_count += 1;
            // .'$TestCase'.description
            res = (pval is String);
            if (! res)
            {
                rep?.add(["unexpected string [.'\$TestCase'.description]", (path != null ? lpath_2 : null)]);
                rep?.add(["invalid mandatory prop value [.'\$TestCase'.description]", (path != null ? lpath_2 : null)]);
                return false;
            }
            continue;
        }
        if (prop.compareTo("comment") == 0)
        {
            // handle may comment property
            // .'$TestCase'.comment
            res = (pval is String);
            if (! res)
            {
                rep?.add(["unexpected string [.'\$TestCase'.comment]", (path != null ? lpath_2 : null)]);
                rep?.add(["invalid optional prop value [.'\$TestCase'.comment]", (path != null ? lpath_2 : null)]);
                return false;
            }
            continue;
        }
        else if (prop.compareTo("specification") == 0)
        {
            // handle may specification property
            // .'$TestCase'.specification
            // .'$TestCase'.specification.'@'
            res = (pval is List);
            if (res)
            {
                for (var arr_1_idx = 0; arr_1_idx < pval.length; arr_1_idx++)
                {
                    var arr_1_item = pval[arr_1_idx];
                    List<dynamic>? arr_1_lpath = ((path != null ? lpath_2 : null) != null ? [...?(path != null ? lpath_2 : null), arr_1_idx] : null);
                    // .'$TestCase'.specification.'@'.0
                    res = json_model_4(arr_1_item, ((path != null ? lpath_2 : null) != null ? arr_1_lpath : null), rep);
                    if (! res)
                    {
                        rep?.add(["unexpected \$Specification [.'\$TestCase'.specification.'@'.0]", ((path != null ? lpath_2 : null) != null ? arr_1_lpath : null)]);
                        break;
                    }
                }
            }
            if (res)
            {
                int ival_2 = pval.length;
                res = runtime.jm_array_is_unique(pval, (path != null ? lpath_2 : null), rep) && ival_2 >= 1;
                if (! res)
                {
                    rep?.add(["constraints failed [.'\$TestCase'.specification]", (path != null ? lpath_2 : null)]);
                }
            }
            else
            {
                rep?.add(["not array or unexpected array [.'\$TestCase'.specification.'@']", (path != null ? lpath_2 : null)]);
            }
            if (! res)
            {
                rep?.add(["invalid optional prop value [.'\$TestCase'.specification]", (path != null ? lpath_2 : null)]);
                return false;
            }
            continue;
        }
        rep?.add(["unexpected prop [.'\$TestCase']", (path != null ? lpath_2 : null)]);
        return false;
    }
    if (must_count != 3)
    {
        if (rep != null)
        {
            if (! val.containsKey("description"))
            {
                rep?.add(["missing mandatory prop <description> [.'\$TestCase']", path]);
            }
            if (! val.containsKey("schema"))
            {
                rep?.add(["missing mandatory prop <schema> [.'\$TestCase']", path]);
            }
            if (! val.containsKey("tests"))
            {
                rep?.add(["missing mandatory prop <tests> [.'\$TestCase']", path]);
            }
        }
        return false;
    }
    return true;
}

// check $ (.)
bool json_model_1(dynamic val, List<dynamic>? path, List<dynamic>? rep)
{
    // Model for the JSON Schema Test Suite tests
    // .
    // .'@'
    bool res = (val is List);
    if (res)
    {
        for (var arr_2_idx = 0; arr_2_idx < val.length; arr_2_idx++)
        {
            var arr_2_item = val[arr_2_idx];
            List<dynamic>? arr_2_lpath = (path != null ? [...?path, arr_2_idx] : null);
            // .'@'.0
            res = json_model_5(arr_2_item, (path != null ? arr_2_lpath : null), rep);
            if (! res)
            {
                rep?.add(["unexpected \$TestCase [.'@'.0]", (path != null ? arr_2_lpath : null)]);
                break;
            }
        }
    }
    if (res)
    {
        int ival_3 = val.length;
        res = ival_3 >= 1;
        if (! res)
        {
            rep?.add(["constraints failed [.]", path]);
        }
    }
    else
    {
        rep?.add(["not array or unexpected array [.'@']", path]);
    }
    return res;
}


// dart_init.dart
class GlobalState {
  static bool initializer = false;
}

Function? check_model_fun(String name) {
  return check_model_map[name];
}


void check_model_init() {
  if (!GlobalState.initializer){
    GlobalState.initializer = true;
  }
        check_model_map[""] = json_model_1;
        check_model_map["Version"] = json_model_2;
        check_model_map["Test"] = json_model_3;
        check_model_map["Specification"] = json_model_4;
        check_model_map["TestCase"] = json_model_5;
}

// dart_free.dart
void check_model_free() {
  GlobalState.initializer = true;
}


void main(List<String> args) {
  runValidation(
    args,
    check_model_init,
    check_model_fun,
    check_model_free,
  );
}
