//
// Generated by JSON Model Compiler version 2
// see https://github.com/clairey-zx81/json-model
//

import json_model.*;
import java.util.Map;
import java.util.Set;
import java.util.HashMap;
import java.util.HashSet;
import java.util.Iterator;
import java.util.regex.Pattern;
import java.util.regex.Matcher;

@SuppressWarnings("unchecked")
public class starts_with_array extends ModelChecker
{
    static public final String VERSION = "2";

    public Map<String, Checker> starts_with_array_map_pmap;

    // check $ (.)
    public boolean json_model_1(Object val, Path path, Report rep)
    {
        // .
        boolean res = json.isArray(val);
        if (res)
        {
            int arr_0_idx = -1;
            Iterator<Object> arr_0_item_loop = json.arrayIterator(val);
            while (arr_0_item_loop.hasNext())
            {
                arr_0_idx++;
                Object arr_0_item = arr_0_item_loop.next();
                // .0
                // "/^hello world!/"
                res = json.isString(arr_0_item) && json.asString(arr_0_item).startsWith("hello world!");
                if (! res)
                {
                    break;
                }
            }
        }
        return res;
    }


    public void init(JSON json)
    {
        if (!initialized)
        {
            try {
            starts_with_array_map_pmap = new HashMap<String, Checker>();
            starts_with_array_map_pmap.put("", new Checker() { public boolean call(Object o, Path p, Report r) { return json_model_1(o, p, r);} });
                super.init(json);
            }
            catch (Exception e) {
                throw new Error("cannot initialized model checker: " + e);
            }
        }
    }

    public void free()
    {
        if (initialized)
        {
            super.free();
            starts_with_array_map_pmap = null;
        }
    }

    public Checker get(String name)
    {
        return starts_with_array_map_pmap.get(name);
    }

    public Set<String> models()
    {
        return starts_with_array_map_pmap.keySet();
    }

    static public void main(String[] args) throws Exception
    {
        ModelChecker checker = new starts_with_array();
        Main.main("starts_with_array", checker, VERSION, args);
    }
}
