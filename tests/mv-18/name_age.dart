//
// Generated by JSON Model Compiler version 2
// see https://github.com/clairey-zx81/json-model
//
import 'dart:convert';
import 'dart:io';
import 'dart:core';
// Import the helper library generated nearby
import 'package:json_model_runtime/json_model.dart' as runtime;
import 'package:json_model_runtime/main.dart';

//
// Generated JSON Model Validator
//
const String JSON_MODEL_VERSION = "2";

var check_model_map = <String, Function>{};

// check $ (.)
bool json_model_1(dynamic val, List<dynamic>? path, List<dynamic>? rep)
{
    // .
    // check close must only props
    if (! (val is Map))
    {
        rep?.add(["not an object [.]", path]);
        return false;
    }
    if (val.length != 2)
    {
        rep?.add(["bad property count [.]", path]);
        return false;
    }
    List<dynamic>? lpath;
    dynamic pval;
    bool res;
    if (! val.containsKey("name"))
    {
        rep?.add(["missing mandatory prop <name> [.]", path]);
        return false;
    }
    lpath = (path != null ? [...?path, "name"] : null);
    pval = val["name"];
    // .name
    res = (pval is String);
    if (! res)
    {
        rep?.add(["unexpected string [.name]", (path != null ? lpath : null)]);
        rep?.add(["unexpected value for mandatory prop <name> [.]", (path != null ? lpath : null)]);
        return false;
    }
    if (! val.containsKey("age"))
    {
        rep?.add(["missing mandatory prop <age> [.]", path]);
        return false;
    }
    lpath = (path != null ? [...?path, "age"] : null);
    pval = val["age"];
    // .age
    res = (pval is int) && pval >= 0;
    if (! res)
    {
        rep?.add(["not a 0 strict int [.age]", (path != null ? lpath : null)]);
        rep?.add(["unexpected value for mandatory prop <age> [.]", (path != null ? lpath : null)]);
        return false;
    }
    return true;
}


// dart_init.dart
class GlobalState {
  static bool initializer = false;
}

Function? check_model_fun(String name) {
  return check_model_map[name];
}


void check_model_init() {
  if (!GlobalState.initializer){
    GlobalState.initializer = true;
  }
        check_model_map[""] = json_model_1;
}

// dart_free.dart
void check_model_free() {
  GlobalState.initializer = true;
}


void main(List<String> args) {
  runValidation(
    args,
    check_model_init,
    check_model_fun,
    check_model_free,
  );
}
