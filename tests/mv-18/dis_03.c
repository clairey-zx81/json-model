//
// Generated by JSON Model Compiler version 2
// see https://github.com/clairey-zx81/json-model
//

// regular expression engine
#include <stddef.h>
#include <cre2.h>

#include <json-model.h>
#define JSON_MODEL_VERSION "2"

static bool _jm_obj_0(const json_t *val, jm_path_t *path, jm_report_t *rep);
static bool _jm_obj_1(const json_t *val, jm_path_t *path, jm_report_t *rep);
static jm_constmap_t _jm_map_0_tab[2];
static bool json_model_1(const json_t *val, jm_path_t *path, jm_report_t *rep);
jm_propmap_t check_model_map_tab[1];
const size_t check_model_map_size = 1;

// object .'|'.0
static INLINE bool _jm_obj_0(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    // check close must only props
    if (! json_is_object(val))
    {
        if (rep) jm_report_add_entry(rep, "not an object [.'|'.0]", path);
        return false;
    }
    if (json_object_size(val) != 2)
    {
        if (rep) jm_report_add_entry(rep, "bad property count [.'|'.0]", path);
        return false;
    }
    json_t * pval;
    bool res;
    if (! (json_object_get(val, "discriminator") != NULL))
    {
        if (rep) jm_report_add_entry(rep, "missing mandatory prop <discriminator> [.'|'.0]", path);
        return false;
    }
    pval = json_object_get(val, "discriminator");
    // .'|'.0.discriminator
    res = json_is_boolean(pval) && json_boolean_value(pval) == true;
    if (! res)
    {
        if (rep) jm_report_add_entry(rep, "unexpected =true [.'|'.0.discriminator]", path);
        if (rep) jm_report_add_entry(rep, "unexpected value for mandatory prop <discriminator> [.'|'.0]", path);
        return false;
    }
    if (! (json_object_get(val, "x") != NULL))
    {
        if (rep) jm_report_add_entry(rep, "missing mandatory prop <x> [.'|'.0]", path);
        return false;
    }
    pval = json_object_get(val, "x");
    // .'|'.0.x
    res = json_is_string(pval);
    if (! res)
    {
        if (rep) jm_report_add_entry(rep, "unexpected string [.'|'.0.x]", path);
        if (rep) jm_report_add_entry(rep, "unexpected value for mandatory prop <x> [.'|'.0]", path);
        return false;
    }
    return true;
}

// object .'|'.1
static INLINE bool _jm_obj_1(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    // check close must only props
    if (! json_is_object(val))
    {
        if (rep) jm_report_add_entry(rep, "not an object [.'|'.1]", path);
        return false;
    }
    if (json_object_size(val) != 2)
    {
        if (rep) jm_report_add_entry(rep, "bad property count [.'|'.1]", path);
        return false;
    }
    json_t * pval;
    bool res;
    if (! (json_object_get(val, "discriminator") != NULL))
    {
        if (rep) jm_report_add_entry(rep, "missing mandatory prop <discriminator> [.'|'.1]", path);
        return false;
    }
    pval = json_object_get(val, "discriminator");
    // .'|'.1.discriminator
    res = json_is_boolean(pval) && json_boolean_value(pval) == false;
    if (! res)
    {
        if (rep) jm_report_add_entry(rep, "unexpected =false [.'|'.1.discriminator]", path);
        if (rep) jm_report_add_entry(rep, "unexpected value for mandatory prop <discriminator> [.'|'.1]", path);
        return false;
    }
    if (! (json_object_get(val, "y") != NULL))
    {
        if (rep) jm_report_add_entry(rep, "missing mandatory prop <y> [.'|'.1]", path);
        return false;
    }
    pval = json_object_get(val, "y");
    // .'|'.1.y
    res = json_is_string(pval);
    if (! res)
    {
        if (rep) jm_report_add_entry(rep, "unexpected string [.'|'.1.y]", path);
        if (rep) jm_report_add_entry(rep, "unexpected value for mandatory prop <y> [.'|'.1]", path);
        return false;
    }
    return true;
}

static INLINE jm_check_fun_t _jm_map_0(json_t *val)
{
    jm_constant_t cst;
    jm_set_cst(&cst, val);
    return jm_search_constmap(&cst, _jm_map_0_tab, 2);
}

// check $ (.)
static bool json_model_1(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    bool res;
    // boolean tag
    // .
    bool iso_0 = json_is_object(val);
    res = iso_0;
    if (res)
    {
        if (json_object_get(val, "discriminator") != NULL)
        {
            json_t * tag_0 = json_object_get(val, "discriminator");
            jm_check_fun_t fun_0 = _jm_map_0(tag_0);
            if (fun_0 != NULL)
            {
                res = fun_0(val, path, rep);
            }
            else
            {
                res = false;
                if (rep) jm_report_add_entry(rep, "tag <discriminator> value not found [.'|']", path);
            }
        }
        else
        {
            res = false;
            if (rep) jm_report_add_entry(rep, "tag prop <discriminator> is missing [.'|']", path);
        }
    }
    else
    {
        if (rep) jm_report_add_entry(rep, "value is not an object [.'|']", path);
    }
    return res;
}

jm_check_fun_t check_model_map(const char *pname)
{
    return jm_search_propmap(pname, check_model_map_tab, 1);
}

static bool initialized = false;

const char *check_model_init(void)
{
    if (!initialized)
    {
        initialized = true;
        jm_version_string = JSON_MODEL_VERSION;
        _jm_map_0_tab[0] = (jm_constmap_t) { (jm_constant_t) { cst_is_bool, { .b = true } }, _jm_obj_0 };
        _jm_map_0_tab[1] = (jm_constmap_t) { (jm_constant_t) { cst_is_bool, { .b = false } }, _jm_obj_1 };
        jm_sort_constmap(_jm_map_0_tab, 2);
        check_model_map_tab[0] = (jm_propmap_t) { "", json_model_1 };
        jm_sort_propmap(check_model_map_tab, 1);
    }
    return NULL;
}

void check_model_free(void)
{
    if (initialized)
    {
        initialized = false;

        // cleanup code
    }
}

/*
 * # API
 *
 * ```c
 * bool error;
 * char *message;
 *
 * bool valid = check(json_value, "model-name", &error, &message);
 *
 * if (message)
 *    free(message), message = NULL;
 * ```
 *
 * - `error` is set of there was an error during initialization or if the model does not exists.
 * - `message` is provided if a non NULL pointer is passed, and must be freed by the caller.
 */
bool
check_model(const json_t *val, const char *name, bool *error, char **reasons)
{
    return jm_generic_entry(check_model_init, check_model_map, val, name, error, reasons);
}
