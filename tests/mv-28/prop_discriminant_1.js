#! /bin/env node
// we may need require to load re2
import { createRequire } from 'node:module';
const require = createRequire(import.meta.url);
//
// Generated by JSON Model Compiler version 2
// see https://github.com/clairey-zx81/json-model
//
import * as runtime from "json_model_runtime"
const JSON_MODEL_VERSION = "2";

var check_model_map = new Map()

// object .'|'.4
function _jm_obj_0(val, path, rep)
{
    if (! (Object.prototype.toString.call(val) === '[object Object]'))
    {
        return false;
    }
    let res;
    let must_count = 0;
    for (const [prop, pval] of Object.entries(val))
    {
        if (prop == "b")
        {
            // handle must b property
            must_count += 1;
            // .'|'.4.b
            res = ((typeof pval === 'number' || pval instanceof Number) && Number.isInteger(pval)) && pval >= 0;
            if (! res)
            {
                return false;
            }
            continue;
        }
        else if (prop == "c")
        {
            // handle must c property
            must_count += 1;
            // .'|'.4.c
            res = ((typeof pval === 'number' || pval instanceof Number) && Number.isInteger(pval)) && pval >= 0;
            if (! res)
            {
                return false;
            }
            continue;
        }
        else if (prop == "d")
        {
            // handle must d property
            must_count += 1;
            // .'|'.4.d
            res = ((typeof pval === 'number' || pval instanceof Number) && Number.isInteger(pval)) && pval >= 0;
            if (! res)
            {
                return false;
            }
            continue;
        }
        else if (prop == "e")
        {
            // handle must e property
            must_count += 1;
            // .'|'.4.e
            res = ((typeof pval === 'number' || pval instanceof Number) && Number.isInteger(pval)) && pval >= 0;
            if (! res)
            {
                return false;
            }
            continue;
        }
        if (prop == "r")
        {
            // handle may r property
            // .'|'.4.r
            res = ((typeof pval === 'number' || pval instanceof Number) && Number.isInteger(pval)) && pval >= 0;
            if (! res)
            {
                return false;
            }
            continue;
        }
        return false;
    }
    return must_count == 4;
}

// object .'|'.3
function _jm_obj_1(val, path, rep)
{
    if (! (Object.prototype.toString.call(val) === '[object Object]'))
    {
        return false;
    }
    let res;
    let must_count = 0;
    for (const [prop, pval] of Object.entries(val))
    {
        if (prop == "a")
        {
            // handle must a property
            must_count += 1;
            // .'|'.3.a
            res = ((typeof pval === 'number' || pval instanceof Number) && Number.isInteger(pval)) && pval >= 0;
            if (! res)
            {
                return false;
            }
            continue;
        }
        else if (prop == "e")
        {
            // handle must e property
            must_count += 1;
            // .'|'.3.e
            res = ((typeof pval === 'number' || pval instanceof Number) && Number.isInteger(pval)) && pval >= 0;
            if (! res)
            {
                return false;
            }
            continue;
        }
        if (prop == "q")
        {
            // handle may q property
            // .'|'.3.q
            res = ((typeof pval === 'number' || pval instanceof Number) && Number.isInteger(pval)) && pval >= 0;
            if (! res)
            {
                return false;
            }
            continue;
        }
        else if (prop == "s")
        {
            // handle may s property
            // .'|'.3.s
            res = ((typeof pval === 'number' || pval instanceof Number) && Number.isInteger(pval)) && pval >= 0;
            if (! res)
            {
                return false;
            }
            continue;
        }
        return false;
    }
    return must_count == 2;
}

// object .'|'.2
function _jm_obj_2(val, path, rep)
{
    if (! (Object.prototype.toString.call(val) === '[object Object]'))
    {
        return false;
    }
    let res;
    let must_count = 0;
    for (const [prop, pval] of Object.entries(val))
    {
        if (prop == "a")
        {
            // handle must a property
            must_count += 1;
            // .'|'.2.a
            res = ((typeof pval === 'number' || pval instanceof Number) && Number.isInteger(pval)) && pval >= 0;
            if (! res)
            {
                return false;
            }
            continue;
        }
        else if (prop == "d")
        {
            // handle must d property
            must_count += 1;
            // .'|'.2.d
            res = ((typeof pval === 'number' || pval instanceof Number) && Number.isInteger(pval)) && pval >= 0;
            if (! res)
            {
                return false;
            }
            continue;
        }
        if (prop == "u")
        {
            // handle may u property
            // .'|'.2.u
            res = ((typeof pval === 'number' || pval instanceof Number) && Number.isInteger(pval)) && pval >= 0;
            if (! res)
            {
                return false;
            }
            continue;
        }
        else if (prop == "x")
        {
            // handle may x property
            // .'|'.2.x
            res = ((typeof pval === 'number' || pval instanceof Number) && Number.isInteger(pval)) && pval >= 0;
            if (! res)
            {
                return false;
            }
            continue;
        }
        return false;
    }
    return must_count == 2;
}

// object .'|'.1
function _jm_obj_3(val, path, rep)
{
    if (! (Object.prototype.toString.call(val) === '[object Object]'))
    {
        return false;
    }
    let res;
    let must_count = 0;
    for (const [prop, pval] of Object.entries(val))
    {
        if (prop == "a")
        {
            // handle must a property
            must_count += 1;
            // .'|'.1.a
            res = ((typeof pval === 'number' || pval instanceof Number) && Number.isInteger(pval)) && pval >= 0;
            if (! res)
            {
                return false;
            }
            continue;
        }
        else if (prop == "c")
        {
            // handle must c property
            must_count += 1;
            // .'|'.1.c
            res = ((typeof pval === 'number' || pval instanceof Number) && Number.isInteger(pval)) && pval >= 0;
            if (! res)
            {
                return false;
            }
            continue;
        }
        if (prop == "v")
        {
            // handle may v property
            // .'|'.1.v
            res = ((typeof pval === 'number' || pval instanceof Number) && Number.isInteger(pval)) && pval >= 0;
            if (! res)
            {
                return false;
            }
            continue;
        }
        else if (prop == "z")
        {
            // handle may z property
            // .'|'.1.z
            res = ((typeof pval === 'number' || pval instanceof Number) && Number.isInteger(pval)) && pval >= 0;
            if (! res)
            {
                return false;
            }
            continue;
        }
        return false;
    }
    return must_count == 2;
}

// object .'|'.0
function _jm_obj_4(val, path, rep)
{
    if (! (Object.prototype.toString.call(val) === '[object Object]'))
    {
        return false;
    }
    let res;
    let must_count = 0;
    for (const [prop, pval] of Object.entries(val))
    {
        if (prop == "a")
        {
            // handle must a property
            must_count += 1;
            // .'|'.0.a
            res = ((typeof pval === 'number' || pval instanceof Number) && Number.isInteger(pval)) && pval >= 0;
            if (! res)
            {
                return false;
            }
            continue;
        }
        else if (prop == "b")
        {
            // handle must b property
            must_count += 1;
            // .'|'.0.b
            res = ((typeof pval === 'number' || pval instanceof Number) && Number.isInteger(pval)) && pval >= 0;
            if (! res)
            {
                return false;
            }
            continue;
        }
        if (prop == "w")
        {
            // handle may w property
            // .'|'.0.w
            res = ((typeof pval === 'number' || pval instanceof Number) && Number.isInteger(pval)) && pval >= 0;
            if (! res)
            {
                return false;
            }
            continue;
        }
        else if (prop == "z")
        {
            // handle may z property
            // .'|'.0.z
            res = ((typeof pval === 'number' || pval instanceof Number) && Number.isInteger(pval)) && pval >= 0;
            if (! res)
            {
                return false;
            }
            continue;
        }
        return false;
    }
    return must_count == 2;
}

// check $ (.)
function json_model_1(val, path, rep)
{
    // objects with partial incompatible mandatory properties
    // .
    return Object.prototype.toString.call(val) === '[object Object]' && (val.hasOwnProperty("b") && _jm_obj_4(val, null, null) || val.hasOwnProperty("c") && _jm_obj_3(val, null, null) || val.hasOwnProperty("d") && _jm_obj_2(val, null, null) || val.hasOwnProperty("e") && _jm_obj_1(val, null, null) || val.hasOwnProperty("b") && _jm_obj_0(val, null, null));
}


var initialized = false

// differed module initializations
export function check_model_init()
{
    if (! initialized)
    {
        initialized = true;
        runtime.jm_set_rx(RegExp)
        check_model_map.set("", json_model_1)
    }
}

// differed module cleanup
export function check_model_free()
{
    if (initialized)
    {
        initialized = false;
    }
}

export function check_model(val, name, rep)
{
    let checker = check_model_map.get(name)
    if (checker === undefined)
        throw `no checker for "${name}"`
    let path = rep !== null ? [] : null

    return checker(val, path, rep)
}

// possibly run as main based on a guess
import main from "json_model_runtime/main.js"

if (import.meta.url.endsWith(process.argv[1]))
    main(check_model_init, check_model, check_model_free)
