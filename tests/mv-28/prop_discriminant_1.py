#! /bin/env python
#
# Generated by JSON Model Compiler version 2
# see https://github.com/clairey-zx81/json-model
#
from typing import Callable
import re2 as re
from json_model.runtime import *
__version__ = "2"

def check_model_fun(name: str) -> CheckFun:
    """Return check function for JSON model name."""
    return check_model_map[name]

# entry point for generated checkers
def check_model(val: Jsonable, name: str = "", rep: Report = None) -> bool:
    """Check val validity against JSON Model name."""
    if name not in check_model_map:
        raise Exception(f"unexpected model name: {name}")
    checker = check_model_map[name]
    return checker(val, [], rep)

check_model_map: PropMap

# object .'|'.4
def _jm_obj_0(val: Jsonable, path: Path, rep: Report) -> bool:
    if not isinstance(val, dict):
        return False
    res: bool
    must_count: int = 0
    for prop, pval in val.items():
        if prop == "b":
            # handle must b property
            must_count += 1
            # .'|'.4.b
            res = isinstance(pval, int) and not isinstance(pval, bool) and pval >= 0
            if not res:
                return False
            continue
        elif prop == "c":
            # handle must c property
            must_count += 1
            # .'|'.4.c
            res = isinstance(pval, int) and not isinstance(pval, bool) and pval >= 0
            if not res:
                return False
            continue
        elif prop == "d":
            # handle must d property
            must_count += 1
            # .'|'.4.d
            res = isinstance(pval, int) and not isinstance(pval, bool) and pval >= 0
            if not res:
                return False
            continue
        elif prop == "e":
            # handle must e property
            must_count += 1
            # .'|'.4.e
            res = isinstance(pval, int) and not isinstance(pval, bool) and pval >= 0
            if not res:
                return False
            continue
        if prop == "r":
            # handle may r property
            # .'|'.4.r
            res = isinstance(pval, int) and not isinstance(pval, bool) and pval >= 0
            if not res:
                return False
            continue
        return False
    return must_count == 4

# object .'|'.3
def _jm_obj_1(val: Jsonable, path: Path, rep: Report) -> bool:
    if not isinstance(val, dict):
        return False
    res: bool
    must_count: int = 0
    for prop, pval in val.items():
        if prop == "a":
            # handle must a property
            must_count += 1
            # .'|'.3.a
            res = isinstance(pval, int) and not isinstance(pval, bool) and pval >= 0
            if not res:
                return False
            continue
        elif prop == "e":
            # handle must e property
            must_count += 1
            # .'|'.3.e
            res = isinstance(pval, int) and not isinstance(pval, bool) and pval >= 0
            if not res:
                return False
            continue
        if prop == "q":
            # handle may q property
            # .'|'.3.q
            res = isinstance(pval, int) and not isinstance(pval, bool) and pval >= 0
            if not res:
                return False
            continue
        elif prop == "s":
            # handle may s property
            # .'|'.3.s
            res = isinstance(pval, int) and not isinstance(pval, bool) and pval >= 0
            if not res:
                return False
            continue
        return False
    return must_count == 2

# object .'|'.2
def _jm_obj_2(val: Jsonable, path: Path, rep: Report) -> bool:
    if not isinstance(val, dict):
        return False
    res: bool
    must_count: int = 0
    for prop, pval in val.items():
        if prop == "a":
            # handle must a property
            must_count += 1
            # .'|'.2.a
            res = isinstance(pval, int) and not isinstance(pval, bool) and pval >= 0
            if not res:
                return False
            continue
        elif prop == "d":
            # handle must d property
            must_count += 1
            # .'|'.2.d
            res = isinstance(pval, int) and not isinstance(pval, bool) and pval >= 0
            if not res:
                return False
            continue
        if prop == "u":
            # handle may u property
            # .'|'.2.u
            res = isinstance(pval, int) and not isinstance(pval, bool) and pval >= 0
            if not res:
                return False
            continue
        elif prop == "x":
            # handle may x property
            # .'|'.2.x
            res = isinstance(pval, int) and not isinstance(pval, bool) and pval >= 0
            if not res:
                return False
            continue
        return False
    return must_count == 2

# object .'|'.1
def _jm_obj_3(val: Jsonable, path: Path, rep: Report) -> bool:
    if not isinstance(val, dict):
        return False
    res: bool
    must_count: int = 0
    for prop, pval in val.items():
        if prop == "a":
            # handle must a property
            must_count += 1
            # .'|'.1.a
            res = isinstance(pval, int) and not isinstance(pval, bool) and pval >= 0
            if not res:
                return False
            continue
        elif prop == "c":
            # handle must c property
            must_count += 1
            # .'|'.1.c
            res = isinstance(pval, int) and not isinstance(pval, bool) and pval >= 0
            if not res:
                return False
            continue
        if prop == "v":
            # handle may v property
            # .'|'.1.v
            res = isinstance(pval, int) and not isinstance(pval, bool) and pval >= 0
            if not res:
                return False
            continue
        elif prop == "z":
            # handle may z property
            # .'|'.1.z
            res = isinstance(pval, int) and not isinstance(pval, bool) and pval >= 0
            if not res:
                return False
            continue
        return False
    return must_count == 2

# object .'|'.0
def _jm_obj_4(val: Jsonable, path: Path, rep: Report) -> bool:
    if not isinstance(val, dict):
        return False
    res: bool
    must_count: int = 0
    for prop, pval in val.items():
        if prop == "a":
            # handle must a property
            must_count += 1
            # .'|'.0.a
            res = isinstance(pval, int) and not isinstance(pval, bool) and pval >= 0
            if not res:
                return False
            continue
        elif prop == "b":
            # handle must b property
            must_count += 1
            # .'|'.0.b
            res = isinstance(pval, int) and not isinstance(pval, bool) and pval >= 0
            if not res:
                return False
            continue
        if prop == "w":
            # handle may w property
            # .'|'.0.w
            res = isinstance(pval, int) and not isinstance(pval, bool) and pval >= 0
            if not res:
                return False
            continue
        elif prop == "z":
            # handle may z property
            # .'|'.0.z
            res = isinstance(pval, int) and not isinstance(pval, bool) and pval >= 0
            if not res:
                return False
            continue
        return False
    return must_count == 2

# check $ (.)
def json_model_1(val: Jsonable, path: Path, rep: Report) -> bool:
    # objects with partial incompatible mandatory properties
    # .
    return isinstance(val, dict) and ("b" in val and _jm_obj_4(val, None, None) or "c" in val and _jm_obj_3(val, None, None) or "d" in val and _jm_obj_2(val, None, None) or "e" in val and _jm_obj_1(val, None, None) or "b" in val and _jm_obj_0(val, None, None))


# initialization guard
initialized: bool = False

# differed module initializations
def check_model_init():
    global initialized
    if not initialized:
        initialized = True
        global check_model_map
        check_model_map = {
            "": json_model_1,
        }

# differed module cleanup
def check_model_free():
    global initialized
    if initialized:
        initialized = False

if __name__ == "__main__":
    check_model_init()
    main(check_model_fun, check_model_map, __version__)
    check_model_free()
