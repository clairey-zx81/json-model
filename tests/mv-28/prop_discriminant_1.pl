#! /usr/bin/perl
#
# Generated by JSON Model Compiler version 2
# see https://github.com/clairey-zx81/json-model
#
use strict;
use warnings;
no warnings "uninitialized";
use re::engine::RE2;
use JSON::JsonModel;
use constant JMC_VERSION => '2';


sub _jm_obj_0($$$);
sub _jm_obj_1($$$);
sub _jm_obj_2($$$);
sub _jm_obj_3($$$);
sub _jm_obj_4($$$);
sub json_model_1($$$);
my %check_model_map;

# object .'|'.4
sub _jm_obj_0($$$)
{
    my ($val, $path, $rep) = @_;
    if (! jm_is_object($val))
    {
        return 0;
    }
    my $res;
    my $must_count = 0;
    scalar keys %$val;
    while (my ($prop, $pval) = each %$val)
    {
        if ($prop eq 'b')
        {
            # handle must b property
            $must_count++;
            # .'|'.4.b
            $res = jm_is_integer($pval) && $pval >= 0;
            if (! $res)
            {
                return 0;
            }
            next;
        }
        elsif ($prop eq 'c')
        {
            # handle must c property
            $must_count++;
            # .'|'.4.c
            $res = jm_is_integer($pval) && $pval >= 0;
            if (! $res)
            {
                return 0;
            }
            next;
        }
        elsif ($prop eq 'd')
        {
            # handle must d property
            $must_count++;
            # .'|'.4.d
            $res = jm_is_integer($pval) && $pval >= 0;
            if (! $res)
            {
                return 0;
            }
            next;
        }
        elsif ($prop eq 'e')
        {
            # handle must e property
            $must_count++;
            # .'|'.4.e
            $res = jm_is_integer($pval) && $pval >= 0;
            if (! $res)
            {
                return 0;
            }
            next;
        }
        if ($prop eq 'r')
        {
            # handle may r property
            # .'|'.4.r
            $res = jm_is_integer($pval) && $pval >= 0;
            if (! $res)
            {
                return 0;
            }
            next;
        }
        return 0;
    }
    return $must_count == 4;
}

# object .'|'.3
sub _jm_obj_1($$$)
{
    my ($val, $path, $rep) = @_;
    if (! jm_is_object($val))
    {
        return 0;
    }
    my $res;
    my $must_count = 0;
    scalar keys %$val;
    while (my ($prop, $pval) = each %$val)
    {
        if ($prop eq 'a')
        {
            # handle must a property
            $must_count++;
            # .'|'.3.a
            $res = jm_is_integer($pval) && $pval >= 0;
            if (! $res)
            {
                return 0;
            }
            next;
        }
        elsif ($prop eq 'e')
        {
            # handle must e property
            $must_count++;
            # .'|'.3.e
            $res = jm_is_integer($pval) && $pval >= 0;
            if (! $res)
            {
                return 0;
            }
            next;
        }
        if ($prop eq 'q')
        {
            # handle may q property
            # .'|'.3.q
            $res = jm_is_integer($pval) && $pval >= 0;
            if (! $res)
            {
                return 0;
            }
            next;
        }
        elsif ($prop eq 's')
        {
            # handle may s property
            # .'|'.3.s
            $res = jm_is_integer($pval) && $pval >= 0;
            if (! $res)
            {
                return 0;
            }
            next;
        }
        return 0;
    }
    return $must_count == 2;
}

# object .'|'.2
sub _jm_obj_2($$$)
{
    my ($val, $path, $rep) = @_;
    if (! jm_is_object($val))
    {
        return 0;
    }
    my $res;
    my $must_count = 0;
    scalar keys %$val;
    while (my ($prop, $pval) = each %$val)
    {
        if ($prop eq 'a')
        {
            # handle must a property
            $must_count++;
            # .'|'.2.a
            $res = jm_is_integer($pval) && $pval >= 0;
            if (! $res)
            {
                return 0;
            }
            next;
        }
        elsif ($prop eq 'd')
        {
            # handle must d property
            $must_count++;
            # .'|'.2.d
            $res = jm_is_integer($pval) && $pval >= 0;
            if (! $res)
            {
                return 0;
            }
            next;
        }
        if ($prop eq 'u')
        {
            # handle may u property
            # .'|'.2.u
            $res = jm_is_integer($pval) && $pval >= 0;
            if (! $res)
            {
                return 0;
            }
            next;
        }
        elsif ($prop eq 'x')
        {
            # handle may x property
            # .'|'.2.x
            $res = jm_is_integer($pval) && $pval >= 0;
            if (! $res)
            {
                return 0;
            }
            next;
        }
        return 0;
    }
    return $must_count == 2;
}

# object .'|'.1
sub _jm_obj_3($$$)
{
    my ($val, $path, $rep) = @_;
    if (! jm_is_object($val))
    {
        return 0;
    }
    my $res;
    my $must_count = 0;
    scalar keys %$val;
    while (my ($prop, $pval) = each %$val)
    {
        if ($prop eq 'a')
        {
            # handle must a property
            $must_count++;
            # .'|'.1.a
            $res = jm_is_integer($pval) && $pval >= 0;
            if (! $res)
            {
                return 0;
            }
            next;
        }
        elsif ($prop eq 'c')
        {
            # handle must c property
            $must_count++;
            # .'|'.1.c
            $res = jm_is_integer($pval) && $pval >= 0;
            if (! $res)
            {
                return 0;
            }
            next;
        }
        if ($prop eq 'v')
        {
            # handle may v property
            # .'|'.1.v
            $res = jm_is_integer($pval) && $pval >= 0;
            if (! $res)
            {
                return 0;
            }
            next;
        }
        elsif ($prop eq 'z')
        {
            # handle may z property
            # .'|'.1.z
            $res = jm_is_integer($pval) && $pval >= 0;
            if (! $res)
            {
                return 0;
            }
            next;
        }
        return 0;
    }
    return $must_count == 2;
}

# object .'|'.0
sub _jm_obj_4($$$)
{
    my ($val, $path, $rep) = @_;
    if (! jm_is_object($val))
    {
        return 0;
    }
    my $res;
    my $must_count = 0;
    scalar keys %$val;
    while (my ($prop, $pval) = each %$val)
    {
        if ($prop eq 'a')
        {
            # handle must a property
            $must_count++;
            # .'|'.0.a
            $res = jm_is_integer($pval) && $pval >= 0;
            if (! $res)
            {
                return 0;
            }
            next;
        }
        elsif ($prop eq 'b')
        {
            # handle must b property
            $must_count++;
            # .'|'.0.b
            $res = jm_is_integer($pval) && $pval >= 0;
            if (! $res)
            {
                return 0;
            }
            next;
        }
        if ($prop eq 'w')
        {
            # handle may w property
            # .'|'.0.w
            $res = jm_is_integer($pval) && $pval >= 0;
            if (! $res)
            {
                return 0;
            }
            next;
        }
        elsif ($prop eq 'z')
        {
            # handle may z property
            # .'|'.0.z
            $res = jm_is_integer($pval) && $pval >= 0;
            if (! $res)
            {
                return 0;
            }
            next;
        }
        return 0;
    }
    return $must_count == 2;
}

# check $ (.)
sub json_model_1($$$)
{
    my ($val, $path, $rep) = @_;
    # objects with partial incompatible mandatory properties
    # .
    return jm_is_object($val) && (exists $$val{'b'} && _jm_obj_4($val, undef, undef) || exists $$val{'c'} && _jm_obj_3($val, undef, undef) || exists $$val{'d'} && _jm_obj_2($val, undef, undef) || exists $$val{'e'} && _jm_obj_1($val, undef, undef) || exists $$val{'b'} && _jm_obj_0($val, undef, undef));
}


# initialization of global variables

our $initialized = 0;

sub check_model_init()
{
    if (!$initialized)
    {
        $initialized = 1;
        %check_model_map = (
            '' => \&json_model_1,
        );
    }
}

sub check_model_free()
{
    if ($initialized)
    {
        $initialized = 0;
        %check_model_map = ();
    }
}

sub check_model($$$)
{
    my ($json, $name, $rep) = @_;
    die "unexpected model name \"$name\"" unless exists $check_model_map{$name};
    my $path = defined $rep ? [] : undef;
    return $check_model_map{$name}($json, $path, $rep);
}

#
# SCRIPT EXECUTION
#
check_model_init();
jm_main(\&check_model, \%check_model_map, JMC_VERSION);
check_model_free();
