//
// Generated by JSON Model Compiler version 2
// see https://github.com/clairey-zx81/json-model
//

import json_model.*;
import java.util.Map;
import java.util.Set;
import java.util.HashMap;
import java.util.HashSet;
import java.util.Iterator;
import java.util.regex.Pattern;
import java.util.regex.Matcher;

@SuppressWarnings("unchecked")
public class prop_discriminant_3 extends ModelChecker
{
    static public final String VERSION = "2";

    public Map<String, Checker> prop_discriminant_3_map_pmap;

    // object .'|'.4
    public boolean _jm_obj_0(Object val, Path path, Report rep)
    {
        if (! json.isObject(val))
        {
            return false;
        }
        boolean res;
        long must_count = 0;
        Iterator<String> prop_loop = json.objectIterator(val);
        while (prop_loop.hasNext())
        {
            String prop = prop_loop.next();
            Object pval = json.objectValue(val, prop);
            if (prop.compareTo("a") == 0)
            {
                // handle must a property
                must_count += 1;
                // .'|'.4.a
                res = json.isInteger(pval) && json.asLong(pval) >= 0;
                if (! res)
                {
                    return false;
                }
                continue;
            }
            else if (prop.compareTo("c") == 0)
            {
                // handle must c property
                must_count += 1;
                // .'|'.4.c
                res = json.isInteger(pval) && json.asLong(pval) >= 0;
                if (! res)
                {
                    return false;
                }
                continue;
            }
            else if (prop.compareTo("e") == 0)
            {
                // handle must e property
                must_count += 1;
                // .'|'.4.e
                res = json.isInteger(pval) && json.asLong(pval) >= 0;
                if (! res)
                {
                    return false;
                }
                continue;
            }
            if (prop.compareTo("r") == 0)
            {
                // handle may r property
                // .'|'.4.r
                res = json.isInteger(pval) && json.asLong(pval) >= 0;
                if (! res)
                {
                    return false;
                }
                continue;
            }
            return false;
        }
        return must_count == 3;
    }

    // object .'|'.3
    public boolean _jm_obj_1(Object val, Path path, Report rep)
    {
        if (! json.isObject(val))
        {
            return false;
        }
        boolean res;
        long must_count = 0;
        Iterator<String> prop_loop = json.objectIterator(val);
        while (prop_loop.hasNext())
        {
            String prop = prop_loop.next();
            Object pval = json.objectValue(val, prop);
            if (prop.compareTo("a") == 0)
            {
                // handle must a property
                must_count += 1;
                // .'|'.3.a
                res = json.isInteger(pval) && json.asLong(pval) >= 0;
                if (! res)
                {
                    return false;
                }
                continue;
            }
            else if (prop.compareTo("e") == 0)
            {
                // handle must e property
                must_count += 1;
                // .'|'.3.e
                res = json.isInteger(pval) && json.asLong(pval) >= 0;
                if (! res)
                {
                    return false;
                }
                continue;
            }
            if (prop.compareTo("q") == 0)
            {
                // handle may q property
                // .'|'.3.q
                res = json.isInteger(pval) && json.asLong(pval) >= 0;
                if (! res)
                {
                    return false;
                }
                continue;
            }
            else if (prop.compareTo("s") == 0)
            {
                // handle may s property
                // .'|'.3.s
                res = json.isInteger(pval) && json.asLong(pval) >= 0;
                if (! res)
                {
                    return false;
                }
                continue;
            }
            return false;
        }
        return must_count == 2;
    }

    // object .'|'.2
    public boolean _jm_obj_2(Object val, Path path, Report rep)
    {
        if (! json.isObject(val))
        {
            return false;
        }
        boolean res;
        long must_count = 0;
        Iterator<String> prop_loop = json.objectIterator(val);
        while (prop_loop.hasNext())
        {
            String prop = prop_loop.next();
            Object pval = json.objectValue(val, prop);
            if (prop.compareTo("a") == 0)
            {
                // handle must a property
                must_count += 1;
                // .'|'.2.a
                res = json.isInteger(pval) && json.asLong(pval) >= 0;
                if (! res)
                {
                    return false;
                }
                continue;
            }
            else if (prop.compareTo("d") == 0)
            {
                // handle must d property
                must_count += 1;
                // .'|'.2.d
                res = json.isInteger(pval) && json.asLong(pval) >= 0;
                if (! res)
                {
                    return false;
                }
                continue;
            }
            if (prop.compareTo("u") == 0)
            {
                // handle may u property
                // .'|'.2.u
                res = json.isInteger(pval) && json.asLong(pval) >= 0;
                if (! res)
                {
                    return false;
                }
                continue;
            }
            else if (prop.compareTo("x") == 0)
            {
                // handle may x property
                // .'|'.2.x
                res = json.isInteger(pval) && json.asLong(pval) >= 0;
                if (! res)
                {
                    return false;
                }
                continue;
            }
            return false;
        }
        return must_count == 2;
    }

    // object .'|'.1
    public boolean _jm_obj_3(Object val, Path path, Report rep)
    {
        if (! json.isObject(val))
        {
            return false;
        }
        boolean res;
        long must_count = 0;
        Iterator<String> prop_loop = json.objectIterator(val);
        while (prop_loop.hasNext())
        {
            String prop = prop_loop.next();
            Object pval = json.objectValue(val, prop);
            if (prop.compareTo("a") == 0)
            {
                // handle must a property
                must_count += 1;
                // .'|'.1.a
                res = json.isInteger(pval) && json.asLong(pval) >= 0;
                if (! res)
                {
                    return false;
                }
                continue;
            }
            else if (prop.compareTo("c") == 0)
            {
                // handle must c property
                must_count += 1;
                // .'|'.1.c
                res = json.isInteger(pval) && json.asLong(pval) >= 0;
                if (! res)
                {
                    return false;
                }
                continue;
            }
            if (prop.compareTo("v") == 0)
            {
                // handle may v property
                // .'|'.1.v
                res = json.isInteger(pval) && json.asLong(pval) >= 0;
                if (! res)
                {
                    return false;
                }
                continue;
            }
            else if (prop.compareTo("z") == 0)
            {
                // handle may z property
                // .'|'.1.z
                res = json.isInteger(pval) && json.asLong(pval) >= 0;
                if (! res)
                {
                    return false;
                }
                continue;
            }
            return false;
        }
        return must_count == 2;
    }

    // object .'|'.0
    public boolean _jm_obj_4(Object val, Path path, Report rep)
    {
        if (! json.isObject(val))
        {
            return false;
        }
        boolean res;
        long must_count = 0;
        Iterator<String> prop_loop = json.objectIterator(val);
        while (prop_loop.hasNext())
        {
            String prop = prop_loop.next();
            Object pval = json.objectValue(val, prop);
            if (prop.compareTo("a") == 0)
            {
                // handle must a property
                must_count += 1;
                // .'|'.0.a
                res = json.isInteger(pval) && json.asLong(pval) >= 0;
                if (! res)
                {
                    return false;
                }
                continue;
            }
            else if (prop.compareTo("b") == 0)
            {
                // handle must b property
                must_count += 1;
                // .'|'.0.b
                res = json.isInteger(pval) && json.asLong(pval) >= 0;
                if (! res)
                {
                    return false;
                }
                continue;
            }
            if (prop.compareTo("w") == 0)
            {
                // handle may w property
                // .'|'.0.w
                res = json.isInteger(pval) && json.asLong(pval) >= 0;
                if (! res)
                {
                    return false;
                }
                continue;
            }
            else if (prop.compareTo("z") == 0)
            {
                // handle may z property
                // .'|'.0.z
                res = json.isInteger(pval) && json.asLong(pval) >= 0;
                if (! res)
                {
                    return false;
                }
                continue;
            }
            return false;
        }
        return must_count == 2;
    }

    // check $ (.)
    public boolean json_model_1(Object val, Path path, Report rep)
    {
        // objects with full (b, d) and partial (c, e) incompatible mandatory properties
        // .
        boolean res = json.isObject(val);
        if (res)
        {
            if (json.objectHasProp(val, "b"))
            {
                // .'|'.0
                res = _jm_obj_4(val, null, null);
            }
            else
            {
                res = json.objectHasProp(val, "c") && _jm_obj_3(val, null, null);
                if (! res)
                {
                    if (json.objectHasProp(val, "d"))
                    {
                        // .'|'.2
                        res = _jm_obj_2(val, null, null);
                    }
                    else
                    {
                        res = json.objectHasProp(val, "e") && _jm_obj_1(val, null, null) || json.objectHasProp(val, "c") && _jm_obj_0(val, null, null);
                    }
                }
            }
        }
        return res;
    }


    public void init(JSON json)
    {
        if (!initialized)
        {
            try {
            prop_discriminant_3_map_pmap = new HashMap<String, Checker>();
            prop_discriminant_3_map_pmap.put("", new Checker() { public boolean call(Object o, Path p, Report r) { return json_model_1(o, p, r);} });
                super.init(json);
            }
            catch (Exception e) {
                throw new Error("cannot initialized model checker: " + e);
            }
        }
    }

    public void free()
    {
        if (initialized)
        {
            super.free();
            prop_discriminant_3_map_pmap = null;
        }
    }

    public Checker get(String name)
    {
        return prop_discriminant_3_map_pmap.get(name);
    }

    public Set<String> models()
    {
        return prop_discriminant_3_map_pmap.keySet();
    }

    static public void main(String[] args) throws Exception
    {
        ModelChecker checker = new prop_discriminant_3();
        Main.main("prop_discriminant_3", checker, VERSION, args);
    }
}
