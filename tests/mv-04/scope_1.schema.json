{
  "$comment": "JSON Schema generated by JSON Model Compiler",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "a string with a lower case later or a digit",
  "$defs": {
    "r": {
      "type": "array",
      "prefixItems": [
        {
          "$ref": "#/$defs/s"
        },
        {
          "$ref": "#/$defs/s"
        }
      ],
      "items": false,
      "minItems": 2
    },
    "s": {
      "type": "string",
      "pattern": "[a-z]"
    }
  },
  "anyOf": [
    {
      "$ref": "#/$defs/s"
    },
    {
      "$ref": "#/$defs/r#s"
    }
  ]
}
