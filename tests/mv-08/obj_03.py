#! /bin/env python
#
# Generated by JSON Model Compiler version 2
# see https://github.com/clairey-zx81/json-model
#
import math
from typing import Callable
import re2 as re
from json_model.runtime import *
__version__ = "2"

def check_model_fun(name: str) -> CheckFun:
    """Return check function for JSON model name."""
    return check_model_map[name]

# entry point for generated checkers
def check_model(val: Jsonable, name: str = "", rep: Report = None) -> bool:
    """Check val validity against JSON Model name."""
    if name not in check_model_map:
        raise Exception(f"unexpected model name: {name}")
    checker = check_model_map[name]
    return checker(val, [], rep)

check_model_map: PropMap

# check $foo (.'$foo')
def json_model_3(val: Jsonable, path: Path, rep: Report) -> bool:
    # .'$foo'
    # .'$foo'.'|'.0
    res: bool = json_model_4(val, path, rep)
    if not res:
        rep is None or rep.append(("unexpected value for model \"$li0\" [.'$foo'.'|'.0]", path))
        # .'$foo'.'|'.1
        res = json_model_5(val, path, rep)
        if not res:
            rep is None or rep.append(("unexpected value for model \"$ls0\" [.'$foo'.'|'.1]", path))
            # .'$foo'.'|'.2
            res = json_model_6(val, path, rep)
            if not res:
                rep is None or rep.append(("unexpected value for model \"$lb0\" [.'$foo'.'|'.2]", path))
                # .'$foo'.'|'.3
                res = json_model_7(val, path, rep)
                if not res:
                    rep is None or rep.append(("unexpected value for model \"$lf0\" [.'$foo'.'|'.3]", path))
    if res:
        rep is None or rep.clear()
    else:
        rep is None or rep.append(("no model matched [.'$foo'.'|']", path))
    return res

# check $ (.)
def json_model_1(val: Jsonable, path: Path, rep: Report) -> bool:
    # .
    res: bool = json_model_5(val, path, rep)
    if not res:
        rep is None or rep.append(("unexpected value for model \"$foo#ls0\" [.]", path))
    return res

# check $foo#li0 (.'$foo#li0')
def json_model_4(val: Jsonable, path: Path, rep: Report) -> bool:
    # .'$foo#li0'
    res: bool = isinstance(val, list)
    if res:
        for arr_0_idx, arr_0_item in enumerate(val):
            arr_0_lpath: Path = (path + [ arr_0_idx ]) if path is not None else None
            # .'$foo#li0'.0
            res = isinstance(arr_0_item, int) and not isinstance(arr_0_item, bool) and arr_0_item >= 0
            if not res:
                rep is None or rep.append(("not a 0 strict int [.'$foo#li0'.0]", arr_0_lpath if path is not None else None))
                break
    if not res:
        rep is None or rep.append(("not array or unexpected array [.'$foo#li0']", path))
    return res

# check $foo#ls0 (.'$foo#ls0')
def json_model_5(val: Jsonable, path: Path, rep: Report) -> bool:
    # .'$foo#ls0'
    res: bool = isinstance(val, list)
    if res:
        for arr_1_idx, arr_1_item in enumerate(val):
            arr_1_lpath: Path = (path + [ arr_1_idx ]) if path is not None else None
            # .'$foo#ls0'.0
            res = is_valid_date(arr_1_item, arr_1_lpath if path is not None else None, rep)
            if not res:
                rep is None or rep.append(("unexpected value for model \"$DATE\" [.'$foo#ls0'.0]", arr_1_lpath if path is not None else None))
                break
    if not res:
        rep is None or rep.append(("not array or unexpected array [.'$foo#ls0']", path))
    return res

# check $foo#lb0 (.'$foo#lb0')
def json_model_6(val: Jsonable, path: Path, rep: Report) -> bool:
    # .'$foo#lb0'
    res: bool = isinstance(val, list)
    if res:
        for arr_2_idx, arr_2_item in enumerate(val):
            arr_2_lpath: Path = (path + [ arr_2_idx ]) if path is not None else None
            # .'$foo#lb0'.0
            res = isinstance(arr_2_item, bool)
            if not res:
                rep is None or rep.append(("unexpected value for model \"$BOOL\" [.'$foo#lb0'.0]", arr_2_lpath if path is not None else None))
                break
    if not res:
        rep is None or rep.append(("not array or unexpected array [.'$foo#lb0']", path))
    return res

# check $foo#lf0 (.'$foo#lf0')
def json_model_7(val: Jsonable, path: Path, rep: Report) -> bool:
    # .'$foo#lf0'
    res: bool = isinstance(val, list)
    if res:
        for arr_3_idx, arr_3_item in enumerate(val):
            arr_3_lpath: Path = (path + [ arr_3_idx ]) if path is not None else None
            # .'$foo#lf0'.0
            res = isinstance(arr_3_item, float) and arr_3_item >= 0.0
            if not res:
                rep is None or rep.append(("not a 0.0 strict float [.'$foo#lf0'.0]", arr_3_lpath if path is not None else None))
                break
    if not res:
        rep is None or rep.append(("not array or unexpected array [.'$foo#lf0']", path))
    return res


# initialization guard
initialized: bool = False

# differed module initializations
def check_model_init():
    global initialized
    if not initialized:
        initialized = True
        global check_model_map
        check_model_map = {
            "": json_model_5,
            "foo": json_model_3,
        }

# differed module cleanup
def check_model_free():
    global initialized
    if initialized:
        initialized = False

if __name__ == "__main__":
    check_model_init()
    main(check_model_fun, check_model_map, __version__)
    check_model_free()
