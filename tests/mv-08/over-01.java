//
// Generated by JSON Model Compiler version 2
// see https://github.com/clairey-zx81/json-model
//

import json_model.*;
import java.util.Map;
import java.util.Set;
import java.util.HashMap;
import java.util.HashSet;
import java.util.Iterator;
import java.util.regex.Pattern;
import java.util.regex.Matcher;

@SuppressWarnings("unchecked")
public class over_01 extends ModelChecker
{
    static public final String VERSION = "2";

    public Map<String, Checker> over_01_map_pmap;

    // check $over (.'$over')
    public boolean json_model_3(Object val, Path path, Report rep)
    {
        boolean res;
        // .'$over'
        res = json_model_4(val, path, rep);
        if (! res)
        {
            if (rep != null) rep.addEntry("unexpected $Foo [.'$over']", path);
        }
        return res;
    }

    // check $ (.)
    public boolean json_model_1(Object val, Path path, Report rep)
    {
        boolean res;
        // .
        res = json_model_4(val, path, rep);
        if (! res)
        {
            if (rep != null) rep.addEntry("unexpected $over#Foo [.]", path);
        }
        return res;
    }

    // object .'$over#Foo'
    public boolean _jm_obj_0(Object val, Path path, Report rep)
    {
        if (! json.isObject(val))
        {
            if (rep != null) rep.addEntry("not an object [.'$over#Foo']", path);
            return false;
        }
        boolean res;
        Iterator<String> prop_loop = json.objectIterator(val);
        while (prop_loop.hasNext())
        {
            String prop = prop_loop.next();
            Object pval = json.objectValue(val, prop);
            Path lpath_0 = new Path(prop, path);
            if (prop.compareTo("foo") == 0)
            {
                // handle may foo property
                // .'$over#Foo'.foo
                res = json.isString(pval) && json.asString(pval).compareTo("rewritten foo") == 0;
                if (! res)
                {
                    if (rep != null) rep.addEntry("unexpected _rewritten foo [.'$over#Foo'.foo]", (path != null ? lpath_0 : null));
                    if (rep != null) rep.addEntry("invalid optional prop value [.'$over#Foo'.foo]", (path != null ? lpath_0 : null));
                    return false;
                }
            }
            else
            {
                if (rep != null) rep.addEntry("unexpected prop [.'$over#Foo']", (path != null ? lpath_0 : null));
                return false;
            }
        }
        return true;
    }

    // check $over#Foo (.'$over#Foo')
    public boolean json_model_4(Object val, Path path, Report rep)
    {
        boolean res;
        // .'$over#Foo'
        res = _jm_obj_0(val, path, rep);
        if (! res)
        {
            if (rep != null) rep.addEntry("unexpected element [.'$over#Foo']", path);
        }
        return res;
    }


    public void init(JSON json)
    {
        if (!initialized)
        {
            try {
            over_01_map_pmap = new HashMap<String, Checker>();
            over_01_map_pmap.put("", new Checker() { public boolean call(Object o, Path p, Report r) { return _jm_obj_0(o, p, r);} });
            over_01_map_pmap.put("over", new Checker() { public boolean call(Object o, Path p, Report r) { return _jm_obj_0(o, p, r);} });
                super.init(json);
            }
            catch (Exception e) {
                throw new Error("cannot initialized model checker: " + e);
            }
        }
    }

    public void free()
    {
        if (initialized)
        {
            super.free();
            over_01_map_pmap = null;
        }
    }

    public Checker get(String name)
    {
        return over_01_map_pmap.get(name);
    }

    public Set<String> models()
    {
        return over_01_map_pmap.keySet();
    }

    static public void main(String[] args) throws Exception
    {
        ModelChecker checker = new over_01();
        Main.main("over_01", checker, VERSION, args);
    }
}
