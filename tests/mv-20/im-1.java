//
// Generated by JSON Model Compiler version 2
// see https://github.com/clairey-zx81/json-model
//

import json_model.*;
import java.util.Map;
import java.util.Set;
import java.util.HashMap;
import java.util.HashSet;
import java.util.Iterator;
import java.util.regex.Pattern;
import java.util.regex.Matcher;

@SuppressWarnings("unchecked")
public class im_1 extends ModelChecker
{
    static public final String VERSION = "2";

    public Map<String, Checker> im_1_map_pmap;

    // check $c (.'$c')
    public boolean json_model_2(Object val, Path path, Report rep)
    {
        boolean res;
        // .'$c'
        res = json.isString(val) && json.asString(val).compareTo("c") == 0;
        if (! res)
        {
            if (rep != null) rep.addEntry("unexpected _c [.'$c']", path);
        }
        return res;
    }

    // check $a (.'$a')
    public boolean json_model_4(Object val, Path path, Report rep)
    {
        boolean res;
        // .'$a'
        res = json.isString(val) && json.asString(val).compareTo("a") == 0;
        if (! res)
        {
            if (rep != null) rep.addEntry("unexpected _a [.'$a']", path);
        }
        return res;
    }

    // check $b (.'$b')
    public boolean json_model_5(Object val, Path path, Report rep)
    {
        boolean res;
        // .'$b'
        res = json.isString(val) && json.asString(val).compareTo("b") == 0;
        if (! res)
        {
            if (rep != null) rep.addEntry("unexpected _b [.'$b']", path);
        }
        return res;
    }

    // check $ (.)
    public boolean json_model_1(Object val, Path path, Report rep)
    {
        // .
        if (! json.isObject(val))
        {
            if (rep != null) rep.addEntry("not an object [.]", path);
            return false;
        }
        if (json.objectSize(val) == 0)
        {
            return true;
        }
        else
        {
            if (rep != null) rep.addEntry("expecting empty object [.]", path);
            return false;
        }
    }


    public void init(JSON json)
    {
        if (!initialized)
        {
            try {
            im_1_map_pmap = new HashMap<String, Checker>();
            im_1_map_pmap.put("", new Checker() { public boolean call(Object o, Path p, Report r) { return json_model_1(o, p, r);} });
            im_1_map_pmap.put("c", new Checker() { public boolean call(Object o, Path p, Report r) { return json_model_2(o, p, r);} });
            im_1_map_pmap.put("a", new Checker() { public boolean call(Object o, Path p, Report r) { return json_model_4(o, p, r);} });
            im_1_map_pmap.put("b", new Checker() { public boolean call(Object o, Path p, Report r) { return json_model_5(o, p, r);} });
                super.init(json);
            }
            catch (Exception e) {
                throw new Error("cannot initialized model checker: " + e);
            }
        }
    }

    public void free()
    {
        if (initialized)
        {
            super.free();
            im_1_map_pmap = null;
        }
    }

    public Checker get(String name)
    {
        return im_1_map_pmap.get(name);
    }

    public Set<String> models()
    {
        return im_1_map_pmap.keySet();
    }

    static public void main(String[] args) throws Exception
    {
        ModelChecker checker = new im_1();
        Main.main("im_1", checker, VERSION, args);
    }
}
