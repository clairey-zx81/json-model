//
// Generated by JSON Model Compiler version 2
// see https://github.com/clairey-zx81/json-model
//

import json_model.*;
import java.util.Map;
import java.util.Set;
import java.util.HashMap;
import java.util.HashSet;
import java.util.Iterator;
import java.util.regex.Pattern;
import java.util.regex.Matcher;

@SuppressWarnings("unchecked")
public class reo_00 extends ModelChecker
{
    static public final String VERSION = "2";

    public Map<String, Checker> reo_00_map_pmap;

    // check $ (.)
    public boolean json_model_1(Object val, Path path, Report rep)
    {
        // mixed sw eq ew regex optim
        // .
        boolean res = json.isString(val);
        if (res)
        {
            // .'|'.0
            // "/^hello/"
            res = json.asString(val).startsWith("hello");
            if (! res)
            {
                if (rep != null) rep.addEntry("unexpected value for model \"/^hello/\" [.'|'.0]", path);
                // .'|'.1
                // "/^world$/"
                res = json.asString(val).compareTo("world") == 0;
                if (! res)
                {
                    if (rep != null) rep.addEntry("unexpected value for model \"/^world$/\" [.'|'.1]", path);
                    // .'|'.2
                    // "/!$/"
                    res = json.asString(val).endsWith("!");
                    if (! res)
                    {
                        if (rep != null) rep.addEntry("unexpected value for model \"/!$/\" [.'|'.2]", path);
                    }
                }
            }
            if (res)
            {
                if (rep != null) rep.clearEntries();
            }
            else
            {
                if (rep != null) rep.addEntry("no model matched [.'|']", path);
            }
        }
        else
        {
            if (rep != null) rep.addEntry("unexpected type [.'|']", path);
        }
        return res;
    }


    public void init(JSON json)
    {
        if (!initialized)
        {
            try {
            reo_00_map_pmap = new HashMap<String, Checker>();
            reo_00_map_pmap.put("", new Checker() { public boolean call(Object o, Path p, Report r) { return json_model_1(o, p, r);} });
                super.init(json);
            }
            catch (Exception e) {
                throw new Error("cannot initialized model checker: " + e);
            }
        }
    }

    public void free()
    {
        if (initialized)
        {
            super.free();
            reo_00_map_pmap = null;
        }
    }

    public Checker get(String name)
    {
        return reo_00_map_pmap.get(name);
    }

    public Set<String> models()
    {
        return reo_00_map_pmap.keySet();
    }

    static public void main(String[] args) throws Exception
    {
        ModelChecker checker = new reo_00();
        Main.main("reo_00", checker, VERSION, args);
    }
}
