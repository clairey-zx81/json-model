//
// Generated by JSON Model Compiler version 2
// see https://github.com/clairey-zx81/json-model
//

import json_model.*;
import java.util.Map;
import java.util.Set;
import java.util.HashMap;
import java.util.HashSet;
import java.util.Iterator;
import java.util.regex.Pattern;
import java.util.regex.Matcher;

@SuppressWarnings("unchecked")
public class ma_prop_00 extends ModelChecker
{
    static public final String VERSION = "2";

    public Map<String, Checker> ma_prop_00_map_pmap;

    // check $ (.)
    public boolean json_model_1(Object val, Path path, Report rep)
    {
        // prop order check is preserved
        // .
        if (! json.isObject(val))
        {
            if (rep != null) rep.addEntry("not an object [.]", path);
            return false;
        }
        boolean res;
        Iterator<String> prop_loop = json.objectIterator(val);
        while (prop_loop.hasNext())
        {
            String prop = prop_loop.next();
            Object pval = json.objectValue(val, prop);
            Path lpath_0 = new Path(prop, path);
            if (prop.compareTo("pretty_long_property_name") == 0)
            {
                // handle may pretty_long_property_name property
                // .pretty_long_property_name
                res = json.isInteger(pval) && json.asLong(pval) >= 0;
                if (! res)
                {
                    if (rep != null) rep.addEntry("not a 0 strict int [.pretty_long_property_name]", (path != null ? lpath_0 : null));
                    if (rep != null) rep.addEntry("invalid optional prop value [.pretty_long_property_name]", (path != null ? lpath_0 : null));
                    return false;
                }
                continue;
            }
            else if (prop.compareTo("zulu") == 0)
            {
                // handle may zulu property
                // .zulu
                res = json.isInteger(pval) && json.asLong(pval) >= 0;
                if (! res)
                {
                    if (rep != null) rep.addEntry("not a 0 strict int [.zulu]", (path != null ? lpath_0 : null));
                    if (rep != null) rep.addEntry("invalid optional prop value [.zulu]", (path != null ? lpath_0 : null));
                    return false;
                }
                continue;
            }
            else if (prop.compareTo("XXX") == 0)
            {
                // handle may XXX property
                // .XXX
                res = json.isInteger(pval) && json.asLong(pval) >= 0;
                if (! res)
                {
                    if (rep != null) rep.addEntry("not a 0 strict int [.XXX]", (path != null ? lpath_0 : null));
                    if (rep != null) rep.addEntry("invalid optional prop value [.XXX]", (path != null ? lpath_0 : null));
                    return false;
                }
                continue;
            }
            else if (prop.compareTo("Susie") == 0)
            {
                // handle may Susie property
                // .Susie
                res = json.isInteger(pval) && json.asLong(pval) >= 0;
                if (! res)
                {
                    if (rep != null) rep.addEntry("not a 0 strict int [.Susie]", (path != null ? lpath_0 : null));
                    if (rep != null) rep.addEntry("invalid optional prop value [.Susie]", (path != null ? lpath_0 : null));
                    return false;
                }
                continue;
            }
            else if (prop.compareTo("last") == 0)
            {
                // handle may last property
                // .last
                res = json.isInteger(pval) && json.asLong(pval) >= 0;
                if (! res)
                {
                    if (rep != null) rep.addEntry("not a 0 strict int [.last]", (path != null ? lpath_0 : null));
                    if (rep != null) rep.addEntry("invalid optional prop value [.last]", (path != null ? lpath_0 : null));
                    return false;
                }
                continue;
            }
            if (rep != null) rep.addEntry("unexpected prop [.]", (path != null ? lpath_0 : null));
            return false;
        }
        return true;
    }


    public void init(JSON json)
    {
        if (!initialized)
        {
            try {
            ma_prop_00_map_pmap = new HashMap<String, Checker>();
            ma_prop_00_map_pmap.put("", new Checker() { public boolean call(Object o, Path p, Report r) { return json_model_1(o, p, r);} });
                super.init(json);
            }
            catch (Exception e) {
                throw new Error("cannot initialized model checker: " + e);
            }
        }
    }

    public void free()
    {
        if (initialized)
        {
            super.free();
            ma_prop_00_map_pmap = null;
        }
    }

    public Checker get(String name)
    {
        return ma_prop_00_map_pmap.get(name);
    }

    public Set<String> models()
    {
        return ma_prop_00_map_pmap.keySet();
    }

    static public void main(String[] args) throws Exception
    {
        ModelChecker checker = new ma_prop_00();
        Main.main("ma_prop_00", checker, VERSION, args);
    }
}
