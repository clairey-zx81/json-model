{
  "$comment": "JSON Schema generated by JSON Model Compiler",
  "$defs": {
    "Array": {
      "additionalProperties": false,
      "description": "WTF about 'content'?",
      "properties": {
        "baseType": {
          "type": "string"
        },
        "content": {
          "$ref": "#/$defs/type-or-ref"
        },
        "kind": {
          "const": "array"
        },
        "maxLength": {
          "minimum": 0,
          "type": "integer"
        },
        "minLength": {
          "minimum": 0,
          "type": "integer"
        },
        "name": {
          "type": "string"
        }
      },
      "required": [
        "kind"
      ],
      "type": "object"
    },
    "Atomic": {
      "additionalProperties": false,
      "description": "could there be derived atomics as well?",
      "properties": {
        "baseType": {
          "$ref": "#/$defs/atomic-types"
        },
        "enumeration": {
          "items": {
            "$ref": "#/$defs/atomic"
          },
          "type": "array"
        },
        "explicitTypeZone": {
          "enum": [
            "required",
            "prohibited",
            "optional"
          ]
        },
        "fractionDigits": {
          "minimum": 0,
          "type": "integer"
        },
        "kind": {
          "const": "atomic"
        },
        "length": {
          "minimum": 0,
          "type": "integer"
        },
        "maxExclusive": {
          "$ref": "#/$defs/atomic"
        },
        "maxInclusive": {
          "$ref": "#/$defs/atomic"
        },
        "maxLength": {
          "minimum": 0,
          "type": "integer"
        },
        "minExclusive": {
          "$ref": "#/$defs/atomic"
        },
        "minInclusive": {
          "$ref": "#/$defs/atomic"
        },
        "minLength": {
          "minimum": 0,
          "type": "integer"
        },
        "name": {
          "type": "string"
        },
        "pattern": {
          "type": "string"
        },
        "totalDigits": {
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "kind",
        "baseType"
      ],
      "type": "object"
    },
    "Fields": {
      "additionalProperties": false,
      "properties": {
        "default": true,
        "name": {
          "type": "string"
        },
        "required": {
          "type": "boolean"
        },
        "type": {
          "$ref": "#/$defs/type-or-ref"
        },
        "unique": {
          "type": "boolean"
        }
      },
      "required": [
        "name",
        "type"
      ],
      "type": "object"
    },
    "Object": {
      "additionalProperties": false,
      "properties": {
        "baseType": {
          "type": "string"
        },
        "closed": {
          "type": "boolean"
        },
        "content": {
          "items": {
            "$ref": "#/$defs/Fields"
          },
          "type": "array"
        },
        "kind": {
          "const": "object"
        },
        "name": {
          "type": "string"
        }
      },
      "required": [
        "kind"
      ],
      "type": "object"
    },
    "Schema": {
      "additionalProperties": false,
      "properties": {
        "metadata": {
          "additionalProperties": false,
          "properties": {
            "authors": {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "date": {
              "type": "string"
            },
            "name": {
              "type": "string"
            },
            "previous": {
              "type": "string"
            }
          },
          "type": "object"
        },
        "types": {
          "items": {
            "$ref": "#/$defs/Type"
          },
          "type": "array"
        }
      },
      "required": [
        "types"
      ],
      "type": "object"
    },
    "Type": {
      "anyOf": [
        {
          "$ref": "#/$defs/Atomic"
        },
        {
          "$ref": "#/$defs/Object"
        },
        {
          "$ref": "#/$defs/Array"
        },
        {
          "$ref": "#/$defs/Union"
        }
      ]
    },
    "Union": {
      "additionalProperties": false,
      "description": "jsound.jsound.json does not say that it is a type\u2026",
      "properties": {
        "baseType": {
          "type": "string"
        },
        "content": {
          "items": {
            "$ref": "#/$defs/type-or-ref"
          },
          "type": "array"
        },
        "kind": {
          "const": "union"
        },
        "name": {
          "type": "string"
        }
      },
      "required": [
        "name",
        "kind",
        "content"
      ],
      "type": "object"
    },
    "atomic": {
      "anyOf": [
        {
          "type": "null"
        },
        {
          "type": "boolean"
        },
        {
          "type": "integer"
        },
        {
          "type": "number"
        },
        {
          "type": "string"
        }
      ]
    },
    "atomic-types": {
      "enum": [
        "string",
        "decimal",
        "integer",
        "double",
        "boolean",
        "anyURI",
        "base64Binary",
        "hexBinary",
        "date",
        "dateTime",
        "time",
        "dateTimeStamp",
        "duration",
        "null"
      ]
    },
    "type-or-ref": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "$ref": "#/$defs/Type"
        }
      ]
    }
  },
  "$id": "./jsound.model.json",
  "$ref": "#/$defs/Schema"
}
