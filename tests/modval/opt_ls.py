# Generated for model: ./opt_ls.model.json
#
# Generated by jmc (JSON Model Compiler) (https://github.com/clairey-zx81/json-model)
#

from typing import Callable
import re as re
import datetime
import urllib.parse

type Jsonable = None|bool|int|float|str|list[Jsonable]|dict[str, Jsonable]
type CheckFun = Callable[[Jsonable, str], bool]
type PropMap = dict[str, CheckFun]
type TagMap = dict[None|bool|float|int|str, CheckFun]

json_model_0_must: PropMap

# define "json_model_0_must_i" ($.i)
def jm_f_0(value: Jsonable, path: str) -> bool:
    # $.i
    result = ((isinstance(value, int) and not isinstance(value, bool)) or (isinstance(value, float) and int(value) == value))
    return result

# define "json_model_0_must_f" ($.f)
def jm_f_1(value: Jsonable, path: str) -> bool:
    # $.f
    result = isinstance(value, float)
    return result



# object $
def json_model_0(value: Jsonable, path: str) -> bool:
    if not isinstance(value, dict):
        return False
    must_count = 0
    for prop, model in value.items():
        assert isinstance(prop, str)
        if prop in json_model_0_must:  # must
            must_count += 1
            if not json_model_0_must[prop](model, f"{path}.{prop}"):
                return False
        else:  # no catch all
            return False
    return must_count == 2



# entry function check_model
def check_model(value: Jsonable, path: str = "$") -> bool:
    return json_model_0(value, path)


# object properties maps
json_model_0_must = {
    "i": jm_f_0,
    "f": jm_f_1,
}


