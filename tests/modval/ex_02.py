#! /bin/env python
#
# Model: ./ex_02.model.json
#
# Generated by jmc (JSON Model Compiler) (https://github.com/clairey-zx81/json-model)
#

from typing import Callable
import re as re
import datetime
import urllib.parse

type Jsonable = None|bool|int|float|str|list[Jsonable]|dict[str, Jsonable]
type Path = list[str]
type Report = list[str]|None
type CheckFun = Callable[[Jsonable, str, Report], bool]
type PropMap = dict[str, CheckFun]
type TagMap = dict[None|bool|float|int|str, CheckFun]

# extract type name
def _tname(value: Jsonable) -> str:
    return type(value).__name__

# maybe add message to report
def _rep(msg: str, rep: Report) -> bool:
    rep is None or rep.append(msg)
    return False


# define "$x1" ($.'$x1')
def json_model_5(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'$x1'
    result = isinstance(value, str) and value == "Susie"
    if not result:
        rep is None or rep.append(f"not an expected _Susie at {path} [$.'$x1']")
    return result

# define "$x2" ($.'$x2')
def json_model_6(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'$x2'
    result = isinstance(value, str) and value == "Susie"
    if not result:
        rep is None or rep.append(f"not an expected _Susie at {path} [$.'$x2']")
    return result

# define "$x3" ($.'$x3')
def json_model_7(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'$x3'
    result = isinstance(value, str) and value == "Susie"
    if not result:
        rep is None or rep.append(f"not an expected _Susie at {path} [$.'$x3']")
    return result

# define "$__external_0" ($.'$__external_0')
def json_model_8(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'$__external_0'
    result = isinstance(value, str) and value == "Susie"
    if not result:
        rep is None or rep.append(f"not an expected _Susie at {path} [$.'$__external_0']")
    return result

# define "$" ($)
def json_model_1(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $
    result = isinstance(value, list) and len(value) == 4
    if result:
        # $.0
        result = isinstance(value[0], str) and value[0] == "Susie"
        if not result:
            rep is None or rep.append(f"not an expected _Susie at {path + '.0'} [$.0]")
        if result:
            # $.1
            result = isinstance(value[1], str) and value[1] == "Susie"
            if not result:
                rep is None or rep.append(f"not an expected _Susie at {path + '.1'} [$.1]")
            if result:
                # $.2
                result = isinstance(value[2], str) and value[2] == "Susie"
                if not result:
                    rep is None or rep.append(f"not an expected _Susie at {path + '.2'} [$.2]")
                if result:
                    # $.3
                    result = isinstance(value[3], str) and value[3] == "Susie"
                    if not result:
                        rep is None or rep.append(f"not an expected _Susie at {path + '.3'} [$.3]")
    else:
        rep is None or rep.append(f"not array or bad array length at {path} [$]")
    if not result:
        rep is None or rep.append(f"not array or unexpected array at {path} [$]")
    return result

# entry function check_model
def check_model(value: Jsonable, path: str = "$", rep: Report = None) -> bool:
    return json_model_1(value, path, rep)


# possibly run as a script: $0 values...
if __name__ == "__main__":
    import json
    import sys
    for fn in sys.argv[1:]:
        try:
            with open(fn) as f:
                value = json.load(f)
            reasons = []
            if check_model(value, "", reasons):
                print(f"{fn}: PASS")
            else:
                print(f"{fn}: FAIL {reasons}")
        except Exception as e:
            print(f"{fn}: ERROR ({e})")

