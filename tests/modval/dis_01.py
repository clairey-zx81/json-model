#! /bin/env python
#
# Model: ./dis_01.model.json
#
# Generated by jmc (JSON Model Compiler) (https://github.com/clairey-zx81/json-model)
#

from typing import Callable
import re as re
import datetime
import urllib.parse

type Jsonable = None|bool|int|float|str|list[Jsonable]|dict[str, Jsonable]
type Path = list[str]
type Report = list[str]|None
type CheckFun = Callable[[Jsonable, str, Report], bool]
type PropMap = dict[str, CheckFun]
type TagMap = dict[None|bool|float|int|str, CheckFun]

# extract type name
def _tname(value: Jsonable) -> str:
    return type(value).__name__

# maybe add message to report
def _rep(msg: str, rep: Report) -> bool:
    rep is None or rep.append(msg)
    return False

_jm_obj_0_must: PropMap
_jm_obj_1_must: PropMap
_jm_obj_2_must: PropMap
_jm_map_0: TagMap

# define "_jm_obj_0_must_tag" ($.'|'.0.tag)
def _jm_f_0(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'|'.0.tag
    result = isinstance(value, int) and not isinstance(value, bool) and value == 18
    if not result:
        rep is None or rep.append(f"not an expected =18 at {path} [$.'|'.0.tag]")
    return result

# define "_jm_obj_0_must_a" ($.'|'.0.a)
def _jm_f_1(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'|'.0.a
    result = isinstance(value, str)
    if not result:
        rep is None or rep.append(f"not an expected string at {path} [$.'|'.0.a]")
    return result

# define "_jm_obj_1_must_tag" ($.'|'.1.tag)
def _jm_f_2(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'|'.1.tag
    result = isinstance(value, int) and not isinstance(value, bool) and value == 42
    if not result:
        rep is None or rep.append(f"not an expected =42 at {path} [$.'|'.1.tag]")
    return result

# define "_jm_obj_1_must_b" ($.'|'.1.b)
def _jm_f_3(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'|'.1.b
    result = isinstance(value, str)
    if not result:
        rep is None or rep.append(f"not an expected string at {path} [$.'|'.1.b]")
    return result

# define "_jm_obj_2_must_tag" ($.'|'.2.tag)
def _jm_f_4(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'|'.2.tag
    result = isinstance(value, int) and not isinstance(value, bool) and value == 1024
    if not result:
        rep is None or rep.append(f"not an expected =1024 at {path} [$.'|'.2.tag]")
    return result

# define "_jm_obj_2_must_c" ($.'|'.2.c)
def _jm_f_5(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'|'.2.c
    result = isinstance(value, str)
    if not result:
        rep is None or rep.append(f"not an expected string at {path} [$.'|'.2.c]")
    return result



# object $.'|'.0
def _jm_obj_0(value: Jsonable, path: str, rep: Report = None) -> bool:
    if not isinstance(value, dict):
        rep is None or rep.append(f"not an object at {path} [$.'|'.0]")
        return False
    must_count = 0
    for prop, val in value.items():
        assert isinstance(prop, str)
        lpath = path + "." + prop
        if prop in _jm_obj_0_must:  # must
            must_count += 1
            if not _jm_obj_0_must[prop](val, lpath, rep):
                rep is None or rep.append(f"invalid must prop value at {lpath} [$.'|'.0.{prop}]")
                return False
        else:  # no catch all
            rep is None or rep.append(f"no other prop expected at {path} [$.'|'.0]")
            return False
    result = must_count == 2
    if not result:
        rep is None or rep.append(f"missing must prop at {path} [$.'|'.0]")
    return result


# object $.'|'.1
def _jm_obj_1(value: Jsonable, path: str, rep: Report = None) -> bool:
    if not isinstance(value, dict):
        rep is None or rep.append(f"not an object at {path} [$.'|'.1]")
        return False
    must_count = 0
    for prop, val in value.items():
        assert isinstance(prop, str)
        lpath = path + "." + prop
        if prop in _jm_obj_1_must:  # must
            must_count += 1
            if not _jm_obj_1_must[prop](val, lpath, rep):
                rep is None or rep.append(f"invalid must prop value at {lpath} [$.'|'.1.{prop}]")
                return False
        else:  # no catch all
            rep is None or rep.append(f"no other prop expected at {path} [$.'|'.1]")
            return False
    result = must_count == 2
    if not result:
        rep is None or rep.append(f"missing must prop at {path} [$.'|'.1]")
    return result


# object $.'|'.2
def _jm_obj_2(value: Jsonable, path: str, rep: Report = None) -> bool:
    if not isinstance(value, dict):
        rep is None or rep.append(f"not an object at {path} [$.'|'.2]")
        return False
    must_count = 0
    for prop, val in value.items():
        assert isinstance(prop, str)
        lpath = path + "." + prop
        if prop in _jm_obj_2_must:  # must
            must_count += 1
            if not _jm_obj_2_must[prop](val, lpath, rep):
                rep is None or rep.append(f"invalid must prop value at {lpath} [$.'|'.2.{prop}]")
                return False
        else:  # no catch all
            rep is None or rep.append(f"no other prop expected at {path} [$.'|'.2]")
            return False
    result = must_count == 2
    if not result:
        rep is None or rep.append(f"missing must prop at {path} [$.'|'.2]")
    return result

# define "$" ($)
def json_model_1(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $
    result = isinstance(value, dict)
    if result:
        result = "tag" in value
        if result:
            tag_0 = value["tag"]
            if tag_0 in _jm_map_0:
                result = _jm_map_0[tag_0](value, path)
            else:
                rep is None or rep.append(f"tag tag value not found at {path} [$.'|'.'|']")
                result = False
        else:
            rep is None or rep.append(f"missing tag prop tag at {path} [$.'|'.'|']")
    else:  # not a dict
        rep is None or rep.append(f"not an object at {path} [$.'|'.'|']")
    return result

# entry function check_model
def check_model(value: Jsonable, path: str = "$", rep: Report = None) -> bool:
    return json_model_1(value, path, rep)


# object properties maps
_jm_obj_0_must = {
    "tag": _jm_f_0,
    "a": _jm_f_1,
}
_jm_obj_1_must = {
    "tag": _jm_f_2,
    "b": _jm_f_3,
}
_jm_obj_2_must = {
    "tag": _jm_f_4,
    "c": _jm_f_5,
}
_jm_map_0 = {
    18: _jm_obj_0,
    42: _jm_obj_1,
    1024: _jm_obj_2,
}


# possibly run as a script: $0 values...
if __name__ == "__main__":
    import json
    import sys
    for fn in sys.argv[1:]:
        try:
            with open(fn) as f:
                value = json.load(f)
            reasons = []
            if check_model(value, "", reasons):
                print(f"{fn}: PASS")
            else:
                print(f"{fn}: FAIL {reasons}")
        except Exception as e:
            print(f"{fn}: ERROR ({e})")

