{
  "description": "xor to or simplification, float vs string",
  "anyOf": [
    {
      "type": "number",
      "minimum": 0.0
    },
    {
      "type": "string",
      "pattern": "(?i)^[a-z]+$"
    }
  ]
}
