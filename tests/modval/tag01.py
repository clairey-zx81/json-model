# Generated for model: ./tag01.model.json
#
# Generated by jmc (JSON Model Compiler) (https://github.com/clairey-zx81/json-model)
#

from typing import Callable
import re as re
import datetime
import urllib.parse

type Jsonable = None|bool|int|float|str|list[Jsonable]|dict[str, Jsonable]
type Path = list[str]
type Report = list[str]|None
type CheckFun = Callable[[Jsonable, str, Report], bool]
type PropMap = dict[str, CheckFun]
type TagMap = dict[None|bool|float|int|str, CheckFun]

# extract type name
def _tname(value: Jsonable) -> str:
    return type(value).__name__

# maybe add message to report
def _rep(msg: str, rep: Report) -> bool:
    rep is None or rep.append(msg)
    return False

json_model_1_must: PropMap
json_model_2_must: PropMap
jm_map_0: TagMap

# define "json_model_1_must_t" ($.'$A'.t)
def jm_f_0(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'$A'.t
    result = isinstance(value, bool) and value == True
    if not result:
        rep is None or rep.append(f"not an expected =true at {path} [$.'$A'.t]")
    return result

# define "json_model_1_must_a" ($.'$A'.a)
def jm_f_1(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'$A'.a
    result = isinstance(value, int) and not isinstance(value, bool) and value >= 0
    if not result:
        rep is None or rep.append(f"not a 0 int at {path} [$.'$A'.a]")
    return result

# define "json_model_2_must_t" ($.'$B'.t)
def jm_f_2(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'$B'.t
    result = isinstance(value, bool) and value == False
    if not result:
        rep is None or rep.append(f"not an expected =false at {path} [$.'$B'.t]")
    return result

# define "json_model_2_must_b" ($.'$B'.b)
def jm_f_3(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'$B'.b
    result = isinstance(value, int) and not isinstance(value, bool) and value >= 0
    if not result:
        rep is None or rep.append(f"not a 0 int at {path} [$.'$B'.b]")
    return result



# object $.'$A'
def json_model_1(value: Jsonable, path: str, rep: Report = None) -> bool:
    if not isinstance(value, dict):
        rep is None or rep.append(f"not an object at {path} [$.'$A']")
        return False
    must_count = 0
    for prop, val in value.items():
        assert isinstance(prop, str)
        lpath = path + "." + prop
        if prop in json_model_1_must:  # must
            must_count += 1
            if not json_model_1_must[prop](val, lpath, rep):
                rep is None or rep.append(f"invalid must prop value at {lpath} [$.'$A'.{prop}]")
                return False
        else:  # no catch all
            rep is None or rep.append(f"no other prop expected at {path} [$.'$A']")
            return False
    result = must_count == 2
    if not result:
        rep is None or rep.append(f"missing must prop at {path} [$.'$A']")
    return result




# object $.'$B'
def json_model_2(value: Jsonable, path: str, rep: Report = None) -> bool:
    if not isinstance(value, dict):
        rep is None or rep.append(f"not an object at {path} [$.'$B']")
        return False
    must_count = 0
    for prop, val in value.items():
        assert isinstance(prop, str)
        lpath = path + "." + prop
        if prop in json_model_2_must:  # must
            must_count += 1
            if not json_model_2_must[prop](val, lpath, rep):
                rep is None or rep.append(f"invalid must prop value at {lpath} [$.'$B'.{prop}]")
                return False
        else:  # no catch all
            rep is None or rep.append(f"no other prop expected at {path} [$.'$B']")
            return False
    result = must_count == 2
    if not result:
        rep is None or rep.append(f"missing must prop at {path} [$.'$B']")
    return result



# define "$" ($)
def json_model_0(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $
    result = isinstance(value, dict)
    if result:
        result = "t" in value
        if result:
            tag_0 = value["t"]
            if tag_0 in jm_map_0:
                result = jm_map_0[tag_0](value, path)
            else:
                rep is None or rep.append(f"tag t value not found at {path} [$.'|'.'|']")
                result = False
        else:
            rep is None or rep.append(f"missing tag prop t at {path} [$.'|'.'|']")
    else:  # not a dict
        rep is None or rep.append(f"not an object at {path} [$.'|'.'|']")
    return result

# entry function check_model
def check_model(value: Jsonable, path: str = "$", rep: Report = None) -> bool:
    return json_model_0(value, path, rep)


# object properties maps
json_model_1_must = {
    "t": jm_f_0,
    "a": jm_f_1,
}
json_model_2_must = {
    "t": jm_f_2,
    "b": jm_f_3,
}
jm_map_0 = {
    True: json_model_1,
    False: json_model_2,
}


