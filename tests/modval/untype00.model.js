// FIXME generated python is wrong, should it be rather be rejected?
// use type-sensitive get_value function in generated code?
{
  "#": "constraint is either on value or on length",
  "@": { "|": [ 1, "$STRING" ] },
  ">=": 10
}
