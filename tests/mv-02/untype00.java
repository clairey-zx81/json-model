//
// Generated by JSON Model Compiler version 2
// see https://github.com/clairey-zx81/json-model
//

import json_model.*;
import java.util.Map;
import java.util.Set;
import java.util.HashMap;
import java.util.HashSet;
import java.util.Iterator;
import java.util.regex.Pattern;
import java.util.regex.Matcher;

@SuppressWarnings("unchecked")
public class untype00 extends ModelChecker
{
    static public final String VERSION = "2";

    public Map<String, Checker> untype00_map_pmap;

    // check $ (.)
    public boolean json_model_1(Object val, Path path, Report rep)
    {
        // constraint is either on value or on length
        // .
        // .'@'
        // .'@'.'|'.0
        boolean res = json.isInteger(val) && json.asLong(val) >= 1;
        if (! res)
        {
            if (rep != null) rep.addEntry("not a 1 strict int [.'@'.'|'.0]", path);
            // .'@'.'|'.1
            res = json.isString(val);
            if (! res)
            {
                if (rep != null) rep.addEntry("unexpected string [.'@'.'|'.1]", path);
            }
        }
        if (res)
        {
            if (rep != null) rep.clearEntries();
            res = rt.check_constraint(val, json_model.Runtime.Operator.GE, json.safeJSON("10"), path, rep);
            if (! res)
            {
                if (rep != null) rep.addEntry("constraints failed [.]", path);
            }
        }
        else
        {
            if (rep != null) rep.addEntry("no model matched [.'@'.'|']", path);
        }
        return res;
    }


    public void init(JSON json)
    {
        if (!initialized)
        {
            try {
            untype00_map_pmap = new HashMap<String, Checker>();
            untype00_map_pmap.put("", new Checker() { public boolean call(Object o, Path p, Report r) { return json_model_1(o, p, r);} });
                super.init(json);
            }
            catch (Exception e) {
                throw new Error("cannot initialized model checker: " + e);
            }
        }
    }

    public void free()
    {
        if (initialized)
        {
            super.free();
            untype00_map_pmap = null;
        }
    }

    public Checker get(String name)
    {
        return untype00_map_pmap.get(name);
    }

    public Set<String> models()
    {
        return untype00_map_pmap.keySet();
    }

    static public void main(String[] args) throws Exception
    {
        ModelChecker checker = new untype00();
        Main.main("untype00", checker, VERSION, args);
    }
}
