{
  "description": "constraint is either on value or on length",
  "anyOf": [
    {
      "type": "integer",
      "minimum": 1
    },
    {
      "type": "string"
    }
  ],
  "minimum": 10,
  "minLength": 10,
  "minProperties": 10,
  "minItems": 10
}
