# Tutorial File and Commands

This directory contains files and commands (`tuto.sh`) used by the JSON Model Tutorial.

Run directly

```sh
./tuto_shell.sh
```

Or with docker:

```sh
./tuto_docker.sh
```
