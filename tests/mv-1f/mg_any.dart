//
// Generated by JSON Model Compiler version 2
// see https://github.com/clairey-zx81/json-model
//
import 'dart:convert';
import 'dart:io';
import 'dart:core';
// Import the helper library generated nearby
import 'package:json_model_runtime/json_model.dart' as runtime;
import 'package:json_model_runtime/main.dart';

//
// Generated JSON Model Validator
//
const String JSON_MODEL_VERSION = "2";

final _jm_re_0_re = RegExp("foo", caseSensitive: true, multiLine: false);
var check_model_map = <String, Function>{};

bool _jm_re_0(String s, List<dynamic>? path, List<dynamic>? rep) => _jm_re_0_re.hasMatch(s);

// check $ (.)
bool json_model_1(dynamic val, List<dynamic>? path, List<dynamic>? rep)
{
    // .
    if (! (val is Map))
    {
        rep?.add(["not an object [.]", path]);
        return false;
    }
    bool res;
    int must_count = 0;
    for (var entry in val.entries)
    {
        var prop = entry.key;
        var pval = entry.value;
        List<dynamic>? lpath_0 = (path != null ? [...?path, prop] : null);
        if (prop.compareTo("a") == 0)
        {
            // handle must a property
            must_count += 1;
            // .a
            res = (pval is int) && pval >= 1;
            if (! res)
            {
                rep?.add(["not a 1 strict int [.a]", (path != null ? lpath_0 : null)]);
                rep?.add(["invalid mandatory prop value [.a]", (path != null ? lpath_0 : null)]);
                return false;
            }
            continue;
        }
        else if (prop.compareTo("b") == 0)
        {
            // handle must b property
            must_count += 1;
            // .b
            res = (pval is int) && pval >= 1;
            if (! res)
            {
                rep?.add(["not a 1 strict int [.b]", (path != null ? lpath_0 : null)]);
                rep?.add(["invalid mandatory prop value [.b]", (path != null ? lpath_0 : null)]);
                return false;
            }
            continue;
        }
        if (prop.compareTo("c") == 0)
        {
            // handle may c property
            // .c
            res = (pval is int) && pval >= 1;
            if (! res)
            {
                rep?.add(["not a 1 strict int [.c]", (path != null ? lpath_0 : null)]);
                rep?.add(["invalid optional prop value [.c]", (path != null ? lpath_0 : null)]);
                return false;
            }
            continue;
        }
        if (runtime.jm_is_valid_date(prop, (path != null ? lpath_0 : null), rep))
        {
            // handle 1 key props
            // .'$DATE'
            res = (pval is int) && pval >= 1;
            if (! res)
            {
                rep?.add(["not a 1 strict int [.'\$DATE']", (path != null ? lpath_0 : null)]);
                return false;
            }
        }
        else if (_jm_re_0(prop, path, rep))
        {
            // handle 1 re props
            // .'/foo/'
            res = (pval is int) && pval >= 1;
            if (! res)
            {
                rep?.add(["not a 1 strict int [.'/foo/']", (path != null ? lpath_0 : null)]);
                return false;
            }
        }
        else
        {
            rep?.add(["unexpected prop [.]", (path != null ? lpath_0 : null)]);
            return false;
        }
    }
    if (must_count != 2)
    {
        if (rep != null)
        {
            if (! val.containsKey("a"))
            {
                rep?.add(["missing mandatory prop <a> [.]", path]);
            }
            if (! val.containsKey("b"))
            {
                rep?.add(["missing mandatory prop <b> [.]", path]);
            }
        }
        return false;
    }
    return true;
}


// dart_init.dart
class GlobalState {
  static bool initializer = false;
}

Function? check_model_fun(String name) {
  return check_model_map[name];
}


void check_model_init() {
  if (!GlobalState.initializer){
    GlobalState.initializer = true;
  }
        check_model_map[""] = json_model_1;
}

// dart_free.dart
void check_model_free() {
  GlobalState.initializer = true;
}


void main(List<String> args) {
  runValidation(
    args,
    check_model_init,
    check_model_fun,
    check_model_free,
  );
}
