//
// Generated by JSON Model Compiler version 2
// see https://github.com/clairey-zx81/json-model
//

// regular expression engine
#include <stddef.h>
#include <cre2.h>

#include <json-model.h>
#define JSON_MODEL_VERSION "2"

static bool json_model_2(const json_t *val, jm_path_t *path, jm_report_t *rep);
static bool _jm_obj_0(const json_t *val, jm_path_t *path, jm_report_t *rep);
static bool json_model_3(const json_t *val, jm_path_t *path, jm_report_t *rep);
static bool _jm_obj_1(const json_t *val, jm_path_t *path, jm_report_t *rep);
static bool json_model_4(const json_t *val, jm_path_t *path, jm_report_t *rep);
static bool _jm_obj_2(const json_t *val, jm_path_t *path, jm_report_t *rep);
static bool json_model_1(const json_t *val, jm_path_t *path, jm_report_t *rep);
jm_propmap_t check_model_map_tab[4];
const size_t check_model_map_size = 4;

// check $t (.'$t')
static bool json_model_2(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    bool res;
    // .'$t'
    res = json_is_string(val) && strcmp(json_string_value(val), "cst_01") == 0;
    if (! res)
    {
        if (rep) jm_report_add_entry(rep, "unexpected _cst_01 [.'$t']", path);
    }
    return res;
}

// object .'$x'
static INLINE bool _jm_obj_0(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    // check close must only props
    if (! json_is_object(val))
    {
        if (rep) jm_report_add_entry(rep, "not an object [.'$x']", path);
        return false;
    }
    if (json_object_size(val) != 2)
    {
        if (rep) jm_report_add_entry(rep, "bad property count [.'$x']", path);
        return false;
    }
    json_t * pval;
    bool res;
    if (! (json_object_get(val, "a") != NULL))
    {
        if (rep) jm_report_add_entry(rep, "missing mandatory prop <a> [.'$x']", path);
        return false;
    }
    pval = json_object_get(val, "a");
    // .'$x'.a
    res = json_is_string(pval) && strcmp(json_string_value(pval), "cst_01") == 0;
    if (! res)
    {
        if (rep) jm_report_add_entry(rep, "unexpected _cst_01 [.'$x'.a]", path);
        if (rep) jm_report_add_entry(rep, "unexpected value for mandatory prop <a> [.'$x']", path);
        return false;
    }
    if (! (json_object_get(val, "b") != NULL))
    {
        if (rep) jm_report_add_entry(rep, "missing mandatory prop <b> [.'$x']", path);
        return false;
    }
    pval = json_object_get(val, "b");
    // .'$x'.b
    res = json_is_string(pval) && strcmp(json_string_value(pval), "cst_01") == 0;
    if (! res)
    {
        if (rep) jm_report_add_entry(rep, "unexpected _cst_01 [.'$x'.b]", path);
        if (rep) jm_report_add_entry(rep, "unexpected value for mandatory prop <b> [.'$x']", path);
        return false;
    }
    return true;
}

// check $x (.'$x')
static bool json_model_3(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    bool res;
    // .'$x'
    res = _jm_obj_0(val, path, rep);
    if (! res)
    {
        if (rep) jm_report_add_entry(rep, "unexpected element [.'$x']", path);
    }
    return res;
}

// object .'$y'
static INLINE bool _jm_obj_1(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    // check close must only props
    if (! json_is_object(val))
    {
        if (rep) jm_report_add_entry(rep, "not an object [.'$y']", path);
        return false;
    }
    if (json_object_size(val) != 2)
    {
        if (rep) jm_report_add_entry(rep, "bad property count [.'$y']", path);
        return false;
    }
    json_t * pval;
    bool res;
    if (! (json_object_get(val, "c") != NULL))
    {
        if (rep) jm_report_add_entry(rep, "missing mandatory prop <c> [.'$y']", path);
        return false;
    }
    pval = json_object_get(val, "c");
    // .'$y'.c
    res = json_is_string(pval) && strcmp(json_string_value(pval), "cst_01") == 0;
    if (! res)
    {
        if (rep) jm_report_add_entry(rep, "unexpected _cst_01 [.'$y'.c]", path);
        if (rep) jm_report_add_entry(rep, "unexpected value for mandatory prop <c> [.'$y']", path);
        return false;
    }
    if (! (json_object_get(val, "d") != NULL))
    {
        if (rep) jm_report_add_entry(rep, "missing mandatory prop <d> [.'$y']", path);
        return false;
    }
    pval = json_object_get(val, "d");
    // .'$y'.d
    res = json_is_string(pval) && strcmp(json_string_value(pval), "cst_01") == 0;
    if (! res)
    {
        if (rep) jm_report_add_entry(rep, "unexpected _cst_01 [.'$y'.d]", path);
        if (rep) jm_report_add_entry(rep, "unexpected value for mandatory prop <d> [.'$y']", path);
        return false;
    }
    return true;
}

// check $y (.'$y')
static bool json_model_4(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    bool res;
    // .'$y'
    res = _jm_obj_1(val, path, rep);
    if (! res)
    {
        if (rep) jm_report_add_entry(rep, "unexpected element [.'$y']", path);
    }
    return res;
}

// object .
static INLINE bool _jm_obj_2(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    // check close must only props
    if (! json_is_object(val))
    {
        if (rep) jm_report_add_entry(rep, "not an object [.]", path);
        return false;
    }
    if (json_object_size(val) != 4)
    {
        if (rep) jm_report_add_entry(rep, "bad property count [.]", path);
        return false;
    }
    json_t * pval;
    bool res;
    if (! (json_object_get(val, "a") != NULL))
    {
        if (rep) jm_report_add_entry(rep, "missing mandatory prop <a> [.]", path);
        return false;
    }
    pval = json_object_get(val, "a");
    // .a
    res = json_is_string(pval) && strcmp(json_string_value(pval), "cst_01") == 0;
    if (! res)
    {
        if (rep) jm_report_add_entry(rep, "unexpected _cst_01 [.a]", path);
        if (rep) jm_report_add_entry(rep, "unexpected value for mandatory prop <a> [.]", path);
        return false;
    }
    if (! (json_object_get(val, "b") != NULL))
    {
        if (rep) jm_report_add_entry(rep, "missing mandatory prop <b> [.]", path);
        return false;
    }
    pval = json_object_get(val, "b");
    // .b
    res = json_is_string(pval) && strcmp(json_string_value(pval), "cst_01") == 0;
    if (! res)
    {
        if (rep) jm_report_add_entry(rep, "unexpected _cst_01 [.b]", path);
        if (rep) jm_report_add_entry(rep, "unexpected value for mandatory prop <b> [.]", path);
        return false;
    }
    if (! (json_object_get(val, "c") != NULL))
    {
        if (rep) jm_report_add_entry(rep, "missing mandatory prop <c> [.]", path);
        return false;
    }
    pval = json_object_get(val, "c");
    // .c
    res = json_is_string(pval) && strcmp(json_string_value(pval), "cst_01") == 0;
    if (! res)
    {
        if (rep) jm_report_add_entry(rep, "unexpected _cst_01 [.c]", path);
        if (rep) jm_report_add_entry(rep, "unexpected value for mandatory prop <c> [.]", path);
        return false;
    }
    if (! (json_object_get(val, "d") != NULL))
    {
        if (rep) jm_report_add_entry(rep, "missing mandatory prop <d> [.]", path);
        return false;
    }
    pval = json_object_get(val, "d");
    // .d
    res = json_is_string(pval) && strcmp(json_string_value(pval), "cst_01") == 0;
    if (! res)
    {
        if (rep) jm_report_add_entry(rep, "unexpected _cst_01 [.d]", path);
        if (rep) jm_report_add_entry(rep, "unexpected value for mandatory prop <d> [.]", path);
        return false;
    }
    return true;
}

// check $ (.)
static bool json_model_1(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    bool res;
    // .
    res = _jm_obj_2(val, path, rep);
    if (! res)
    {
        if (rep) jm_report_add_entry(rep, "unexpected element [.]", path);
    }
    return res;
}

jm_check_fun_t check_model_map(const char *pname)
{
    return jm_search_propmap(pname, check_model_map_tab, 4);
}

static bool initialized = false;

const char *check_model_init(void)
{
    if (!initialized)
    {
        initialized = true;
        jm_version_string = JSON_MODEL_VERSION;
        check_model_map_tab[0] = (jm_propmap_t) { "", _jm_obj_2 };
        check_model_map_tab[1] = (jm_propmap_t) { "t", json_model_2 };
        check_model_map_tab[2] = (jm_propmap_t) { "x", _jm_obj_0 };
        check_model_map_tab[3] = (jm_propmap_t) { "y", _jm_obj_1 };
        jm_sort_propmap(check_model_map_tab, 4);
    }
    return NULL;
}

void check_model_free(void)
{
    if (initialized)
    {
        initialized = false;

        // cleanup code
    }
}

/*
 * # API
 *
 * ```c
 * bool error;
 * char *message;
 *
 * bool valid = check(json_value, "model-name", &error, &message);
 *
 * if (message)
 *    free(message), message = NULL;
 * ```
 *
 * - `error` is set of there was an error during initialization or if the model does not exists.
 * - `message` is provided if a non NULL pointer is passed, and must be freed by the caller.
 */
bool
check_model(const json_t *val, const char *name, bool *error, char **reasons)
{
    return jm_generic_entry(check_model_init, check_model_map, val, name, error, reasons);
}
