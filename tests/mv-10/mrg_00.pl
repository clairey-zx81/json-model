#! /usr/bin/perl
#
# Generated by JSON Model Compiler version 2
# see https://github.com/clairey-zx81/json-model
#
use utf8;
use strict;
use warnings;
no warnings "uninitialized";
use re::engine::RE2;
use JSON::JsonModel;
use constant JMC_VERSION => "2";


sub json_model_1($$$);
my %check_model_map;

# check $ (.)
sub json_model_1($$$)
{
    my ($val, $path, $rep) = @_;
    # .
    # check close must only props
    unless (jm_is_object($val))
    {
        push @$rep, ["not an object [.]", $path] if defined $rep;
        return 0;
    }
    if (jm_obj_size($val) != 4)
    {
        push @$rep, ["bad property count [.]", $path] if defined $rep;
        return 0;
    }
    my $lpath;
    my $pval;
    unless (exists $$val{"a"})
    {
        push @$rep, ["missing mandatory prop <a> [.]", $path] if defined $rep;
        return 0;
    }
    $lpath = defined $path ? [@{$path}, "a"] : undef;
    $pval = $$val{"a"};
    # .a
    my $res = jm_is_integer($pval) && $pval >= 0;
    unless ($res)
    {
        push @$rep, ["not a 0 strict int [.a]", defined $path ? $lpath : undef] if defined $rep;
        push @$rep, ["unexpected value for mandatory prop <a> [.]", defined $path ? $lpath : undef] if defined $rep;
        return 0;
    }
    unless (exists $$val{"b"})
    {
        push @$rep, ["missing mandatory prop <b> [.]", $path] if defined $rep;
        return 0;
    }
    $lpath = defined $path ? [@{$path}, "b"] : undef;
    $pval = $$val{"b"};
    # .b
    $res = jm_is_integer($pval) && $pval >= 1;
    unless ($res)
    {
        push @$rep, ["not a 1 strict int [.b]", defined $path ? $lpath : undef] if defined $rep;
        push @$rep, ["unexpected value for mandatory prop <b> [.]", defined $path ? $lpath : undef] if defined $rep;
        return 0;
    }
    unless (exists $$val{"c"})
    {
        push @$rep, ["missing mandatory prop <c> [.]", $path] if defined $rep;
        return 0;
    }
    $lpath = defined $path ? [@{$path}, "c"] : undef;
    $pval = $$val{"c"};
    # .c
    $res = jm_is_boolean($pval);
    unless ($res)
    {
        push @$rep, ["not a bool [.c]", defined $path ? $lpath : undef] if defined $rep;
        push @$rep, ["unexpected value for mandatory prop <c> [.]", defined $path ? $lpath : undef] if defined $rep;
        return 0;
    }
    unless (exists $$val{"d"})
    {
        push @$rep, ["missing mandatory prop <d> [.]", $path] if defined $rep;
        return 0;
    }
    $lpath = defined $path ? [@{$path}, "d"] : undef;
    $pval = $$val{"d"};
    # .d
    $res = jm_is_integer($pval);
    unless ($res)
    {
        push @$rep, ["not a -1 strict int [.d]", defined $path ? $lpath : undef] if defined $rep;
        push @$rep, ["unexpected value for mandatory prop <d> [.]", defined $path ? $lpath : undef] if defined $rep;
        return 0;
    }
    return 1;
}


# initialization of global variables

our $initialized = 0;

sub check_model_init()
{
    if (!$initialized)
    {
        $initialized = 1;
        %check_model_map = (
            "" => \&json_model_1,
        );
    }
}

sub check_model_free()
{
    if ($initialized)
    {
        $initialized = 0;
        %check_model_map = ();
    }
}

sub check_model_mapper($)
{
    my ($name) = @_;
    die "unexpected model name \"$name\"" unless exists $check_model_map{$name};
    return $check_model_map{$name};
}

sub check_model($$$)
{
    my ($json, $name, $rep) = @_;
    die "unexpected model name \"$name\"" unless exists $check_model_map{$name};
    my $path = defined $rep ? [] : undef;
    return $check_model_map{$name}($json, $path, $rep);
}

#
# SCRIPT EXECUTION
#
check_model_init();
jm_main(\&check_model, \%check_model_map, JMC_VERSION);
check_model_free();
