//
// Generated by JSON Model Compiler version 2
// see https://github.com/clairey-zx81/json-model
//

#include <json-model.h>
#define JSON_MODEL_VERSION "2"

static bool json_model_2(const json_t *val, jm_path_t *path, jm_report_t *rep);
static INLINE bool _jm_cst_0_str_test(const char *);
static bool json_model_3(const json_t *val, jm_path_t *path, jm_report_t *rep);
static bool json_model_4(const json_t *val, jm_path_t *path, jm_report_t *rep);
static bool _jm_obj_1(const json_t *val, jm_path_t *path, jm_report_t *rep);
static bool _jm_obj_2(const json_t *val, jm_path_t *path, jm_report_t *rep);
static bool _jm_obj_0(const json_t *val, jm_path_t *path, jm_report_t *rep);
static bool json_model_5(const json_t *val, jm_path_t *path, jm_report_t *rep);
static bool _jm_obj_5(const json_t *val, jm_path_t *path, jm_report_t *rep);
static bool _jm_obj_4(const json_t *val, jm_path_t *path, jm_report_t *rep);
static bool _jm_obj_6(const json_t *val, jm_path_t *path, jm_report_t *rep);
static bool _jm_obj_3(const json_t *val, jm_path_t *path, jm_report_t *rep);
static bool json_model_6(const json_t *val, jm_path_t *path, jm_report_t *rep);
static bool json_model_7(const json_t *val, jm_path_t *path, jm_report_t *rep);
static bool json_model_8(const json_t *val, jm_path_t *path, jm_report_t *rep);
static bool _jm_obj_7(const json_t *val, jm_path_t *path, jm_report_t *rep);
static bool json_model_9(const json_t *val, jm_path_t *path, jm_report_t *rep);
static bool json_model_10(const json_t *val, jm_path_t *path, jm_report_t *rep);
static bool _jm_obj_8(const json_t *val, jm_path_t *path, jm_report_t *rep);
static bool json_model_11(const json_t *val, jm_path_t *path, jm_report_t *rep);
static jm_constmap_t _jm_map_0_tab[3];
static bool json_model_1(const json_t *val, jm_path_t *path, jm_report_t *rep);
jm_propmap_t check_model_map_tab[11];
const size_t check_model_map_size = 11;

// check $Object (.'$Object')
static bool json_model_2(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    // .'$Object'
    if (unlikely(! json_is_object(val)))
    {
        if (rep) jm_report_add_entry(rep, "not an object [.'$Object']", path);
        return false;
    }
    // accept any object
    return true;
}

static INLINE bool _jm_cst_0_str_test(const char *s)
{
    return
           jm_str_eq_8(s, 0x2d31312d34323032LL) && jm_str_eq_3(s + 8, 0x00003530)  // "2024-11-05"
        || jm_str_eq_8(s, 0x2d33302d35323032LL) && jm_str_eq_3(s + 8, 0x00003632)  // "2025-03-26"
        || jm_str_eq_8(s, 0x2d36302d35323032LL) && jm_str_eq_3(s + 8, 0x00003831)  // "2025-06-18"
        || jm_str_eq_8(s, 0x2d31312d35323032LL) && jm_str_eq_3(s + 8, 0x00003532)  // "2025-11-25"
    ;
}

// check $ProtocolVersion (.'$ProtocolVersion')
static bool json_model_3(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    // .'$ProtocolVersion'
    bool res = json_is_string(val) && _jm_cst_0_str_test(json_string_value(val));
    if (unlikely(! res))
    {
        if (rep) jm_report_add_entry(rep, "value not in enum [.'$ProtocolVersion'.'|']", path);
    }
    return res;
}

// check $Tool (.'$Tool')
static bool json_model_4(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    // .'$Tool'
    if (unlikely(! json_is_object(val)))
    {
        if (rep) jm_report_add_entry(rep, "not an object [.'$Tool']", path);
        return false;
    }
    bool res;
    int64_t must_count = 0;
    const char *prop;
    json_t *pval;
    json_object_foreach((json_t *) val, prop, pval)
    {
        jm_path_t lpath_0 = (jm_path_t) { prop, 0, path, NULL };
        if (unlikely(jm_str_eq_5(prop, 0x00000000656d616eLL)))
        {
            // handle must name property
            must_count += 1;
            // .'$Tool'.name
            res = json_is_string(pval);
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "unexpected value for model \"\" [.'$Tool'.name]", (path ? &lpath_0 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'$Tool'.name]", (path ? &lpath_0 : NULL));
                return false;
            }
            continue;
        }
        else if (unlikely(jm_str_eq_8(prop, 0x6863537475706e69LL) && jm_str_eq_4(prop + 8, 0x00616d65)))
        {
            // handle must inputSchema property
            must_count += 1;
            // .'$Tool'.inputSchema
            res = json_model_2(pval, (path ? &lpath_0 : NULL), rep);
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "unexpected value for model \"$Object\" [.'$Tool'.inputSchema]", (path ? &lpath_0 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'$Tool'.inputSchema]", (path ? &lpath_0 : NULL));
                return false;
            }
            continue;
        }
        if (unlikely(jm_str_eq_6(prop, 0x000000656c746974LL)))
        {
            // handle may title property
            // .'$Tool'.title
            res = json_is_string(pval);
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "unexpected value for model \"\" [.'$Tool'.title]", (path ? &lpath_0 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid optional prop value [.'$Tool'.title]", (path ? &lpath_0 : NULL));
                return false;
            }
            continue;
        }
        else if (unlikely(jm_str_eq_8(prop, 0x7470697263736564LL) && jm_str_eq_4(prop + 8, 0x006e6f69)))
        {
            // handle may description property
            // .'$Tool'.description
            res = json_is_string(pval);
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "unexpected value for model \"\" [.'$Tool'.description]", (path ? &lpath_0 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid optional prop value [.'$Tool'.description]", (path ? &lpath_0 : NULL));
                return false;
            }
            continue;
        }
        // accept any other props
    }
    if (unlikely(must_count != 2))
    {
        if (likely(rep != NULL))
        {
            if (! (json_object_get(val, "inputSchema") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <inputSchema> [.'$Tool'.'']", path);
            }
            if (! (json_object_get(val, "name") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <name> [.'$Tool'.'']", path);
            }
        }
        return false;
    }
    return true;
}

// object .'$InitializeRequest'.params.capabilities
static INLINE bool _jm_obj_1(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    if (unlikely(! json_is_object(val)))
    {
        if (rep) jm_report_add_entry(rep, "not an object [.'$InitializeRequest'.params.capabilities]", path);
        return false;
    }
    bool res;
    int64_t must_count = 0;
    const char *prop;
    json_t *pval;
    json_object_foreach((json_t *) val, prop, pval)
    {
        jm_path_t lpath_3 = (jm_path_t) { prop, 0, path, NULL };
        if (jm_str_eq_8(prop, 0x7461746963696c65LL) && jm_str_eq_4(prop + 8, 0x006e6f69))
        {
            // handle must elicitation property
            must_count += 1;
            // .'$InitializeRequest'.params.capabilities.elicitation
            res = json_model_2(pval, (path ? &lpath_3 : NULL), rep);
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "unexpected value for model \"$Object\" [.'$InitializeRequest'.params.capabilities.elicitation]", (path ? &lpath_3 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'$InitializeRequest'.params.capabilities.elicitation]", (path ? &lpath_3 : NULL));
                return false;
            }
            continue;
        }
        // accept any other props
    }
    if (unlikely(must_count != 1))
    {
        if (likely(rep != NULL))
        {
            if (! (json_object_get(val, "elicitation") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <elicitation> [.'$InitializeRequest'.params.capabilities.'']", path);
            }
        }
        return false;
    }
    return true;
}

// object .'$InitializeRequest'.params.clientInfo
static INLINE bool _jm_obj_2(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    if (unlikely(! json_is_object(val)))
    {
        if (rep) jm_report_add_entry(rep, "not an object [.'$InitializeRequest'.params.clientInfo]", path);
        return false;
    }
    bool res;
    int64_t must_count = 0;
    const char *prop;
    json_t *pval;
    json_object_foreach((json_t *) val, prop, pval)
    {
        jm_path_t lpath_4 = (jm_path_t) { prop, 0, path, NULL };
        if (unlikely(jm_str_eq_5(prop, 0x00000000656d616eLL)))
        {
            // handle must name property
            must_count += 1;
            // .'$InitializeRequest'.params.clientInfo.name
            res = json_is_string(pval);
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "unexpected value for model \"\" [.'$InitializeRequest'.params.clientInfo.name]", (path ? &lpath_4 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'$InitializeRequest'.params.clientInfo.name]", (path ? &lpath_4 : NULL));
                return false;
            }
            continue;
        }
        else if (jm_str_eq_8(prop, 0x006e6f6973726576LL))
        {
            // handle must version property
            must_count += 1;
            // .'$InitializeRequest'.params.clientInfo.version
            res = json_is_string(pval);
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "unexpected value for model \"\" [.'$InitializeRequest'.params.clientInfo.version]", (path ? &lpath_4 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'$InitializeRequest'.params.clientInfo.version]", (path ? &lpath_4 : NULL));
                return false;
            }
            continue;
        }
        // accept any other props
    }
    if (unlikely(must_count != 2))
    {
        if (likely(rep != NULL))
        {
            if (! (json_object_get(val, "name") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <name> [.'$InitializeRequest'.params.clientInfo.'']", path);
            }
            if (! (json_object_get(val, "version") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <version> [.'$InitializeRequest'.params.clientInfo.'']", path);
            }
        }
        return false;
    }
    return true;
}

// object .'$InitializeRequest'.params
static INLINE bool _jm_obj_0(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    if (unlikely(! json_is_object(val)))
    {
        if (rep) jm_report_add_entry(rep, "not an object [.'$InitializeRequest'.params]", path);
        return false;
    }
    bool res;
    int64_t must_count = 0;
    const char *prop;
    json_t *pval;
    json_object_foreach((json_t *) val, prop, pval)
    {
        jm_path_t lpath_2 = (jm_path_t) { prop, 0, path, NULL };
        if (unlikely(jm_str_eq_8(prop, 0x6c6f636f746f7270LL) && jm_str_eq_8(prop + 8, 0x006e6f6973726556LL)))
        {
            // handle must protocolVersion property
            must_count += 1;
            // .'$InitializeRequest'.params.protocolVersion
            res = json_model_3(pval, (path ? &lpath_2 : NULL), rep);
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "unexpected value for model \"$ProtocolVersion\" [.'$InitializeRequest'.params.protocolVersion]", (path ? &lpath_2 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'$InitializeRequest'.params.protocolVersion]", (path ? &lpath_2 : NULL));
                return false;
            }
            continue;
        }
        else if (jm_str_eq_8(prop, 0x696c696261706163LL) && jm_str_eq_5(prop + 8, 0x0000000073656974LL))
        {
            // handle must capabilities property
            must_count += 1;
            // .'$InitializeRequest'.params.capabilities
            res = _jm_obj_1(pval, (path ? &lpath_2 : NULL), rep);
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "unexpected element [.'$InitializeRequest'.params.capabilities]", (path ? &lpath_2 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'$InitializeRequest'.params.capabilities]", (path ? &lpath_2 : NULL));
                return false;
            }
            continue;
        }
        else if (likely(jm_str_eq_8(prop, 0x6e49746e65696c63LL) && jm_str_eq_3(prop + 8, 0x00006f66)))
        {
            // handle must clientInfo property
            must_count += 1;
            // .'$InitializeRequest'.params.clientInfo
            res = _jm_obj_2(pval, (path ? &lpath_2 : NULL), rep);
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "unexpected element [.'$InitializeRequest'.params.clientInfo]", (path ? &lpath_2 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'$InitializeRequest'.params.clientInfo]", (path ? &lpath_2 : NULL));
                return false;
            }
            continue;
        }
        if (rep) jm_report_add_entry(rep, "unexpected prop [.'$InitializeRequest'.params]", (path ? &lpath_2 : NULL));
        return false;
    }
    if (unlikely(must_count != 3))
    {
        if (likely(rep != NULL))
        {
            if (! (json_object_get(val, "capabilities") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <capabilities> [.'$InitializeRequest'.params]", path);
            }
            if (! (json_object_get(val, "clientInfo") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <clientInfo> [.'$InitializeRequest'.params]", path);
            }
            if (! (json_object_get(val, "protocolVersion") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <protocolVersion> [.'$InitializeRequest'.params]", path);
            }
        }
        return false;
    }
    return true;
}

// check $InitializeRequest (.'$InitializeRequest')
static bool json_model_5(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    // .'$InitializeRequest'
    if (unlikely(! json_is_object(val)))
    {
        if (rep) jm_report_add_entry(rep, "not an object [.'$InitializeRequest']", path);
        return false;
    }
    bool res;
    int64_t must_count = 0;
    const char *prop;
    json_t *pval;
    json_object_foreach((json_t *) val, prop, pval)
    {
        jm_path_t lpath_1 = (jm_path_t) { prop, 0, path, NULL };
        if (unlikely(jm_str_eq_8(prop, 0x006370726e6f736aLL)))
        {
            // handle must jsonrpc property
            must_count += 1;
            // .'$InitializeRequest'.jsonrpc
            res = json_is_string(pval) && jm_str_eq_4(json_string_value(pval), 0x00302e32);
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "unexpected value for model \"_2.0\" [.'$InitializeRequest'.jsonrpc]", (path ? &lpath_1 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'$InitializeRequest'.jsonrpc]", (path ? &lpath_1 : NULL));
                return false;
            }
            continue;
        }
        else if (unlikely(jm_str_eq_3(prop, 0x00006469)))
        {
            // handle must id property
            must_count += 1;
            // .'$InitializeRequest'.id
            res = json_is_integer(pval);
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "not a -1 strict int [.'$InitializeRequest'.id]", (path ? &lpath_1 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'$InitializeRequest'.id]", (path ? &lpath_1 : NULL));
                return false;
            }
            continue;
        }
        else if (jm_str_eq_7(prop, 0x0000646f6874656dLL))
        {
            // handle must method property
            must_count += 1;
            // .'$InitializeRequest'.method
            const char *sval_0;
            res = json_is_string(pval) && ((sval_0 = json_string_value(pval)), jm_str_eq_8(sval_0, 0x696c616974696e69LL) && jm_str_eq_3(sval_0 + 8, 0x0000657a));
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "unexpected value for model \"_initialize\" [.'$InitializeRequest'.method]", (path ? &lpath_1 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'$InitializeRequest'.method]", (path ? &lpath_1 : NULL));
                return false;
            }
            continue;
        }
        else if (likely(jm_str_eq_7(prop, 0x0000736d61726170LL)))
        {
            // handle must params property
            must_count += 1;
            // .'$InitializeRequest'.params
            res = _jm_obj_0(pval, (path ? &lpath_1 : NULL), rep);
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "unexpected element [.'$InitializeRequest'.params]", (path ? &lpath_1 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'$InitializeRequest'.params]", (path ? &lpath_1 : NULL));
                return false;
            }
            continue;
        }
        if (rep) jm_report_add_entry(rep, "unexpected prop [.'$InitializeRequest']", (path ? &lpath_1 : NULL));
        return false;
    }
    if (unlikely(must_count != 4))
    {
        if (likely(rep != NULL))
        {
            if (! (json_object_get(val, "id") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <id> [.'$InitializeRequest']", path);
            }
            if (! (json_object_get(val, "jsonrpc") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <jsonrpc> [.'$InitializeRequest']", path);
            }
            if (! (json_object_get(val, "method") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <method> [.'$InitializeRequest']", path);
            }
            if (! (json_object_get(val, "params") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <params> [.'$InitializeRequest']", path);
            }
        }
        return false;
    }
    return true;
}

// object .'$InitializeResponse'.result.capabilities.tools
static INLINE bool _jm_obj_5(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    if (unlikely(! json_is_object(val)))
    {
        if (rep) jm_report_add_entry(rep, "not an object [.'$InitializeResponse'.result.capabilities.tools]", path);
        return false;
    }
    bool res;
    int64_t must_count = 0;
    const char *prop;
    json_t *pval;
    json_object_foreach((json_t *) val, prop, pval)
    {
        jm_path_t lpath_8 = (jm_path_t) { prop, 0, path, NULL };
        if (likely(jm_str_eq_8(prop, 0x6e6168437473696cLL) && jm_str_eq_4(prop + 8, 0x00646567)))
        {
            // handle must listChanged property
            must_count += 1;
            // .'$InitializeResponse'.result.capabilities.tools.listChanged
            res = json_is_boolean(pval);
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "not a bool [.'$InitializeResponse'.result.capabilities.tools.listChanged]", (path ? &lpath_8 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'$InitializeResponse'.result.capabilities.tools.listChanged]", (path ? &lpath_8 : NULL));
                return false;
            }
            continue;
        }
        if (rep) jm_report_add_entry(rep, "unexpected prop [.'$InitializeResponse'.result.capabilities.tools]", (path ? &lpath_8 : NULL));
        return false;
    }
    if (unlikely(must_count != 1))
    {
        if (likely(rep != NULL))
        {
            if (! (json_object_get(val, "listChanged") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <listChanged> [.'$InitializeResponse'.result.capabilities.tools]", path);
            }
        }
        return false;
    }
    return true;
}

// object .'$InitializeResponse'.result.capabilities
static INLINE bool _jm_obj_4(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    if (unlikely(! json_is_object(val)))
    {
        if (rep) jm_report_add_entry(rep, "not an object [.'$InitializeResponse'.result.capabilities]", path);
        return false;
    }
    bool res;
    int64_t must_count = 0;
    const char *prop;
    json_t *pval;
    json_object_foreach((json_t *) val, prop, pval)
    {
        jm_path_t lpath_7 = (jm_path_t) { prop, 0, path, NULL };
        if (unlikely(jm_str_eq_6(prop, 0x000000736c6f6f74LL)))
        {
            // handle must tools property
            must_count += 1;
            // .'$InitializeResponse'.result.capabilities.tools
            res = _jm_obj_5(pval, (path ? &lpath_7 : NULL), rep);
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "unexpected element [.'$InitializeResponse'.result.capabilities.tools]", (path ? &lpath_7 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'$InitializeResponse'.result.capabilities.tools]", (path ? &lpath_7 : NULL));
                return false;
            }
            continue;
        }
        else if (jm_str_eq_8(prop, 0x656372756f736572LL) && jm_str_eq_2(prop + 8, 0x00000073))
        {
            // handle must resources property
            must_count += 1;
            // .'$InitializeResponse'.result.capabilities.resources
            res = json_model_2(pval, (path ? &lpath_7 : NULL), rep);
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "unexpected value for model \"$Object\" [.'$InitializeResponse'.result.capabilities.resources]", (path ? &lpath_7 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'$InitializeResponse'.result.capabilities.resources]", (path ? &lpath_7 : NULL));
                return false;
            }
            continue;
        }
        // accept any other props
    }
    if (unlikely(must_count != 2))
    {
        if (likely(rep != NULL))
        {
            if (! (json_object_get(val, "resources") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <resources> [.'$InitializeResponse'.result.capabilities.'']", path);
            }
            if (! (json_object_get(val, "tools") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <tools> [.'$InitializeResponse'.result.capabilities.'']", path);
            }
        }
        return false;
    }
    return true;
}

// object .'$InitializeResponse'.result.serverInfo
static INLINE bool _jm_obj_6(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    if (unlikely(! json_is_object(val)))
    {
        if (rep) jm_report_add_entry(rep, "not an object [.'$InitializeResponse'.result.serverInfo]", path);
        return false;
    }
    bool res;
    int64_t must_count = 0;
    const char *prop;
    json_t *pval;
    json_object_foreach((json_t *) val, prop, pval)
    {
        jm_path_t lpath_9 = (jm_path_t) { prop, 0, path, NULL };
        if (unlikely(jm_str_eq_5(prop, 0x00000000656d616eLL)))
        {
            // handle must name property
            must_count += 1;
            // .'$InitializeResponse'.result.serverInfo.name
            res = json_is_string(pval);
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "unexpected value for model \"\" [.'$InitializeResponse'.result.serverInfo.name]", (path ? &lpath_9 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'$InitializeResponse'.result.serverInfo.name]", (path ? &lpath_9 : NULL));
                return false;
            }
            continue;
        }
        else if (jm_str_eq_8(prop, 0x006e6f6973726576LL))
        {
            // handle must version property
            must_count += 1;
            // .'$InitializeResponse'.result.serverInfo.version
            res = json_is_string(pval);
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "unexpected value for model \"\" [.'$InitializeResponse'.result.serverInfo.version]", (path ? &lpath_9 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'$InitializeResponse'.result.serverInfo.version]", (path ? &lpath_9 : NULL));
                return false;
            }
            continue;
        }
        // accept any other props
    }
    if (unlikely(must_count != 2))
    {
        if (likely(rep != NULL))
        {
            if (! (json_object_get(val, "name") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <name> [.'$InitializeResponse'.result.serverInfo.'']", path);
            }
            if (! (json_object_get(val, "version") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <version> [.'$InitializeResponse'.result.serverInfo.'']", path);
            }
        }
        return false;
    }
    return true;
}

// object .'$InitializeResponse'.result
static INLINE bool _jm_obj_3(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    if (unlikely(! json_is_object(val)))
    {
        if (rep) jm_report_add_entry(rep, "not an object [.'$InitializeResponse'.result]", path);
        return false;
    }
    bool res;
    int64_t must_count = 0;
    const char *prop;
    json_t *pval;
    json_object_foreach((json_t *) val, prop, pval)
    {
        jm_path_t lpath_6 = (jm_path_t) { prop, 0, path, NULL };
        if (unlikely(jm_str_eq_8(prop, 0x6c6f636f746f7270LL) && jm_str_eq_8(prop + 8, 0x006e6f6973726556LL)))
        {
            // handle must protocolVersion property
            must_count += 1;
            // .'$InitializeResponse'.result.protocolVersion
            res = json_model_3(pval, (path ? &lpath_6 : NULL), rep);
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "unexpected value for model \"$ProtocolVersion\" [.'$InitializeResponse'.result.protocolVersion]", (path ? &lpath_6 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'$InitializeResponse'.result.protocolVersion]", (path ? &lpath_6 : NULL));
                return false;
            }
            continue;
        }
        else if (jm_str_eq_8(prop, 0x696c696261706163LL) && jm_str_eq_5(prop + 8, 0x0000000073656974LL))
        {
            // handle must capabilities property
            must_count += 1;
            // .'$InitializeResponse'.result.capabilities
            res = _jm_obj_4(pval, (path ? &lpath_6 : NULL), rep);
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "unexpected element [.'$InitializeResponse'.result.capabilities]", (path ? &lpath_6 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'$InitializeResponse'.result.capabilities]", (path ? &lpath_6 : NULL));
                return false;
            }
            continue;
        }
        else if (likely(jm_str_eq_8(prop, 0x6e49726576726573LL) && jm_str_eq_3(prop + 8, 0x00006f66)))
        {
            // handle must serverInfo property
            must_count += 1;
            // .'$InitializeResponse'.result.serverInfo
            res = _jm_obj_6(pval, (path ? &lpath_6 : NULL), rep);
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "unexpected element [.'$InitializeResponse'.result.serverInfo]", (path ? &lpath_6 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'$InitializeResponse'.result.serverInfo]", (path ? &lpath_6 : NULL));
                return false;
            }
            continue;
        }
        if (rep) jm_report_add_entry(rep, "unexpected prop [.'$InitializeResponse'.result]", (path ? &lpath_6 : NULL));
        return false;
    }
    if (unlikely(must_count != 3))
    {
        if (likely(rep != NULL))
        {
            if (! (json_object_get(val, "capabilities") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <capabilities> [.'$InitializeResponse'.result]", path);
            }
            if (! (json_object_get(val, "protocolVersion") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <protocolVersion> [.'$InitializeResponse'.result]", path);
            }
            if (! (json_object_get(val, "serverInfo") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <serverInfo> [.'$InitializeResponse'.result]", path);
            }
        }
        return false;
    }
    return true;
}

// check $InitializeResponse (.'$InitializeResponse')
static bool json_model_6(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    // .'$InitializeResponse'
    if (unlikely(! json_is_object(val)))
    {
        if (rep) jm_report_add_entry(rep, "not an object [.'$InitializeResponse']", path);
        return false;
    }
    bool res;
    int64_t must_count = 0;
    const char *prop;
    json_t *pval;
    json_object_foreach((json_t *) val, prop, pval)
    {
        jm_path_t lpath_5 = (jm_path_t) { prop, 0, path, NULL };
        if (unlikely(jm_str_eq_8(prop, 0x006370726e6f736aLL)))
        {
            // handle must jsonrpc property
            must_count += 1;
            // .'$InitializeResponse'.jsonrpc
            res = json_is_string(pval) && jm_str_eq_4(json_string_value(pval), 0x00302e32);
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "unexpected value for model \"_2.0\" [.'$InitializeResponse'.jsonrpc]", (path ? &lpath_5 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'$InitializeResponse'.jsonrpc]", (path ? &lpath_5 : NULL));
                return false;
            }
            continue;
        }
        else if (jm_str_eq_3(prop, 0x00006469))
        {
            // handle must id property
            must_count += 1;
            // .'$InitializeResponse'.id
            res = json_is_integer(pval);
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "not a -1 strict int [.'$InitializeResponse'.id]", (path ? &lpath_5 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'$InitializeResponse'.id]", (path ? &lpath_5 : NULL));
                return false;
            }
            continue;
        }
        else if (likely(jm_str_eq_7(prop, 0x0000746c75736572LL)))
        {
            // handle must result property
            must_count += 1;
            // .'$InitializeResponse'.result
            res = _jm_obj_3(pval, (path ? &lpath_5 : NULL), rep);
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "unexpected element [.'$InitializeResponse'.result]", (path ? &lpath_5 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'$InitializeResponse'.result]", (path ? &lpath_5 : NULL));
                return false;
            }
            continue;
        }
        if (rep) jm_report_add_entry(rep, "unexpected prop [.'$InitializeResponse']", (path ? &lpath_5 : NULL));
        return false;
    }
    if (unlikely(must_count != 3))
    {
        if (likely(rep != NULL))
        {
            if (! (json_object_get(val, "id") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <id> [.'$InitializeResponse']", path);
            }
            if (! (json_object_get(val, "jsonrpc") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <jsonrpc> [.'$InitializeResponse']", path);
            }
            if (! (json_object_get(val, "result") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <result> [.'$InitializeResponse']", path);
            }
        }
        return false;
    }
    return true;
}

// check $Notification (.'$Notification')
static bool json_model_7(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    // .'$Notification'
    if (unlikely(! json_is_object(val)))
    {
        if (rep) jm_report_add_entry(rep, "not an object [.'$Notification']", path);
        return false;
    }
    bool res;
    int64_t must_count = 0;
    const char *prop;
    json_t *pval;
    json_object_foreach((json_t *) val, prop, pval)
    {
        jm_path_t lpath_10 = (jm_path_t) { prop, 0, path, NULL };
        if (jm_str_eq_8(prop, 0x006370726e6f736aLL))
        {
            // handle must jsonrpc property
            must_count += 1;
            // .'$Notification'.jsonrpc
            res = json_is_string(pval) && jm_str_eq_4(json_string_value(pval), 0x00302e32);
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "unexpected value for model \"_2.0\" [.'$Notification'.jsonrpc]", (path ? &lpath_10 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'$Notification'.jsonrpc]", (path ? &lpath_10 : NULL));
                return false;
            }
            continue;
        }
        else if (likely(jm_str_eq_7(prop, 0x0000646f6874656dLL)))
        {
            // handle must method property
            must_count += 1;
            // .'$Notification'.method
            // "/^notifications/"
            const char *sval_1;
            res = json_is_string(pval) && ((sval_1 = json_string_value(pval)), jm_str_eq_8(sval_1, 0x6163696669746f6eLL) && jm_str_eq_5(sval_1 + 8, 0x000000736e6f6974LL));
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "unexpected value for model \"/^notifications/\" [.'$Notification'.method]", (path ? &lpath_10 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'$Notification'.method]", (path ? &lpath_10 : NULL));
                return false;
            }
            continue;
        }
        if (rep) jm_report_add_entry(rep, "unexpected prop [.'$Notification']", (path ? &lpath_10 : NULL));
        return false;
    }
    if (unlikely(must_count != 2))
    {
        if (likely(rep != NULL))
        {
            if (! (json_object_get(val, "jsonrpc") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <jsonrpc> [.'$Notification']", path);
            }
            if (! (json_object_get(val, "method") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <method> [.'$Notification']", path);
            }
        }
        return false;
    }
    return true;
}

// check $ToolsListRequest (.'$ToolsListRequest')
static bool json_model_8(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    // .'$ToolsListRequest'
    if (unlikely(! json_is_object(val)))
    {
        if (rep) jm_report_add_entry(rep, "not an object [.'$ToolsListRequest']", path);
        return false;
    }
    bool res;
    int64_t must_count = 0;
    const char *prop;
    json_t *pval;
    json_object_foreach((json_t *) val, prop, pval)
    {
        jm_path_t lpath_11 = (jm_path_t) { prop, 0, path, NULL };
        if (unlikely(jm_str_eq_8(prop, 0x006370726e6f736aLL)))
        {
            // handle must jsonrpc property
            must_count += 1;
            // .'$ToolsListRequest'.jsonrpc
            res = json_is_string(pval) && jm_str_eq_4(json_string_value(pval), 0x00302e32);
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "unexpected value for model \"_2.0\" [.'$ToolsListRequest'.jsonrpc]", (path ? &lpath_11 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'$ToolsListRequest'.jsonrpc]", (path ? &lpath_11 : NULL));
                return false;
            }
            continue;
        }
        else if (unlikely(jm_str_eq_3(prop, 0x00006469)))
        {
            // handle must id property
            must_count += 1;
            // .'$ToolsListRequest'.id
            res = json_is_integer(pval);
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "not a -1 strict int [.'$ToolsListRequest'.id]", (path ? &lpath_11 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'$ToolsListRequest'.id]", (path ? &lpath_11 : NULL));
                return false;
            }
            continue;
        }
        else if (jm_str_eq_7(prop, 0x0000646f6874656dLL))
        {
            // handle must method property
            must_count += 1;
            // .'$ToolsListRequest'.method
            const char *sval_2;
            res = json_is_string(pval) && ((sval_2 = json_string_value(pval)), jm_str_eq_8(sval_2, 0x696c2f736c6f6f74LL) && jm_str_eq_3(sval_2 + 8, 0x00007473));
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "unexpected value for model \"_tools/list\" [.'$ToolsListRequest'.method]", (path ? &lpath_11 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'$ToolsListRequest'.method]", (path ? &lpath_11 : NULL));
                return false;
            }
            continue;
        }
        // accept any other props
    }
    if (unlikely(must_count != 3))
    {
        if (likely(rep != NULL))
        {
            if (! (json_object_get(val, "id") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <id> [.'$ToolsListRequest'.'']", path);
            }
            if (! (json_object_get(val, "jsonrpc") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <jsonrpc> [.'$ToolsListRequest'.'']", path);
            }
            if (! (json_object_get(val, "method") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <method> [.'$ToolsListRequest'.'']", path);
            }
        }
        return false;
    }
    return true;
}

// object .'$ToolsListResponse'.result
static INLINE bool _jm_obj_7(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    if (unlikely(! json_is_object(val)))
    {
        if (rep) jm_report_add_entry(rep, "not an object [.'$ToolsListResponse'.result]", path);
        return false;
    }
    bool res;
    int64_t must_count = 0;
    const char *prop;
    json_t *pval;
    json_object_foreach((json_t *) val, prop, pval)
    {
        jm_path_t lpath_13 = (jm_path_t) { prop, 0, path, NULL };
        if (likely(jm_str_eq_6(prop, 0x000000736c6f6f74LL)))
        {
            // handle must tools property
            must_count += 1;
            // .'$ToolsListResponse'.result.tools
            // .'$ToolsListResponse'.result.tools.'@'
            res = json_is_array(pval);
            if (likely(res))
            {
                size_t arr_0_idx;
                json_t *arr_0_item;
                json_array_foreach(pval, arr_0_idx, arr_0_item)
                {
                    jm_path_t arr_0_lpath = (jm_path_t) { NULL, arr_0_idx, (path ? &lpath_13 : NULL), NULL };
                    // .'$ToolsListResponse'.result.tools.'@'.0
                    res = json_model_4(arr_0_item, ((path ? &lpath_13 : NULL) ? &arr_0_lpath : NULL), rep);
                    if (unlikely(! res))
                    {
                        if (rep) jm_report_add_entry(rep, "unexpected value for model \"$Tool\" [.'$ToolsListResponse'.result.tools.'@'.0]", ((path ? &lpath_13 : NULL) ? &arr_0_lpath : NULL));
                        break;
                    }
                }
            }
            if (likely(res))
            {
                res = jm_obj_array_is_unique(pval, (path ? &lpath_13 : NULL), rep);
                if (unlikely(! res))
                {
                    if (rep) jm_report_add_entry(rep, "constraints failed [.'$ToolsListResponse'.result.tools]", (path ? &lpath_13 : NULL));
                }
            }
            else
            {
                if (rep) jm_report_add_entry(rep, "not array or unexpected array [.'$ToolsListResponse'.result.tools.'@']", (path ? &lpath_13 : NULL));
            }
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'$ToolsListResponse'.result.tools]", (path ? &lpath_13 : NULL));
                return false;
            }
            continue;
        }
        if (rep) jm_report_add_entry(rep, "unexpected prop [.'$ToolsListResponse'.result]", (path ? &lpath_13 : NULL));
        return false;
    }
    if (unlikely(must_count != 1))
    {
        if (likely(rep != NULL))
        {
            if (! (json_object_get(val, "tools") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <tools> [.'$ToolsListResponse'.result]", path);
            }
        }
        return false;
    }
    return true;
}

// check $ToolsListResponse (.'$ToolsListResponse')
static bool json_model_9(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    // .'$ToolsListResponse'
    if (unlikely(! json_is_object(val)))
    {
        if (rep) jm_report_add_entry(rep, "not an object [.'$ToolsListResponse']", path);
        return false;
    }
    bool res;
    int64_t must_count = 0;
    const char *prop;
    json_t *pval;
    json_object_foreach((json_t *) val, prop, pval)
    {
        jm_path_t lpath_12 = (jm_path_t) { prop, 0, path, NULL };
        if (unlikely(jm_str_eq_8(prop, 0x006370726e6f736aLL)))
        {
            // handle must jsonrpc property
            must_count += 1;
            // .'$ToolsListResponse'.jsonrpc
            res = json_is_string(pval) && jm_str_eq_4(json_string_value(pval), 0x00302e32);
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "unexpected value for model \"_2.0\" [.'$ToolsListResponse'.jsonrpc]", (path ? &lpath_12 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'$ToolsListResponse'.jsonrpc]", (path ? &lpath_12 : NULL));
                return false;
            }
            continue;
        }
        else if (jm_str_eq_3(prop, 0x00006469))
        {
            // handle must id property
            must_count += 1;
            // .'$ToolsListResponse'.id
            res = json_is_integer(pval);
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "not a -1 strict int [.'$ToolsListResponse'.id]", (path ? &lpath_12 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'$ToolsListResponse'.id]", (path ? &lpath_12 : NULL));
                return false;
            }
            continue;
        }
        else if (likely(jm_str_eq_7(prop, 0x0000746c75736572LL)))
        {
            // handle must result property
            must_count += 1;
            // .'$ToolsListResponse'.result
            res = _jm_obj_7(pval, (path ? &lpath_12 : NULL), rep);
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "unexpected element [.'$ToolsListResponse'.result]", (path ? &lpath_12 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'$ToolsListResponse'.result]", (path ? &lpath_12 : NULL));
                return false;
            }
            continue;
        }
        if (rep) jm_report_add_entry(rep, "unexpected prop [.'$ToolsListResponse']", (path ? &lpath_12 : NULL));
        return false;
    }
    if (unlikely(must_count != 3))
    {
        if (likely(rep != NULL))
        {
            if (! (json_object_get(val, "id") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <id> [.'$ToolsListResponse']", path);
            }
            if (! (json_object_get(val, "jsonrpc") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <jsonrpc> [.'$ToolsListResponse']", path);
            }
            if (! (json_object_get(val, "result") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <result> [.'$ToolsListResponse']", path);
            }
        }
        return false;
    }
    return true;
}

// check $ToolCallRequest (.'$ToolCallRequest')
static bool json_model_10(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    // .'$ToolCallRequest'
    if (unlikely(! json_is_object(val)))
    {
        if (rep) jm_report_add_entry(rep, "not an object [.'$ToolCallRequest']", path);
        return false;
    }
    bool res;
    int64_t must_count = 0;
    const char *prop;
    json_t *pval;
    json_object_foreach((json_t *) val, prop, pval)
    {
        jm_path_t lpath_14 = (jm_path_t) { prop, 0, path, NULL };
        if (unlikely(jm_str_eq_8(prop, 0x006370726e6f736aLL)))
        {
            // handle must jsonrpc property
            must_count += 1;
            // .'$ToolCallRequest'.jsonrpc
            res = json_is_string(pval) && jm_str_eq_4(json_string_value(pval), 0x00302e32);
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "unexpected value for model \"_2.0\" [.'$ToolCallRequest'.jsonrpc]", (path ? &lpath_14 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'$ToolCallRequest'.jsonrpc]", (path ? &lpath_14 : NULL));
                return false;
            }
            continue;
        }
        else if (unlikely(jm_str_eq_3(prop, 0x00006469)))
        {
            // handle must id property
            must_count += 1;
            // .'$ToolCallRequest'.id
            res = json_is_integer(pval);
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "not a -1 strict int [.'$ToolCallRequest'.id]", (path ? &lpath_14 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'$ToolCallRequest'.id]", (path ? &lpath_14 : NULL));
                return false;
            }
            continue;
        }
        else if (jm_str_eq_7(prop, 0x0000646f6874656dLL))
        {
            // handle must method property
            must_count += 1;
            // .'$ToolCallRequest'.method
            const char *sval_3;
            res = json_is_string(pval) && ((sval_3 = json_string_value(pval)), jm_str_eq_8(sval_3, 0x61632f736c6f6f74LL) && jm_str_eq_3(sval_3 + 8, 0x00006c6c));
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "unexpected value for model \"_tools/call\" [.'$ToolCallRequest'.method]", (path ? &lpath_14 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'$ToolCallRequest'.method]", (path ? &lpath_14 : NULL));
                return false;
            }
            continue;
        }
        else if (likely(jm_str_eq_7(prop, 0x0000736d61726170LL)))
        {
            // handle must params property
            must_count += 1;
            // .'$ToolCallRequest'.params
            res = json_model_2(pval, (path ? &lpath_14 : NULL), rep);
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "unexpected value for model \"$Object\" [.'$ToolCallRequest'.params]", (path ? &lpath_14 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'$ToolCallRequest'.params]", (path ? &lpath_14 : NULL));
                return false;
            }
            continue;
        }
        if (rep) jm_report_add_entry(rep, "unexpected prop [.'$ToolCallRequest']", (path ? &lpath_14 : NULL));
        return false;
    }
    if (unlikely(must_count != 4))
    {
        if (likely(rep != NULL))
        {
            if (! (json_object_get(val, "id") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <id> [.'$ToolCallRequest']", path);
            }
            if (! (json_object_get(val, "jsonrpc") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <jsonrpc> [.'$ToolCallRequest']", path);
            }
            if (! (json_object_get(val, "method") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <method> [.'$ToolCallRequest']", path);
            }
            if (! (json_object_get(val, "params") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <params> [.'$ToolCallRequest']", path);
            }
        }
        return false;
    }
    return true;
}

// object .'$ToolCallResponse'.result
static INLINE bool _jm_obj_8(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    if (unlikely(! json_is_object(val)))
    {
        if (rep) jm_report_add_entry(rep, "not an object [.'$ToolCallResponse'.result]", path);
        return false;
    }
    bool res;
    int64_t must_count = 0;
    const char *prop;
    json_t *pval;
    json_object_foreach((json_t *) val, prop, pval)
    {
        jm_path_t lpath_16 = (jm_path_t) { prop, 0, path, NULL };
        if (jm_str_eq_8(prop, 0x00746e65746e6f63LL))
        {
            // handle must content property
            must_count += 1;
            // .'$ToolCallResponse'.result.content
            res = json_is_array(pval);
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "not array or unexpected array [.'$ToolCallResponse'.result.content]", (path ? &lpath_16 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'$ToolCallResponse'.result.content]", (path ? &lpath_16 : NULL));
                return false;
            }
            continue;
        }
        // accept any other props
    }
    if (unlikely(must_count != 1))
    {
        if (likely(rep != NULL))
        {
            if (! (json_object_get(val, "content") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <content> [.'$ToolCallResponse'.result.'']", path);
            }
        }
        return false;
    }
    return true;
}

// check $ToolCallResponse (.'$ToolCallResponse')
static bool json_model_11(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    // .'$ToolCallResponse'
    if (unlikely(! json_is_object(val)))
    {
        if (rep) jm_report_add_entry(rep, "not an object [.'$ToolCallResponse']", path);
        return false;
    }
    bool res;
    int64_t must_count = 0;
    const char *prop;
    json_t *pval;
    json_object_foreach((json_t *) val, prop, pval)
    {
        jm_path_t lpath_15 = (jm_path_t) { prop, 0, path, NULL };
        if (unlikely(jm_str_eq_8(prop, 0x006370726e6f736aLL)))
        {
            // handle must jsonrpc property
            must_count += 1;
            // .'$ToolCallResponse'.jsonrpc
            res = json_is_string(pval) && jm_str_eq_4(json_string_value(pval), 0x00302e32);
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "unexpected value for model \"_2.0\" [.'$ToolCallResponse'.jsonrpc]", (path ? &lpath_15 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'$ToolCallResponse'.jsonrpc]", (path ? &lpath_15 : NULL));
                return false;
            }
            continue;
        }
        else if (jm_str_eq_3(prop, 0x00006469))
        {
            // handle must id property
            must_count += 1;
            // .'$ToolCallResponse'.id
            res = json_is_integer(pval);
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "not a -1 strict int [.'$ToolCallResponse'.id]", (path ? &lpath_15 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'$ToolCallResponse'.id]", (path ? &lpath_15 : NULL));
                return false;
            }
            continue;
        }
        else if (likely(jm_str_eq_7(prop, 0x0000746c75736572LL)))
        {
            // handle must result property
            must_count += 1;
            // .'$ToolCallResponse'.result
            res = _jm_obj_8(pval, (path ? &lpath_15 : NULL), rep);
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "unexpected element [.'$ToolCallResponse'.result]", (path ? &lpath_15 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'$ToolCallResponse'.result]", (path ? &lpath_15 : NULL));
                return false;
            }
            continue;
        }
        if (rep) jm_report_add_entry(rep, "unexpected prop [.'$ToolCallResponse']", (path ? &lpath_15 : NULL));
        return false;
    }
    if (unlikely(must_count != 3))
    {
        if (likely(rep != NULL))
        {
            if (! (json_object_get(val, "id") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <id> [.'$ToolCallResponse']", path);
            }
            if (! (json_object_get(val, "jsonrpc") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <jsonrpc> [.'$ToolCallResponse']", path);
            }
            if (! (json_object_get(val, "result") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <result> [.'$ToolCallResponse']", path);
            }
        }
        return false;
    }
    return true;
}

static INLINE jm_check_fun_t _jm_map_0(json_t *val)
{
    jm_constant_t cst;
    jm_set_cst(&cst, val);
    return jm_search_constmap(&cst, _jm_map_0_tab, 3);
}

// check $ (.)
static bool json_model_1(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    // MCP - Model Context Protocol
    // .
    bool res = json_is_object(val);
    if (likely(res))
    {
        json_t * tag_0;
        if (likely((tag_0 = json_object_get(val, "method")) != NULL))
        {
            jm_check_fun_t fun_0 = _jm_map_0(tag_0);
            if (likely(fun_0 != NULL))
                res = fun_0(val, path, rep);
            else
            {
                res = false;
                if (rep) jm_report_add_entry(rep, "tag <method> value not found [.'|']", path);
            }
        }
        else
        {
            res = false;
            if (rep) jm_report_add_entry(rep, "tag prop <method> is missing [.'|']", path);
        }
    }
    else
    {
        if (rep) jm_report_add_entry(rep, "value is not an object [.'|']", path);
    }
    return res || json_model_6(val, path, rep) || json_model_7(val, path, rep) || json_model_9(val, path, rep) || json_model_11(val, path, rep);
}

jm_check_fun_t check_model_map(const char *pname)
{
    return jm_search_propmap(pname, check_model_map_tab, 11);
}

static bool initialized = false;

const char *check_model_init(void)
{
    if (!initialized)
    {
        initialized = true;
        jm_version_string = JSON_MODEL_VERSION;
        _jm_map_0_tab[0] = (jm_constmap_t) { (jm_constant_t) { 11, { .s = "initialize" } }, json_model_5 };
        _jm_map_0_tab[1] = (jm_constmap_t) { (jm_constant_t) { 11, { .s = "tools/list" } }, json_model_8 };
        _jm_map_0_tab[2] = (jm_constmap_t) { (jm_constant_t) { 11, { .s = "tools/call" } }, json_model_10 };
        jm_sort_constmap(_jm_map_0_tab, 3);
        check_model_map_tab[0] = (jm_propmap_t) { "", json_model_1 };
        check_model_map_tab[1] = (jm_propmap_t) { "Object", json_model_2 };
        check_model_map_tab[2] = (jm_propmap_t) { "ProtocolVersion", json_model_3 };
        check_model_map_tab[3] = (jm_propmap_t) { "Tool", json_model_4 };
        check_model_map_tab[4] = (jm_propmap_t) { "InitializeRequest", json_model_5 };
        check_model_map_tab[5] = (jm_propmap_t) { "InitializeResponse", json_model_6 };
        check_model_map_tab[6] = (jm_propmap_t) { "Notification", json_model_7 };
        check_model_map_tab[7] = (jm_propmap_t) { "ToolsListRequest", json_model_8 };
        check_model_map_tab[8] = (jm_propmap_t) { "ToolsListResponse", json_model_9 };
        check_model_map_tab[9] = (jm_propmap_t) { "ToolCallRequest", json_model_10 };
        check_model_map_tab[10] = (jm_propmap_t) { "ToolCallResponse", json_model_11 };
        jm_sort_propmap(check_model_map_tab, 11);
    }
    return NULL;
}

void check_model_free(void)
{
    if (initialized)
        initialized = false;

        // cleanup code
}

/*
 * # API
 *
 * ```c
 * bool error;
 * char *message;
 *
 * bool valid = check(json_value, "model-name", &error, &message);
 *
 * if (message)
 *    free(message), message = NULL;
 * ```
 *
 * - `error` is set of there was an error during initialization or if the model does not exists.
 * - `message` is provided if a non NULL pointer is passed, and must be freed by the caller.
 */
bool
check_model(const json_t *val, const char *name, bool *error, char **reasons)
{
    return jm_generic_entry(check_model_init, check_model_map, val, name, error, reasons);
}
