//
// Generated by JSON Model Compiler version 2
// see https://github.com/clairey-zx81/json-model
//
import 'dart:convert';
import 'dart:io';
import 'dart:core';
// Import the helper library generated nearby
import 'package:json_model_runtime/json_model.dart' as runtime;
import 'package:json_model_runtime/main.dart';

//
// Generated JSON Model Validator
//
const String JSON_MODEL_VERSION = "2";

var check_model_map = <String, Function>{};

// check $bibi (.'$bibi')
bool json_model_2(dynamic val, List<dynamic>? path, List<dynamic>? rep)
{
    // .'$bibi'
    // check close must only props
    if (! (val is Map))
    {
        rep?.add(["not an object [.'\$bibi']", path]);
        return false;
    }
    if (val.length != 1)
    {
        rep?.add(["bad property count [.'\$bibi']", path]);
        return false;
    }
    List<dynamic>? lpath;
    dynamic pval;
    bool res;
    if (! val.containsKey("bibi"))
    {
        rep?.add(["missing mandatory prop <bibi> [.'\$bibi']", path]);
        return false;
    }
    lpath = (path != null ? [...?path, "bibi"] : null);
    pval = val["bibi"];
    // .'$bibi'.bibi
    res = (pval is List);
    if (res)
    {
        for (var arr_0_idx = 0; arr_0_idx < pval.length; arr_0_idx++)
        {
            var arr_0_item = pval[arr_0_idx];
            List<dynamic>? arr_0_lpath = ((path != null ? lpath : null) != null ? [...?(path != null ? lpath : null), arr_0_idx] : null);
            // .'$bibi'.bibi.0
            res = json_model_2(arr_0_item, ((path != null ? lpath : null) != null ? arr_0_lpath : null), rep);
            if (! res)
            {
                rep?.add(["unexpected \$bibi [.'\$bibi'.bibi.0]", ((path != null ? lpath : null) != null ? arr_0_lpath : null)]);
                break;
            }
        }
    }
    if (! res)
    {
        rep?.add(["not array or unexpected array [.'\$bibi'.bibi]", (path != null ? lpath : null)]);
        rep?.add(["unexpected value for mandatory prop <bibi> [.'\$bibi']", (path != null ? lpath : null)]);
        return false;
    }
    return true;
}

// check $ (.)
bool json_model_1(dynamic val, List<dynamic>? path, List<dynamic>? rep)
{
    // .
    bool res = json_model_2(val, path, rep);
    if (! res)
    {
        rep?.add(["unexpected \$bibi [.]", path]);
    }
    return res;
}


// dart_init.dart
class GlobalState {
  static bool initializer = false;
}

Function? check_model_fun(String name) {
  return check_model_map[name];
}


void check_model_init() {
  if (!GlobalState.initializer){
    GlobalState.initializer = true;
  }
        check_model_map[""] = json_model_2;
        check_model_map["bibi"] = json_model_2;
}

// dart_free.dart
void check_model_free() {
  GlobalState.initializer = true;
}


void main(List<String> args) {
  runValidation(
    args,
    check_model_init,
    check_model_fun,
    check_model_free,
  );
}
