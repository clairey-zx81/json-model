//
// Generated by JSON Model Compiler version 2
// see https://github.com/clairey-zx81/json-model
//
import 'dart:convert';
import 'dart:io';
import 'dart:core';
// Import the helper library generated nearby
import 'package:json_model_runtime/json_model.dart' as runtime;
import 'package:json_model_runtime/main.dart';

//
// Generated JSON Model Validator
//
const String JSON_MODEL_VERSION = "2";

var check_model_map = <String, Function>{};

// check $ (.)
bool json_model_1(dynamic val, List<dynamic>? path, List<dynamic>? rep)
{
    // inline up to 3 must/may properties
    // .
    if (! (val is Map))
    {
        rep?.add(["not an object [.]", path]);
        return false;
    }
    bool res;
    int must_count = 0;
    for (var entry in val.entries)
    {
        var prop = entry.key;
        var pval = entry.value;
        List<dynamic>? lpath_0 = (path != null ? [...?path, prop] : null);
        if (prop.compareTo("mu1") == 0)
        {
            // handle must mu1 property
            must_count += 1;
            // .mu1
            res = pval == null;
            if (! res)
            {
                rep?.add(["not null [.mu1]", (path != null ? lpath_0 : null)]);
                rep?.add(["invalid mandatory prop value [.mu1]", (path != null ? lpath_0 : null)]);
                return false;
            }
            continue;
        }
        else if (prop.compareTo("mu2") == 0)
        {
            // handle must mu2 property
            must_count += 1;
            // .mu2
            res = (pval is bool);
            if (! res)
            {
                rep?.add(["not a bool [.mu2]", (path != null ? lpath_0 : null)]);
                rep?.add(["invalid mandatory prop value [.mu2]", (path != null ? lpath_0 : null)]);
                return false;
            }
            continue;
        }
        else if (prop.compareTo("mu3") == 0)
        {
            // handle must mu3 property
            must_count += 1;
            // .mu3
            res = (pval is int) && pval >= 0;
            if (! res)
            {
                rep?.add(["not a 0 strict int [.mu3]", (path != null ? lpath_0 : null)]);
                rep?.add(["invalid mandatory prop value [.mu3]", (path != null ? lpath_0 : null)]);
                return false;
            }
            continue;
        }
        if (prop.compareTo("ma1") == 0)
        {
            // handle may ma1 property
            // .ma1
            res = (pval is double) && pval >= 0.0;
            if (! res)
            {
                rep?.add(["not a 0.0 strict float [.ma1]", (path != null ? lpath_0 : null)]);
                rep?.add(["invalid optional prop value [.ma1]", (path != null ? lpath_0 : null)]);
                return false;
            }
            continue;
        }
        else if (prop.compareTo("ma2") == 0)
        {
            // handle may ma2 property
            // .ma2
            res = (pval is String);
            if (! res)
            {
                rep?.add(["unexpected string [.ma2]", (path != null ? lpath_0 : null)]);
                rep?.add(["invalid optional prop value [.ma2]", (path != null ? lpath_0 : null)]);
                return false;
            }
            continue;
        }
        else if (prop.compareTo("ma3") == 0)
        {
            // handle may ma3 property
            // .ma3
            res = runtime.jm_is_valid_date(pval, (path != null ? lpath_0 : null), rep);
            if (! res)
            {
                rep?.add(["unexpected \$DATE [.ma3]", (path != null ? lpath_0 : null)]);
                rep?.add(["invalid optional prop value [.ma3]", (path != null ? lpath_0 : null)]);
                return false;
            }
            continue;
        }
        rep?.add(["unexpected prop [.]", (path != null ? lpath_0 : null)]);
        return false;
    }
    if (must_count != 3)
    {
        if (rep != null)
        {
            if (! val.containsKey("mu1"))
            {
                rep?.add(["missing mandatory prop <mu1> [.]", path]);
            }
            if (! val.containsKey("mu2"))
            {
                rep?.add(["missing mandatory prop <mu2> [.]", path]);
            }
            if (! val.containsKey("mu3"))
            {
                rep?.add(["missing mandatory prop <mu3> [.]", path]);
            }
        }
        return false;
    }
    return true;
}


// dart_init.dart
class GlobalState {
  static bool initializer = false;
}

Function? check_model_fun(String name) {
  return check_model_map[name];
}


void check_model_init() {
  if (!GlobalState.initializer){
    GlobalState.initializer = true;
  }
        check_model_map[""] = json_model_1;
}

// dart_free.dart
void check_model_free() {
  GlobalState.initializer = true;
}


void main(List<String> args) {
  runValidation(
    args,
    check_model_init,
    check_model_fun,
    check_model_free,
  );
}
