#! /bin/env python
#
# Model: ./json-schema-draft-04-fuzzy.model.json
#
# Generated by jmc (JSON Model Compiler) (https://github.com/clairey-zx81/json-model)
#

from typing import Callable
import re as re
import datetime
import urllib.parse

type Jsonable = None|bool|int|float|str|list[Jsonable]|dict[str, Jsonable]
type Path = list[str]
type Report = list[str]|None
type CheckFun = Callable[[Jsonable, str, Report], bool]
type PropMap = dict[str, CheckFun]
type TagMap = dict[None|bool|float|int|str, CheckFun]

# extract type name
def _tname(value: Jsonable) -> str:
    return type(value).__name__

# maybe add message to report
def _rep(msg: str, rep: Report) -> bool:
    rep is None or rep.append(msg)
    return False

json_model_10_may: PropMap

def _is_valid_re(value: Jsonable, path: str, rep: Report = None) -> bool:
    if isinstance(value, str):
        try:
            re.compile(value)
            return True
        except Exception as e:
            rep is None or rep.append(f"regex compile error at {path}: {value} ({e})")
            return False
    rep is None or rep.append(f"incompatible type for regex at {path}: {_tname(value)}")
    return False

def _is_valid_url(value: Jsonable, path: str, rep: Report = None) -> bool:
    if isinstance(value, str):
        try:
            urllib.parse.urlparse(value)
            return True
        except Exception as e:
            rep is None or re.append(f"invalid url at {path}: {value} ({e})")
            return False
    rep is None or rep.append(f"incompatible type for url at {path}: {_tname(value)}")
    return False

# define "json_model_10_may_id" ($.'$schema'.id)
def _jm_f_0(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'$schema'.id
    result = isinstance(value, str)
    if not result:
        rep is None or rep.append(f"not an expected string at {path} [$.'$schema'.id]")
    return result

# define "json_model_10_may_$schema" ($.'$schema'.'$schema')
def _jm_f_1(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'$schema'.'$schema'
    result = isinstance(value, str)
    if not result:
        rep is None or rep.append(f"not an expected string at {path} [$.'$schema'.'$schema']")
    return result

# define "json_model_10_may_title" ($.'$schema'.title)
def _jm_f_2(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'$schema'.title
    result = isinstance(value, str)
    if not result:
        rep is None or rep.append(f"not an expected string at {path} [$.'$schema'.title]")
    return result

# define "json_model_10_may_description" ($.'$schema'.description)
def _jm_f_3(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'$schema'.description
    result = isinstance(value, str)
    if not result:
        rep is None or rep.append(f"not an expected string at {path} [$.'$schema'.description]")
    return result

# define "json_model_10_may_default" ($.'$schema'.default)
def _jm_f_4(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'$schema'.default
    result = True or _rep(f"invalid $ANY at {path}", rep)
    if not result:
        rep is None or rep.append(f"not an expected $ANY at {path} [$.'$schema'.default]")
    return result

# define "json_model_10_may_multipleOf" ($.'$schema'.multipleOf)
def _jm_f_5(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'$schema'.multipleOf
    result = isinstance(value, float) and value > 0.0
    if not result:
        rep is None or rep.append(f"not a 1.0 float at {path} [$.'$schema'.multipleOf]")
    return result

# define "json_model_10_may_maximum" ($.'$schema'.maximum)
def _jm_f_6(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'$schema'.maximum
    result = isinstance(value, float)
    if not result:
        rep is None or rep.append(f"not a -1.0 float at {path} [$.'$schema'.maximum]")
    return result

# define "json_model_10_may_exclusiveMaximum" ($.'$schema'.exclusiveMaximum)
def _jm_f_7(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'$schema'.exclusiveMaximum
    result = isinstance(value, bool)
    if not result:
        rep is None or rep.append(f"not a bool at {path} [$.'$schema'.exclusiveMaximum]")
    return result

# define "json_model_10_may_minimum" ($.'$schema'.minimum)
def _jm_f_8(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'$schema'.minimum
    result = isinstance(value, float)
    if not result:
        rep is None or rep.append(f"not a -1.0 float at {path} [$.'$schema'.minimum]")
    return result

# define "json_model_10_may_exclusiveMinimum" ($.'$schema'.exclusiveMinimum)
def _jm_f_9(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'$schema'.exclusiveMinimum
    result = isinstance(value, bool)
    if not result:
        rep is None or rep.append(f"not a bool at {path} [$.'$schema'.exclusiveMinimum]")
    return result

# define "json_model_10_may_maxLength" ($.'$schema'.maxLength)
def _jm_f_10(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'$schema'.maxLength
    result = isinstance(value, int) and not isinstance(value, bool) and value >= 0
    if not result:
        rep is None or rep.append(f"not a 0 int at {path} [$.'$schema'.maxLength]")
    return result

# define "json_model_10_may_minLength" ($.'$schema'.minLength)
def _jm_f_11(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'$schema'.minLength
    result = isinstance(value, int) and not isinstance(value, bool) and value >= 0
    if not result:
        rep is None or rep.append(f"not a 0 int at {path} [$.'$schema'.minLength]")
    return result

# define "json_model_10_may_pattern" ($.'$schema'.pattern)
def _jm_f_12(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'$schema'.pattern
    result = _is_valid_re(value, path, rep) or _rep(f"invalid $REGEX at {path}", rep)
    if not result:
        rep is None or rep.append(f"not an expected $REGEX at {path} [$.'$schema'.pattern]")
    return result

# define "json_model_10_may_additionalItems" ($.'$schema'.additionalItems)
def _jm_f_13(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'$schema'.additionalItems
    # $.'$schema'.additionalItems.'|'.0
    result = isinstance(value, bool)
    if not result:
        rep is None or rep.append(f"not a bool at {path} [$.'$schema'.additionalItems.'|'.0]")
    if not result:
        # $.'$schema'.additionalItems.'|'.1
        result = json_model_10(value, path, rep)
        if not result:
            rep is None or rep.append(f"not an expected $Schema at {path} [$.'$schema'.additionalItems.'|'.1]")
    if not result:
        rep is None or rep.append(f"not any model match at {path} [$.'$schema'.additionalItems.'|']")
    return result

# define "json_model_10_may_items" ($.'$schema'.items)
def _jm_f_14(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'$schema'.items
    # $.'$schema'.items.'|'.0
    result = json_model_10(value, path, rep)
    if not result:
        rep is None or rep.append(f"not an expected $Schema at {path} [$.'$schema'.items.'|'.0]")
    if not result:
        # $.'$schema'.items.'|'.1
        result = json_model_12(value, path, rep)
        if not result:
            rep is None or rep.append(f"not an expected $schemaArray at {path} [$.'$schema'.items.'|'.1]")
    if not result:
        rep is None or rep.append(f"not any model match at {path} [$.'$schema'.items.'|']")
    return result

# define "json_model_10_may_maxItems" ($.'$schema'.maxItems)
def _jm_f_15(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'$schema'.maxItems
    result = isinstance(value, int) and not isinstance(value, bool) and value >= 0
    if not result:
        rep is None or rep.append(f"not a 0 int at {path} [$.'$schema'.maxItems]")
    return result

# define "json_model_10_may_minItems" ($.'$schema'.minItems)
def _jm_f_16(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'$schema'.minItems
    result = isinstance(value, int) and not isinstance(value, bool) and value >= 0
    if not result:
        rep is None or rep.append(f"not a 0 int at {path} [$.'$schema'.minItems]")
    return result

# define "json_model_10_may_uniqueItems" ($.'$schema'.uniqueItems)
def _jm_f_17(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'$schema'.uniqueItems
    result = isinstance(value, bool)
    if not result:
        rep is None or rep.append(f"not a bool at {path} [$.'$schema'.uniqueItems]")
    return result

# define "json_model_10_may_maxProperties" ($.'$schema'.maxProperties)
def _jm_f_18(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'$schema'.maxProperties
    result = isinstance(value, int) and not isinstance(value, bool) and value >= 0
    if not result:
        rep is None or rep.append(f"not a 0 int at {path} [$.'$schema'.maxProperties]")
    return result

# define "json_model_10_may_minProperties" ($.'$schema'.minProperties)
def _jm_f_19(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'$schema'.minProperties
    result = isinstance(value, int) and not isinstance(value, bool) and value >= 0
    if not result:
        rep is None or rep.append(f"not a 0 int at {path} [$.'$schema'.minProperties]")
    return result

# define "json_model_10_may_required" ($.'$schema'.required)
def _jm_f_20(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'$schema'.required
    result = json_model_14(value, path, rep)
    if not result:
        rep is None or rep.append(f"not an expected $stringArray at {path} [$.'$schema'.required]")
    return result

# define "json_model_10_may_additionalProperties" ($.'$schema'.additionalProperties)
def _jm_f_21(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'$schema'.additionalProperties
    # $.'$schema'.additionalProperties.'|'.0
    result = isinstance(value, bool)
    if not result:
        rep is None or rep.append(f"not a bool at {path} [$.'$schema'.additionalProperties.'|'.0]")
    if not result:
        # $.'$schema'.additionalProperties.'|'.1
        result = json_model_10(value, path, rep)
        if not result:
            rep is None or rep.append(f"not an expected $Schema at {path} [$.'$schema'.additionalProperties.'|'.1]")
    if not result:
        rep is None or rep.append(f"not any model match at {path} [$.'$schema'.additionalProperties.'|']")
    return result









# define "json_model_10_may_enum" ($.'$schema'.enum)
def _jm_f_26(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'$schema'.enum
    # $.'$schema'.enum.'@'
    result = isinstance(value, list)
    if result:
        for array_0_idx, array_0_item in enumerate(value):
            lpath = path + '.' + str(array_0_idx)
            # $.'$schema'.enum.'@'.0
            result = True or _rep(f"invalid $ANY at {path}", rep)
            if not result:
                rep is None or rep.append(f"not an expected $ANY at {lpath} [$.'$schema'.enum.'@'.0]")
            if not result:
                break
    if not result:
        rep is None or rep.append(f"not array or unexpected array at {path} [$.'$schema'.enum.'@']")
    result &= len(set(value)) == len(value)
    result &= len(value) >= 1
    if not result:
        rep is None or rep.append(f"invalid type or constraints at {path} [$.'$schema'.enum]")
    return result

# define "json_model_10_may_type" ($.'$schema'.type)
def _jm_f_27(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'$schema'.type
    # $.'$schema'.type.'|'.0
    result = json_model_13(value, path, rep)
    if not result:
        rep is None or rep.append(f"not an expected $simpleTypes at {path} [$.'$schema'.type.'|'.0]")
    if not result:
        # $.'$schema'.type.'|'.1
        result = json_model_15(value, path, rep)
        if not result:
            rep is None or rep.append(f"not an expected $typeArray at {path} [$.'$schema'.type.'|'.1]")
    if not result:
        rep is None or rep.append(f"not any model match at {path} [$.'$schema'.type.'|']")
    return result

# define "json_model_10_may_format" ($.'$schema'.format)
def _jm_f_28(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'$schema'.format
    result = isinstance(value, str)
    if not result:
        rep is None or rep.append(f"not an expected string at {path} [$.'$schema'.format]")
    return result

# define "json_model_10_may_allOf" ($.'$schema'.allOf)
def _jm_f_29(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'$schema'.allOf
    result = json_model_12(value, path, rep)
    if not result:
        rep is None or rep.append(f"not an expected $schemaArray at {path} [$.'$schema'.allOf]")
    return result

# define "json_model_10_may_anyOf" ($.'$schema'.anyOf)
def _jm_f_30(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'$schema'.anyOf
    result = json_model_12(value, path, rep)
    if not result:
        rep is None or rep.append(f"not an expected $schemaArray at {path} [$.'$schema'.anyOf]")
    return result

# define "json_model_10_may_oneOf" ($.'$schema'.oneOf)
def _jm_f_31(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'$schema'.oneOf
    result = json_model_12(value, path, rep)
    if not result:
        rep is None or rep.append(f"not an expected $schemaArray at {path} [$.'$schema'.oneOf]")
    return result

# define "json_model_10_may_not" ($.'$schema'.not)
def _jm_f_32(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'$schema'.not
    result = json_model_10(value, path, rep)
    if not result:
        rep is None or rep.append(f"not an expected $Schema at {path} [$.'$schema'.not]")
    return result

# define "json_model_10_may_$ref" ($.'$schema'.'$ref')
def _jm_f_33(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'$schema'.'$ref'
    result = _is_valid_url(value, path, rep) or _rep(f"invalid $URL at {path}", rep)
    if not result:
        rep is None or rep.append(f"not an expected $URL at {path} [$.'$schema'.'$ref']")
    return result



# object $.'$schema'.definitions
def _jm_f_22(value: Jsonable, path: str, rep: Report = None) -> bool:
    if not isinstance(value, dict):
        rep is None or rep.append(f"not an object at {path} [$.'$schema'.definitions]")
        return False
    for prop, val in value.items():
        assert isinstance(prop, str)
        lpath = path + "." + prop
        # $.'$schema'.definitions.''
        result = json_model_10(val, path, rep)
        if not result:
            rep is None or rep.append(f"not an expected $Schema at {lpath} [$.'$schema'.definitions.'']")
        if not result:
            rep is None or rep.append(f"unexpected other value at {lpath} [$.'$schema'.definitions.'']")
            return False
    return True


# object $.'$schema'.properties
def _jm_f_23(value: Jsonable, path: str, rep: Report = None) -> bool:
    if not isinstance(value, dict):
        rep is None or rep.append(f"not an object at {path} [$.'$schema'.properties]")
        return False
    for prop, val in value.items():
        assert isinstance(prop, str)
        lpath = path + "." + prop
        # $.'$schema'.properties.''
        result = json_model_10(val, path, rep)
        if not result:
            rep is None or rep.append(f"not an expected $Schema at {lpath} [$.'$schema'.properties.'']")
        if not result:
            rep is None or rep.append(f"unexpected other value at {lpath} [$.'$schema'.properties.'']")
            return False
    return True


# object $.'$schema'.patternProperties
def _jm_f_24(value: Jsonable, path: str, rep: Report = None) -> bool:
    if not isinstance(value, dict):
        rep is None or rep.append(f"not an object at {path} [$.'$schema'.patternProperties]")
        return False
    for prop, val in value.items():
        assert isinstance(prop, str)
        lpath = path + "." + prop
        # $.'$schema'.patternProperties.''
        result = json_model_10(val, path, rep)
        if not result:
            rep is None or rep.append(f"not an expected $Schema at {lpath} [$.'$schema'.patternProperties.'']")
        if not result:
            rep is None or rep.append(f"unexpected other value at {lpath} [$.'$schema'.patternProperties.'']")
            return False
    return True


# object $.'$schema'.dependencies
def _jm_f_25(value: Jsonable, path: str, rep: Report = None) -> bool:
    if not isinstance(value, dict):
        rep is None or rep.append(f"not an object at {path} [$.'$schema'.dependencies]")
        return False
    for prop, val in value.items():
        assert isinstance(prop, str)
        lpath = path + "." + prop
        # $.'$schema'.dependencies.''
        # $.'$schema'.dependencies.''.'|'.0
        result = json_model_10(val, path, rep)
        if not result:
            rep is None or rep.append(f"not an expected $Schema at {lpath} [$.'$schema'.dependencies.''.'|'.0]")
        if not result:
            # $.'$schema'.dependencies.''.'|'.1
            result = json_model_14(val, path, rep)
            if not result:
                rep is None or rep.append(f"not an expected $stringArray at {lpath} [$.'$schema'.dependencies.''.'|'.1]")
        if not result:
            rep is None or rep.append(f"not any model match at {lpath} [$.'$schema'.dependencies.''.'|']")
        if not result:
            rep is None or rep.append(f"unexpected other value at {lpath} [$.'$schema'.dependencies.'']")
            return False
    return True


# object $.'$schema'
def json_model_10(value: Jsonable, path: str, rep: Report = None) -> bool:
    if not isinstance(value, dict):
        rep is None or rep.append(f"not an object at {path} [$.'$schema']")
        return False
    for prop, val in value.items():
        assert isinstance(prop, str)
        lpath = path + "." + prop
        if prop in json_model_10_may:  # may
            if not json_model_10_may[prop](val, lpath, rep):
                rep is None or rep.append(f"invalid may prop value at {lpath} [$.'$schema'.{prop}]")
                return False
        else:  # no catch all
            rep is None or rep.append(f"no other prop expected at {path} [$.'$schema']")
            return False
    return True



# define "$schema#schemaArray" ($.'$schema#schemaArray')
def json_model_12(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'$schema#schemaArray'
    # $.'$schema#schemaArray'.'@'
    result = isinstance(value, list)
    if result:
        for array_1_idx, array_1_item in enumerate(value):
            lpath = path + '.' + str(array_1_idx)
            # $.'$schema#schemaArray'.'@'.0
            result = json_model_10(array_1_item, path, rep)
            if not result:
                rep is None or rep.append(f"not an expected $Schema at {lpath} [$.'$schema#schemaArray'.'@'.0]")
            if not result:
                break
    if not result:
        rep is None or rep.append(f"not array or unexpected array at {path} [$.'$schema#schemaArray'.'@']")
    result &= len(value) >= 1
    if not result:
        rep is None or rep.append(f"invalid type or constraints at {path} [$.'$schema#schemaArray']")
    return result

# define "$schema#simpleTypes" ($.'$schema#simpleTypes')
def json_model_13(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'$schema#simpleTypes'
    result = not isinstance(value, (list, dict)) and value in {'array', 'boolean', 'integer', 'null', 'number', 'object', 'string'}
    if not result:
        rep is None or rep.append(f"value not in enum at {path} [$.'$schema#simpleTypes'.'|']")
    return result

# define "$schema#stringArray" ($.'$schema#stringArray')
def json_model_14(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'$schema#stringArray'
    # $.'$schema#stringArray'.'@'
    result = isinstance(value, list)
    if result:
        for array_2_idx, array_2_item in enumerate(value):
            lpath = path + '.' + str(array_2_idx)
            # $.'$schema#stringArray'.'@'.0
            result = isinstance(array_2_item, str)
            if not result:
                rep is None or rep.append(f"not an expected string at {lpath} [$.'$schema#stringArray'.'@'.0]")
            if not result:
                break
    if not result:
        rep is None or rep.append(f"not array or unexpected array at {path} [$.'$schema#stringArray'.'@']")
    result &= len(set(value)) == len(value)
    result &= len(value) >= 1
    if not result:
        rep is None or rep.append(f"invalid type or constraints at {path} [$.'$schema#stringArray']")
    return result

# define "$schema#typeArray" ($.'$schema#typeArray')
def json_model_15(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $.'$schema#typeArray'
    # $.'$schema#typeArray'.'@'
    result = isinstance(value, list)
    if result:
        for array_3_idx, array_3_item in enumerate(value):
            lpath = path + '.' + str(array_3_idx)
            # $.'$schema#typeArray'.'@'.0
            result = json_model_13(array_3_item, path, rep)
            if not result:
                rep is None or rep.append(f"not an expected $simpleTypes at {lpath} [$.'$schema#typeArray'.'@'.0]")
            if not result:
                break
    if not result:
        rep is None or rep.append(f"not array or unexpected array at {path} [$.'$schema#typeArray'.'@']")
    result &= len(set(value)) == len(value)
    result &= len(value) >= 1
    if not result:
        rep is None or rep.append(f"invalid type or constraints at {path} [$.'$schema#typeArray']")
    return result

# define "$" ($)
def json_model_1(value: Jsonable, path: str, rep: Report = None) -> bool:
    # $
    result = json_model_10(value, path, rep)
    if not result:
        rep is None or rep.append(f"not an expected $schema at {path} [$]")
    return result

# entry function check_model
def check_model(value: Jsonable, path: str = "$", rep: Report = None) -> bool:
    return json_model_1(value, path, rep)


# object properties maps
json_model_10_may = {
    "id": _jm_f_0,
    "$schema": _jm_f_1,
    "title": _jm_f_2,
    "description": _jm_f_3,
    "default": _jm_f_4,
    "multipleOf": _jm_f_5,
    "maximum": _jm_f_6,
    "exclusiveMaximum": _jm_f_7,
    "minimum": _jm_f_8,
    "exclusiveMinimum": _jm_f_9,
    "maxLength": _jm_f_10,
    "minLength": _jm_f_11,
    "pattern": _jm_f_12,
    "additionalItems": _jm_f_13,
    "items": _jm_f_14,
    "maxItems": _jm_f_15,
    "minItems": _jm_f_16,
    "uniqueItems": _jm_f_17,
    "maxProperties": _jm_f_18,
    "minProperties": _jm_f_19,
    "required": _jm_f_20,
    "additionalProperties": _jm_f_21,
    "definitions": _jm_f_22,
    "properties": _jm_f_23,
    "patternProperties": _jm_f_24,
    "dependencies": _jm_f_25,
    "enum": _jm_f_26,
    "type": _jm_f_27,
    "format": _jm_f_28,
    "allOf": _jm_f_29,
    "anyOf": _jm_f_30,
    "oneOf": _jm_f_31,
    "not": _jm_f_32,
    "$ref": _jm_f_33,
}


# possibly run as a script: $0 values...
if __name__ == "__main__":
    import json
    import sys
    for fn in sys.argv[1:]:
        try:
            with open(fn) as f:
                value = json.load(f)
            reasons = []
            if check_model(value, "", reasons):
                print(f"{fn}: PASS")
            else:
                print(f"{fn}: FAIL {reasons}")
        except Exception as e:
            print(f"{fn}: ERROR ({e})")

