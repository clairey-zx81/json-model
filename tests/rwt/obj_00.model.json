{
  "#": "torteous definition of a simple boolean",
  "$": {
    "#": "initially define foo as a string",
    "foo": "$STRING"
  },
  "%": {
    "#": "rename .target as @, rewrite foo definition to bool",
    ".target": "@",
    "$foo": "$BOOLEAN"
  },
  ".target": "$foo"
}
