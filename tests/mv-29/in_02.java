//
// Generated by JSON Model Compiler version 2
// see https://github.com/clairey-zx81/json-model
//

import json_model.*;
import java.util.Map;
import java.util.Set;
import java.util.HashMap;
import java.util.HashSet;
import java.util.Iterator;
import java.util.regex.Pattern;
import java.util.regex.Matcher;

@SuppressWarnings("unchecked")
public class in_02 extends ModelChecker
{
    static public final String VERSION = "2";

    public Map<String, Checker> in_02_map_pmap;

    // check $ (.)
    public boolean json_model_1(Object val, Path path, Report rep)
    {
        // one 1 in the array
        // .
        // .'@'
        boolean res = json.isArray(val);
        if (! res)
        {
            if (rep != null) rep.addEntry("not array or unexpected array [.'@']", path);
        }
        // .in len at .
        long arr_0_inlen = 0;
        if (res)
        {
            int arr_0_idx = -1;
            Iterator<Object> arr_0_item_loop = json.arrayIterator(val);
            while (arr_0_item_loop.hasNext())
            {
                arr_0_idx++;
                Object arr_0_item = arr_0_item_loop.next();
                Path arr_0_lpath = new Path(arr_0_idx, path);
                // .'.in'
                boolean arr_0_inres = json.isInteger(arr_0_item) && json.asLong(arr_0_item) == 1;
                if (arr_0_inres)
                {
                    arr_0_inlen += 1;
                }
                else
                {
                    if (rep != null) rep.addEntry("unexpected value for model \"=1\" [.'.in']", (path != null ? arr_0_lpath : null));
                }
            }
            res = arr_0_inlen == 1;
            if (! res)
            {
                if (rep != null) rep.addEntry("constraints failed [.]", path);
            }
        }
        return res;
    }


    public void init(JSON json)
    {
        if (!initialized)
        {
            try {
            in_02_map_pmap = new HashMap<String, Checker>();
            in_02_map_pmap.put("", new Checker() { public boolean call(Object o, Path p, Report r) { return json_model_1(o, p, r);} });
                super.init(json);
            }
            catch (Exception e) {
                throw new Error("cannot initialized model checker: " + e);
            }
        }
    }

    public void free()
    {
        if (initialized)
        {
            super.free();
            in_02_map_pmap = null;
        }
    }

    public Checker get(String name)
    {
        return in_02_map_pmap.get(name);
    }

    public Set<String> models()
    {
        return in_02_map_pmap.keySet();
    }

    static public void main(String[] args) throws Exception
    {
        ModelChecker checker = new in_02();
        Main.main("in_02", checker, VERSION, args);
    }
}
