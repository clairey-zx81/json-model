//
// Generated by JSON Model Compiler version 2
// see https://github.com/clairey-zx81/json-model
//

import json_model.*;
import java.util.Map;
import java.util.Set;
import java.util.HashMap;
import java.util.HashSet;
import java.util.Iterator;
import java.util.regex.Pattern;
import java.util.regex.Matcher;

@SuppressWarnings("unchecked")
public class typed_and_00 extends ModelChecker
{
    static public final String VERSION = "2";

    public Map<String, Checker> typed_and_00_map_pmap;

    // check $foo (.'$foo')
    public boolean json_model_2(Object val, Path path, Report rep)
    {
        // .'$foo'
        // check close must only props
        if (! json.isObject(val))
        {
            if (rep != null) rep.addEntry("not an object [.'$foo']", path);
            return false;
        }
        if (json.objectSize(val) != 1)
        {
            if (rep != null) rep.addEntry("bad property count [.'$foo']", path);
            return false;
        }
        Path lpath;
        Object pval;
        boolean res;
        if (! ((pval = json.objectValue(val, "bla")) != null))
        {
            if (rep != null) rep.addEntry("missing mandatory prop <bla> [.'$foo']", path);
            return false;
        }
        lpath = new Path("bla", path);
        // .'$foo'.bla
        res = json.isInteger(pval) && json.asLong(pval) >= 1;
        if (! res)
        {
            if (rep != null) rep.addEntry("not a 1 strict int [.'$foo'.bla]", (path != null ? lpath : null));
            if (rep != null) rep.addEntry("unexpected value for mandatory prop <bla> [.'$foo']", (path != null ? lpath : null));
            return false;
        }
        return true;
    }

    // check $ (.)
    public boolean json_model_1(Object val, Path path, Report rep)
    {
        // .
        boolean res = json_model_2(val, path, rep);
        if (! res)
        {
            if (rep != null) rep.addEntry("unexpected value for model \"$foo\" [.]", path);
        }
        return res;
    }


    public void init(JSON json)
    {
        if (!initialized)
        {
            try {
            typed_and_00_map_pmap = new HashMap<String, Checker>();
            typed_and_00_map_pmap.put("", new Checker() { public boolean call(Object o, Path p, Report r) { return json_model_2(o, p, r);} });
            typed_and_00_map_pmap.put("foo", new Checker() { public boolean call(Object o, Path p, Report r) { return json_model_2(o, p, r);} });
                super.init(json);
            }
            catch (Exception e) {
                throw new Error("cannot initialized model checker: " + e);
            }
        }
    }

    public void free()
    {
        if (initialized)
        {
            super.free();
            typed_and_00_map_pmap = null;
        }
    }

    public Checker get(String name)
    {
        return typed_and_00_map_pmap.get(name);
    }

    public Set<String> models()
    {
        return typed_and_00_map_pmap.keySet();
    }

    static public void main(String[] args) throws Exception
    {
        ModelChecker checker = new typed_and_00();
        Main.main("typed_and_00", checker, VERSION, args);
    }
}
