//
// Generated by JSON Model Compiler version 2
// see https://github.com/clairey-zx81/json-model
//

// regular expression engine
#include <stddef.h>
#include <cre2.h>

#include <json-model.h>
#define JSON_MODEL_VERSION "2"

static bool json_model_1(const json_t *val, jm_path_t *path, jm_report_t *rep);
jm_propmap_t check_model_map_tab[1];
const size_t check_model_map_size = 1;

// check $ (.)
static bool json_model_1(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    // .
    if (unlikely(! json_is_object(val)))
    {
        if (rep) jm_report_add_entry(rep, "not an object [.]", path);
        return false;
    }
    bool res;
    const char *prop;
    json_t *pval;
    json_object_foreach((json_t *) val, prop, pval)
    {
        jm_path_t lpath_0 = (jm_path_t) { prop, 0, path, NULL };
        if (unlikely(jm_str_eq_2(prop, 0x00000062)))
        {
            // handle may b property
            // .b
            res = json_is_boolean(pval);
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "not a bool [.b]", (path ? &lpath_0 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid optional prop value [.b]", (path ? &lpath_0 : NULL));
                return false;
            }
            continue;
        }
        else if (unlikely(jm_str_eq_2(prop, 0x00000069)))
        {
            // handle may i property
            // .i
            res = json_is_integer(pval) && json_integer_value(pval) >= 1;
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "not a 1 strict int [.i]", (path ? &lpath_0 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid optional prop value [.i]", (path ? &lpath_0 : NULL));
                return false;
            }
            continue;
        }
        else if (unlikely(jm_str_eq_2(prop, 0x00000073)))
        {
            // handle may s property
            // .s
            res = json_is_string(pval);
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "unexpected value for model \"\" [.s]", (path ? &lpath_0 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid optional prop value [.s]", (path ? &lpath_0 : NULL));
                return false;
            }
            continue;
        }
        if (unlikely(jm_str_eq_1(prop, 0x64)))
        {
            // handle 2 re props
            // .'/^d/'
            res = json_is_string(pval);
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "unexpected value for model \"\" [.'/^d/']", (path ? &lpath_0 : NULL));
                return false;
            }
        }
        else if (unlikely(jm_str_eq_1(prop, 0x65)))
        {
            // handle 2 re props
            // .'/^e/'
            res = json_is_integer(pval) && json_integer_value(pval) >= 1;
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "not a 1 strict int [.'/^e/']", (path ? &lpath_0 : NULL));
                return false;
            }
        }
        else
        {
            // accept any other props
        }
    }
    return true;
}

jm_check_fun_t check_model_map(const char *pname)
{
    return jm_search_propmap(pname, check_model_map_tab, 1);
}

static bool initialized = false;

const char *check_model_init(void)
{
    if (!initialized)
    {
        initialized = true;
        jm_version_string = JSON_MODEL_VERSION;
        check_model_map_tab[0] = (jm_propmap_t) { "", json_model_1 };
        jm_sort_propmap(check_model_map_tab, 1);
    }
    return NULL;
}

void check_model_free(void)
{
    if (initialized)
        initialized = false;

        // cleanup code
}

/*
 * # API
 *
 * ```c
 * bool error;
 * char *message;
 *
 * bool valid = check(json_value, "model-name", &error, &message);
 *
 * if (message)
 *    free(message), message = NULL;
 * ```
 *
 * - `error` is set of there was an error during initialization or if the model does not exists.
 * - `message` is provided if a non NULL pointer is passed, and must be freed by the caller.
 */
bool
check_model(const json_t *val, const char *name, bool *error, char **reasons)
{
    return jm_generic_entry(check_model_init, check_model_map, val, name, error, reasons);
}
