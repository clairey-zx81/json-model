//
// Generated by JSON Model Compiler version 2
// see https://github.com/clairey-zx81/json-model
//

import json_model.*;
import java.util.Map;
import java.util.Set;
import java.util.HashMap;
import java.util.HashSet;
import java.util.Iterator;
import java.util.regex.Pattern;
import java.util.regex.Matcher;

@SuppressWarnings("unchecked")
public class v105 extends ModelChecker
{
    static public final String VERSION = "2";

    public Map<String, Checker> v105_map_pmap;

    // check $ (.)
    public boolean json_model_1(Object val, Path path, Report rep)
    {
        // .
        boolean res = json.isInteger(val);
        if (res)
        {
            // .'&'.0
            // .'&'.0.'@'
            res = json.isInteger(val) && json.asLong(val) >= 1;
            if (res)
            {
                long ival_2 = json.asLong(val);
                res = (ival_2 % 7) == 0;
                if (! res)
                {
                    if (rep != null) rep.addEntry("constraints failed [.'&'.0]", path);
                }
            }
            else
            {
                if (rep != null) rep.addEntry("not a 1 strict int [.'&'.0.'@']", path);
            }
            if (res)
            {
                // .'&'.1
                // .'&'.1.'@'
                res = json.isInteger(val) && json.asLong(val) >= 1;
                if (res)
                {
                    long ival_1 = json.asLong(val);
                    res = (ival_1 % 3) == 0;
                    if (! res)
                    {
                        if (rep != null) rep.addEntry("constraints failed [.'&'.1]", path);
                    }
                }
                else
                {
                    if (rep != null) rep.addEntry("not a 1 strict int [.'&'.1.'@']", path);
                }
                if (res)
                {
                    // .'&'.2
                    // .'&'.2.'@'
                    res = json.isInteger(val) && json.asLong(val) >= 1;
                    if (res)
                    {
                        long ival_0 = json.asLong(val);
                        res = (ival_0 % 5) == 0;
                        if (! res)
                        {
                            if (rep != null) rep.addEntry("constraints failed [.'&'.2]", path);
                        }
                    }
                    else
                    {
                        if (rep != null) rep.addEntry("not a 1 strict int [.'&'.2.'@']", path);
                    }
                }
            }
        }
        if (res)
        {
            if (rep != null) rep.clearEntries();
        }
        else
        {
            if (rep != null) rep.addEntry("not all model match [.'&']", path);
        }
        return res;
    }


    public void init(JSON json)
    {
        if (!initialized)
        {
            try {
            v105_map_pmap = new HashMap<String, Checker>();
            v105_map_pmap.put("", new Checker() { public boolean call(Object o, Path p, Report r) { return json_model_1(o, p, r);} });
                super.init(json);
            }
            catch (Exception e) {
                throw new Error("cannot initialized model checker: " + e);
            }
        }
    }

    public void free()
    {
        if (initialized)
        {
            super.free();
            v105_map_pmap = null;
        }
    }

    public Checker get(String name)
    {
        return v105_map_pmap.get(name);
    }

    public Set<String> models()
    {
        return v105_map_pmap.keySet();
    }

    static public void main(String[] args) throws Exception
    {
        ModelChecker checker = new v105();
        Main.main("v105", checker, VERSION, args);
    }
}
