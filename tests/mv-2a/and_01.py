#! /bin/env python
#
# Generated by JSON Model Compiler version 2
# see https://github.com/clairey-zx81/json-model
#
import math
from typing import Callable
import re2 as re
from json_model.runtime import *
__version__ = "2"

def check_model_fun(name: str) -> CheckFun:
    """Return check function for JSON model name."""
    return check_model_map[name]

# entry point for generated checkers
def check_model(val: Jsonable, name: str = "", rep: Report = None) -> bool:
    """Check val validity against JSON Model name."""
    if name not in check_model_map:
        raise Exception(f"unexpected model name: {name}")
    checker = check_model_map[name]
    return checker(val, [], rep)

check_model_map: PropMap

# object .'&'.1
def _jm_obj_0(val: Jsonable, path: Path, rep: Report) -> bool:
    # value known to be an object
    res: bool
    for prop, pval in val.items():
        lpath_0: Path = (path + [ prop ]) if path is not None else None
        if prop.startswith("s"):
            # handle 2 re props
            # .'&'.1.'/^s/'
            res = isinstance(pval, bool)
            if not res:
                rep is None or rep.append(("not a bool [.'&'.1.'/^s/']", lpath_0 if path is not None else None))
                return False
        elif prop.startswith("i"):
            # handle 2 re props
            # .'&'.1.'/^i/'
            res = isinstance(pval, int) and not isinstance(pval, bool) and pval >= 1
            if not res:
                rep is None or rep.append(("not a 1 strict int [.'&'.1.'/^i/']", lpath_0 if path is not None else None))
                return False
        else:
            # accept any other props
            pass
    return True

# object .'&'.0
def _jm_obj_1(val: Jsonable, path: Path, rep: Report) -> bool:
    # check open must/may only props
    # value known to be an object
    lpath: Path
    pval: Jsonable
    res: bool
    if not ((pval := val.get("i", UNDEFINED)) != UNDEFINED):
        rep is None or rep.append(("missing mandatory prop <i> [.'&'.0]", path))
        return False
    lpath = (path + [ "i" ]) if path is not None else None
    # .'&'.0.i
    res = isinstance(pval, int) and not isinstance(pval, bool) and pval >= 1
    if not res:
        rep is None or rep.append(("not a 1 strict int [.'&'.0.i]", lpath if path is not None else None))
        rep is None or rep.append(("unexpected value for mandatory prop <i> [.'&'.0]", lpath if path is not None else None))
        return False
    if not ((pval := val.get("b", UNDEFINED)) != UNDEFINED):
        rep is None or rep.append(("missing mandatory prop <b> [.'&'.0]", path))
        return False
    lpath = (path + [ "b" ]) if path is not None else None
    # .'&'.0.b
    res = isinstance(pval, bool)
    if not res:
        rep is None or rep.append(("not a bool [.'&'.0.b]", lpath if path is not None else None))
        rep is None or rep.append(("unexpected value for mandatory prop <b> [.'&'.0]", lpath if path is not None else None))
        return False
    if (pval := val.get("ss", UNDEFINED)) != UNDEFINED:
        lpath = (path + [ "ss" ]) if path is not None else None
        # .'&'.0.ss
        res = isinstance(pval, str)
        if not res:
            rep is None or rep.append(("unexpected value for model \"\" [.'&'.0.ss]", lpath if path is not None else None))
            rep is None or rep.append(("unexpected value for optional prop <ss> [.'&'.0]", lpath if path is not None else None))
            return False
    return True

# check $ (.)
def json_model_1(val: Jsonable, path: Path, rep: Report) -> bool:
    # merge object stuff if possible, cannot have ss
    # .
    res: bool = isinstance(val, dict)
    if res:
        # .'&'.0
        res = _jm_obj_1(val, path, rep)
        if res:
            # .'&'.1
            res = _jm_obj_0(val, path, rep)
            if not res:
                rep is None or rep.append(("unexpected element [.'&'.1]", path))
        else:
            rep is None or rep.append(("unexpected element [.'&'.0]", path))
    if res:
        rep is None or rep.clear()
    else:
        rep is None or rep.append(("not all model match [.'&']", path))
    return res


# initialization guard
initialized: bool = False

# differed module initializations
def check_model_init():
    global initialized
    if not initialized:
        initialized = True
        global check_model_map
        check_model_map = {
            "": json_model_1,
        }

# differed module cleanup
def check_model_free():
    global initialized
    if initialized:
        initialized = False

if __name__ == "__main__":
    check_model_init()
    main(check_model_fun, check_model_map, __version__)
    check_model_free()
