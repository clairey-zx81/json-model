//
// Generated by JSON Model Compiler version 2
// see https://github.com/clairey-zx81/json-model
//

import json_model.*;
import java.util.Map;
import java.util.Set;
import java.util.HashMap;
import java.util.HashSet;
import java.util.Iterator;
import java.util.regex.Pattern;
import java.util.regex.Matcher;

@SuppressWarnings("unchecked")
public class and_03 extends ModelChecker
{
    static public final String VERSION = "2";

    public Map<String, Checker> and_03_map_pmap;

    // check $ (.)
    public boolean json_model_1(Object val, Path path, Report rep)
    {
        // .
        if (! json.isObject(val))
        {
            if (rep != null) rep.addEntry("not an object [.]", path);
            return false;
        }
        boolean res;
        long must_count = 0;
        Iterator<String> prop_loop = json.objectIterator(val);
        while (prop_loop.hasNext())
        {
            String prop = prop_loop.next();
            Object pval = json.objectValue(val, prop);
            Path lpath_0 = new Path(prop, path);
            if (prop.compareTo("s") == 0)
            {
                // handle must s property
                must_count += 1;
                // .s
                res = json.isString(pval);
                if (! res)
                {
                    if (rep != null) rep.addEntry("unexpected value for model \"\" [.s]", (path != null ? lpath_0 : null));
                    if (rep != null) rep.addEntry("invalid mandatory prop value [.s]", (path != null ? lpath_0 : null));
                    return false;
                }
                continue;
            }
            if (prop.compareTo("b") == 0)
            {
                // handle may b property
                // .b
                res = json.isBoolean(pval);
                if (! res)
                {
                    if (rep != null) rep.addEntry("not a bool [.b]", (path != null ? lpath_0 : null));
                    if (rep != null) rep.addEntry("invalid optional prop value [.b]", (path != null ? lpath_0 : null));
                    return false;
                }
                continue;
            }
            else if (prop.compareTo("f") == 0)
            {
                // handle may f property
                // .f
                res = json.isDouble(pval) && json.asDouble(pval) > 0.0;
                if (! res)
                {
                    if (rep != null) rep.addEntry("not a 1.0 strict float [.f]", (path != null ? lpath_0 : null));
                    if (rep != null) rep.addEntry("invalid optional prop value [.f]", (path != null ? lpath_0 : null));
                    return false;
                }
                continue;
            }
            else if (prop.compareTo("i") == 0)
            {
                // handle may i property
                // .i
                res = json.isInteger(pval) && json.asLong(pval) >= 1;
                if (! res)
                {
                    if (rep != null) rep.addEntry("not a 1 strict int [.i]", (path != null ? lpath_0 : null));
                    if (rep != null) rep.addEntry("invalid optional prop value [.i]", (path != null ? lpath_0 : null));
                    return false;
                }
                continue;
            }
            if (prop.startsWith("xs"))
            {
                // handle 4 re props
                // .'/^xs/'
                res = json.isString(pval);
                if (! res)
                {
                    if (rep != null) rep.addEntry("unexpected value for model \"\" [.'/^xs/']", (path != null ? lpath_0 : null));
                    return false;
                }
            }
            else if (prop.startsWith("xb"))
            {
                // handle 4 re props
                // .'/^xb/'
                res = json.isBoolean(pval);
                if (! res)
                {
                    if (rep != null) rep.addEntry("not a bool [.'/^xb/']", (path != null ? lpath_0 : null));
                    return false;
                }
            }
            else if (prop.startsWith("xi"))
            {
                // handle 4 re props
                // .'/^xi/'
                res = json.isInteger(pval) && json.asLong(pval) >= 1;
                if (! res)
                {
                    if (rep != null) rep.addEntry("not a 1 strict int [.'/^xi/']", (path != null ? lpath_0 : null));
                    return false;
                }
            }
            else if (prop.startsWith("xf"))
            {
                // handle 4 re props
                // .'/^xf/'
                res = json.isDouble(pval) && json.asDouble(pval) > 0.0;
                if (! res)
                {
                    if (rep != null) rep.addEntry("not a 1.0 strict float [.'/^xf/']", (path != null ? lpath_0 : null));
                    return false;
                }
            }
            else
            {
                // accept any other props
            }
        }
        if (must_count != 1)
        {
            if (rep != null)
            {
                if (! json.objectHasProp(val, "s"))
                {
                    if (rep != null) rep.addEntry("missing mandatory prop <s> [.'']", path);
                }
            }
            return false;
        }
        return true;
    }


    public void init(JSON json)
    {
        if (!initialized)
        {
            try {
            and_03_map_pmap = new HashMap<String, Checker>();
            and_03_map_pmap.put("", new Checker() { public boolean call(Object o, Path p, Report r) { return json_model_1(o, p, r);} });
                super.init(json);
            }
            catch (Exception e) {
                throw new Error("cannot initialized model checker: " + e);
            }
        }
    }

    public void free()
    {
        if (initialized)
        {
            super.free();
            and_03_map_pmap = null;
        }
    }

    public Checker get(String name)
    {
        return and_03_map_pmap.get(name);
    }

    public Set<String> models()
    {
        return and_03_map_pmap.keySet();
    }

    static public void main(String[] args) throws Exception
    {
        ModelChecker checker = new and_03();
        Main.main("and_03", checker, VERSION, args);
    }
}
