//
// Generated by JSON Model Compiler version 2
// see https://github.com/clairey-zx81/json-model
//

import json_model.*;
import java.util.Map;
import java.util.Set;
import java.util.HashMap;
import java.util.HashSet;
import java.util.Iterator;
import java.util.regex.Pattern;
import java.util.regex.Matcher;

@SuppressWarnings("unchecked")
public class and_01 extends ModelChecker
{
    static public final String VERSION = "2";

    public Map<String, Checker> and_01_map_pmap;

    // object .'&'.1
    public boolean _jm_obj_0(Object val, Path path, Report rep)
    {
        // value known to be an object
        boolean res;
        Iterator<String> prop_loop = json.objectIterator(val);
        while (prop_loop.hasNext())
        {
            String prop = prop_loop.next();
            Object pval = json.objectValue(val, prop);
            Path lpath_0 = new Path(prop, path);
            if (prop.startsWith("s"))
            {
                // handle 2 re props
                // .'&'.1.'/^s/'
                res = json.isBoolean(pval);
                if (! res)
                {
                    if (rep != null) rep.addEntry("not a bool [.'&'.1.'/^s/']", (path != null ? lpath_0 : null));
                    return false;
                }
            }
            else if (prop.startsWith("i"))
            {
                // handle 2 re props
                // .'&'.1.'/^i/'
                res = json.isInteger(pval) && json.asLong(pval) >= 1;
                if (! res)
                {
                    if (rep != null) rep.addEntry("not a 1 strict int [.'&'.1.'/^i/']", (path != null ? lpath_0 : null));
                    return false;
                }
            }
            else
            {
                // accept any other props
            }
        }
        return true;
    }

    // object .'&'.0
    public boolean _jm_obj_1(Object val, Path path, Report rep)
    {
        // check open must/may only props
        // value known to be an object
        Path lpath;
        Object pval;
        if (! ((pval = json.objectValue(val, "i")) != null))
        {
            if (rep != null) rep.addEntry("missing mandatory prop <i> [.'&'.0]", path);
            return false;
        }
        lpath = new Path("i", path);
        // .'&'.0.i
        boolean res = json.isInteger(pval) && json.asLong(pval) >= 1;
        if (! res)
        {
            if (rep != null) rep.addEntry("not a 1 strict int [.'&'.0.i]", (path != null ? lpath : null));
            if (rep != null) rep.addEntry("unexpected value for mandatory prop <i> [.'&'.0]", (path != null ? lpath : null));
            return false;
        }
        if (! ((pval = json.objectValue(val, "b")) != null))
        {
            if (rep != null) rep.addEntry("missing mandatory prop <b> [.'&'.0]", path);
            return false;
        }
        lpath = new Path("b", path);
        // .'&'.0.b
        res = json.isBoolean(pval);
        if (! res)
        {
            if (rep != null) rep.addEntry("not a bool [.'&'.0.b]", (path != null ? lpath : null));
            if (rep != null) rep.addEntry("unexpected value for mandatory prop <b> [.'&'.0]", (path != null ? lpath : null));
            return false;
        }
        if ((pval = json.objectValue(val, "ss")) != null)
        {
            lpath = new Path("ss", path);
            // .'&'.0.ss
            res = json.isString(pval);
            if (! res)
            {
                if (rep != null) rep.addEntry("unexpected value for model \"\" [.'&'.0.ss]", (path != null ? lpath : null));
                if (rep != null) rep.addEntry("unexpected value for optional prop <ss> [.'&'.0]", (path != null ? lpath : null));
                return false;
            }
        }
        return true;
    }

    // check $ (.)
    public boolean json_model_1(Object val, Path path, Report rep)
    {
        // merge object stuff if possible, cannot have ss
        // .
        boolean res = json.isObject(val);
        if (res)
        {
            // .'&'.0
            res = _jm_obj_1(val, path, rep);
            if (res)
            {
                // .'&'.1
                res = _jm_obj_0(val, path, rep);
                if (! res)
                {
                    if (rep != null) rep.addEntry("unexpected element [.'&'.1]", path);
                }
            }
            else
            {
                if (rep != null) rep.addEntry("unexpected element [.'&'.0]", path);
            }
        }
        if (res)
        {
            if (rep != null) rep.clearEntries();
        }
        else
        {
            if (rep != null) rep.addEntry("not all model match [.'&']", path);
        }
        return res;
    }


    public void init(JSON json)
    {
        if (!initialized)
        {
            try {
            and_01_map_pmap = new HashMap<String, Checker>();
            and_01_map_pmap.put("", new Checker() { public boolean call(Object o, Path p, Report r) { return json_model_1(o, p, r);} });
                super.init(json);
            }
            catch (Exception e) {
                throw new Error("cannot initialized model checker: " + e);
            }
        }
    }

    public void free()
    {
        if (initialized)
        {
            super.free();
            and_01_map_pmap = null;
        }
    }

    public Checker get(String name)
    {
        return and_01_map_pmap.get(name);
    }

    public Set<String> models()
    {
        return and_01_map_pmap.keySet();
    }

    static public void main(String[] args) throws Exception
    {
        ModelChecker checker = new and_01();
        Main.main("and_01", checker, VERSION, args);
    }
}
