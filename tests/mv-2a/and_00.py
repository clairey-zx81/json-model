#! /bin/env python
#
# Generated by JSON Model Compiler version 2
# see https://github.com/clairey-zx81/json-model
#
import math
from typing import Callable
import re2 as re
from json_model.runtime import *
__version__ = "2"

def check_model_fun(name: str) -> CheckFun:
    """Return check function for JSON model name."""
    return check_model_map[name]

# entry point for generated checkers
def check_model(val: Jsonable, name: str = "", rep: Report = None) -> bool:
    """Check val validity against JSON Model name."""
    if name not in check_model_map:
        raise Exception(f"unexpected model name: {name}")
    checker = check_model_map[name]
    return checker(val, [], rep)

check_model_map: PropMap

# check $ (.)
def json_model_1(val: Jsonable, path: Path, rep: Report) -> bool:
    # .
    if not isinstance(val, dict):
        rep is None or rep.append(("not an object [.]", path))
        return False
    res: bool
    for prop, pval in val.items():
        lpath_0: Path = (path + [ prop ]) if path is not None else None
        if prop == "b":
            # handle may b property
            # .b
            res = isinstance(pval, bool)
            if not res:
                rep is None or rep.append(("not a bool [.b]", lpath_0 if path is not None else None))
                rep is None or rep.append(("invalid optional prop value [.b]", lpath_0 if path is not None else None))
                return False
            continue
        elif prop == "i":
            # handle may i property
            # .i
            res = isinstance(pval, int) and not isinstance(pval, bool) and pval >= 1
            if not res:
                rep is None or rep.append(("not a 1 strict int [.i]", lpath_0 if path is not None else None))
                rep is None or rep.append(("invalid optional prop value [.i]", lpath_0 if path is not None else None))
                return False
            continue
        elif prop == "s":
            # handle may s property
            # .s
            res = isinstance(pval, str)
            if not res:
                rep is None or rep.append(("unexpected value for model \"\" [.s]", lpath_0 if path is not None else None))
                rep is None or rep.append(("invalid optional prop value [.s]", lpath_0 if path is not None else None))
                return False
            continue
        if prop.startswith("d"):
            # handle 2 re props
            # .'/^d/'
            res = isinstance(pval, str)
            if not res:
                rep is None or rep.append(("unexpected value for model \"\" [.'/^d/']", lpath_0 if path is not None else None))
                return False
        elif prop.startswith("e"):
            # handle 2 re props
            # .'/^e/'
            res = isinstance(pval, int) and not isinstance(pval, bool) and pval >= 1
            if not res:
                rep is None or rep.append(("not a 1 strict int [.'/^e/']", lpath_0 if path is not None else None))
                return False
        else:
            # accept any other props
            pass
    return True


# initialization guard
initialized: bool = False

# differed module initializations
def check_model_init():
    global initialized
    if not initialized:
        initialized = True
        global check_model_map
        check_model_map = {
            "": json_model_1,
        }

# differed module cleanup
def check_model_free():
    global initialized
    if initialized:
        initialized = False

if __name__ == "__main__":
    check_model_init()
    main(check_model_fun, check_model_map, __version__)
    check_model_free()
