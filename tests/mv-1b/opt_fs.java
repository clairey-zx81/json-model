//
// Generated by JSON Model Compiler version 2
// see https://github.com/clairey-zx81/json-model
//

import json_model.*;
import java.util.Map;
import java.util.Set;
import java.util.HashMap;
import java.util.HashSet;
import java.util.Iterator;
import java.util.regex.Pattern;
import java.util.regex.Matcher;

@SuppressWarnings("unchecked")
public class opt_fs extends ModelChecker
{
    static public final String VERSION = "2";

    public Map<String, Checker> opt_fs_map_pmap;

    // check $ (.)
    public boolean json_model_1(Object val, Path path, Report rep)
    {
        // JSON_MODEL_STRICT_FLOAT
        // .
        // .'@'
        boolean res = json.isDouble(val);
        if (! res)
        {
            if (rep != null) rep.addEntry("not a -1.0 strict float [.'@']", path);
        }
        return res;
    }


    public void init(JSON json)
    {
        if (!initialized)
        {
            try {
            opt_fs_map_pmap = new HashMap<String, Checker>();
            opt_fs_map_pmap.put("", new Checker() { public boolean call(Object o, Path p, Report r) { return json_model_1(o, p, r);} });
                super.init(json);
            }
            catch (Exception e) {
                throw new Error("cannot initialized model checker: " + e);
            }
        }
    }

    public void free()
    {
        if (initialized)
        {
            super.free();
            opt_fs_map_pmap = null;
        }
    }

    public Checker get(String name)
    {
        return opt_fs_map_pmap.get(name);
    }

    public Set<String> models()
    {
        return opt_fs_map_pmap.keySet();
    }

    static public void main(String[] args) throws Exception
    {
        ModelChecker checker = new opt_fs();
        Main.main("opt_fs", checker, VERSION, args);
    }
}
