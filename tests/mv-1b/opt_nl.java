//
// Generated by JSON Model Compiler version 2
// see https://github.com/clairey-zx81/json-model
//

import json_model.*;
import java.util.Map;
import java.util.Set;
import java.util.HashMap;
import java.util.HashSet;
import java.util.Iterator;
import java.util.regex.Pattern;
import java.util.regex.Matcher;

@SuppressWarnings("unchecked")
public class opt_nl extends ModelChecker
{
    static public final String VERSION = "2";

    public Map<String, Checker> opt_nl_map_pmap;

    // object .
    public boolean _jm_obj_0(Object val, Path path, Report rep)
    {
        // check close must only props
        if (! json.isObject(val))
        {
            if (rep != null) rep.addEntry("not an object [.]", path);
            return false;
        }
        if (json.objectSize(val) != 2)
        {
            if (rep != null) rep.addEntry("bad property count [.]", path);
            return false;
        }
        Object pval;
        boolean res;
        if (! json.objectHasProp(val, "i"))
        {
            if (rep != null) rep.addEntry("missing mandatory prop <i> [.]", path);
            return false;
        }
        pval = json.objectValue(val, "i");
        // .i
        res = (json.isInteger(pval) || (json.isDouble(pval) && json.asDouble(pval) == ((long) json.asDouble(pval))));
        if (! res)
        {
            if (rep != null) rep.addEntry("not a -1 loose int [.i]", path);
            if (rep != null) rep.addEntry("unexpected value for mandatory prop <i> [.]", path);
            return false;
        }
        if (! json.objectHasProp(val, "f"))
        {
            if (rep != null) rep.addEntry("missing mandatory prop <f> [.]", path);
            return false;
        }
        pval = json.objectValue(val, "f");
        // .f
        res = json.isNumber(pval);
        if (! res)
        {
            if (rep != null) rep.addEntry("not a -1.0 loose float [.f]", path);
            if (rep != null) rep.addEntry("unexpected value for mandatory prop <f> [.]", path);
            return false;
        }
        return true;
    }

    // check $ (.)
    public boolean json_model_1(Object val, Path path, Report rep)
    {
        boolean res;
        // JSON_MODEL_LOOSE_NUMBER
        // .
        res = _jm_obj_0(val, path, rep);
        if (! res)
        {
            if (rep != null) rep.addEntry("unexpected element [.]", path);
        }
        return res;
    }


    public void init(JSON json)
    {
        if (!initialized)
        {
            try {
            opt_nl_map_pmap = new HashMap<String, Checker>();
            opt_nl_map_pmap.put("", new Checker() { public boolean call(Object o, Path p, Report r) { return _jm_obj_0(o, p, r);} });
                super.init(json);
            }
            catch (Exception e) {
                throw new Error("cannot initialized model checker: " + e);
            }
        }
    }

    public void free()
    {
        if (initialized)
        {
            super.free();
            opt_nl_map_pmap = null;
        }
    }

    public Checker get(String name)
    {
        return opt_nl_map_pmap.get(name);
    }

    public Set<String> models()
    {
        return opt_nl_map_pmap.keySet();
    }

    static public void main(String[] args) throws Exception
    {
        ModelChecker checker = new opt_nl();
        Main.main("opt_nl", checker, VERSION, args);
    }
}
