//
// Generated by JSON Model Compiler version 2
// see https://github.com/clairey-zx81/json-model
//

import json_model.*;
import java.util.Map;
import java.util.Set;
import java.util.HashMap;
import java.util.HashSet;
import java.util.Iterator;
import java.util.regex.Pattern;
import java.util.regex.Matcher;

@SuppressWarnings("unchecked")
public class base_008 extends ModelChecker
{
    static public final String VERSION = "2";

    public Map<String, Checker> base_008_map_pmap;

    // check $ (.)
    public boolean json_model_1(Object val, Path path, Report rep)
    {
        // .
        if (! json.isObject(val))
        {
            if (rep != null) rep.addEntry("not an object [.]", path);
            return false;
        }
        boolean res;
        long must_count = 0;
        Iterator<String> prop_loop = json.objectIterator(val);
        while (prop_loop.hasNext())
        {
            String prop = prop_loop.next();
            Object pval = json.objectValue(val, prop);
            Path lpath_0 = new Path(prop, path);
            if (prop.compareTo("nom") == 0)
            {
                // handle must nom property
                must_count += 1;
                // .nom
                res = json.isString(pval);
                if (! res)
                {
                    if (rep != null) rep.addEntry("unexpected string [.nom]", (path != null ? lpath_0 : null));
                    if (rep != null) rep.addEntry("invalid mandatory prop value [.nom]", (path != null ? lpath_0 : null));
                    return false;
                }
                continue;
            }
            else if (prop.compareTo("prenom") == 0)
            {
                // handle must prenom property
                must_count += 1;
                // .prenom
                res = json.isString(pval);
                if (! res)
                {
                    if (rep != null) rep.addEntry("unexpected string [.prenom]", (path != null ? lpath_0 : null));
                    if (rep != null) rep.addEntry("invalid mandatory prop value [.prenom]", (path != null ? lpath_0 : null));
                    return false;
                }
                continue;
            }
            if (prop.compareTo("age") == 0)
            {
                // handle may age property
                // .age
                res = json.isInteger(pval) && json.asLong(pval) >= 0;
                if (! res)
                {
                    if (rep != null) rep.addEntry("not a 0 strict int [.age]", (path != null ? lpath_0 : null));
                    if (rep != null) rep.addEntry("invalid optional prop value [.age]", (path != null ? lpath_0 : null));
                    return false;
                }
                continue;
            }
            if (rep != null) rep.addEntry("unexpected prop [.]", (path != null ? lpath_0 : null));
            return false;
        }
        if (must_count != 2)
        {
            if (rep != null)
            {
                if (! json.objectHasProp(val, "nom"))
                {
                    if (rep != null) rep.addEntry("missing mandatory prop <nom> [.]", path);
                }
                if (! json.objectHasProp(val, "prenom"))
                {
                    if (rep != null) rep.addEntry("missing mandatory prop <prenom> [.]", path);
                }
            }
            return false;
        }
        return true;
    }


    public void init(JSON json)
    {
        if (!initialized)
        {
            try {
            base_008_map_pmap = new HashMap<String, Checker>();
            base_008_map_pmap.put("", new Checker() { public boolean call(Object o, Path p, Report r) { return json_model_1(o, p, r);} });
                super.init(json);
            }
            catch (Exception e) {
                throw new Error("cannot initialized model checker: " + e);
            }
        }
    }

    public void free()
    {
        if (initialized)
        {
            super.free();
            base_008_map_pmap = null;
        }
    }

    public Checker get(String name)
    {
        return base_008_map_pmap.get(name);
    }

    public Set<String> models()
    {
        return base_008_map_pmap.keySet();
    }

    static public void main(String[] args) throws Exception
    {
        ModelChecker checker = new base_008();
        Main.main("base_008", checker, VERSION, args);
    }
}
