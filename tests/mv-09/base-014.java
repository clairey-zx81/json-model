//
// Generated by JSON Model Compiler version 2
// see https://github.com/clairey-zx81/json-model
//

import json_model.*;
import java.util.Map;
import java.util.Set;
import java.util.HashMap;
import java.util.HashSet;
import java.util.Iterator;
import java.util.regex.Pattern;
import java.util.regex.Matcher;

@SuppressWarnings("unchecked")
public class base_014 extends ModelChecker
{
    static public final String VERSION = "2";

    public Map<String, Checker> base_014_map_pmap;

    // object .
    public boolean _jm_obj_0(Object val, Path path, Report rep)
    {
        if (! json.isObject(val))
        {
            if (rep != null) rep.addEntry("not an object [.]", path);
            return false;
        }
        boolean res;
        long must_count = 0;
        Iterator<String> prop_loop = json.objectIterator(val);
        while (prop_loop.hasNext())
        {
            String prop = prop_loop.next();
            Object pval = json.objectValue(val, prop);
            Path lpath_0 = new Path(prop, path);
            if (prop.compareTo("nom") == 0)
            {
                // handle must nom property
                must_count += 1;
                // .nom
                res = json.isString(pval);
                if (! res)
                {
                    if (rep != null) rep.addEntry("unexpected string [.nom]", (path != null ? lpath_0 : null));
                    if (rep != null) rep.addEntry("invalid mandatory prop value [.nom]", (path != null ? lpath_0 : null));
                    return false;
                }
            }
            else if (prop.compareTo("age") == 0)
            {
                // handle may age property
                // .age
                res = json.isInteger(pval) && json.asLong(pval) >= 0;
                if (! res)
                {
                    if (rep != null) rep.addEntry("not a 0 strict int [.age]", (path != null ? lpath_0 : null));
                    if (rep != null) rep.addEntry("invalid optional prop value [.age]", (path != null ? lpath_0 : null));
                    return false;
                }
            }
            else
            {
                // handle other props
                // .''
                res = json.isString(pval);
                if (! res)
                {
                    if (rep != null) rep.addEntry("unexpected string [.'']", (path != null ? lpath_0 : null));
                    return false;
                }
            }
        }
        if (must_count != 1)
        {
            if (rep != null)
            {
                if (! json.objectHasProp(val, "nom"))
                {
                    if (rep != null) rep.addEntry("missing mandatory prop <nom> [.'']", path);
                }
            }
            return false;
        }
        return true;
    }

    // check $ (.)
    public boolean json_model_1(Object val, Path path, Report rep)
    {
        boolean res;
        // .
        res = _jm_obj_0(val, path, rep);
        if (! res)
        {
            if (rep != null) rep.addEntry("unexpected element [.]", path);
        }
        return res;
    }


    public void init(JSON json)
    {
        if (!initialized)
        {
            try {
            base_014_map_pmap = new HashMap<String, Checker>();
            base_014_map_pmap.put("", new Checker() { public boolean call(Object o, Path p, Report r) { return json_model_1(o, p, r);} });
                super.init(json);
            }
            catch (Exception e) {
                throw new Error("cannot initialized model checker: " + e);
            }
        }
    }

    public void free()
    {
        if (initialized)
        {
            super.free();
            base_014_map_pmap = null;
        }
    }

    public Checker get(String name)
    {
        return base_014_map_pmap.get(name);
    }

    public Set<String> models()
    {
        return base_014_map_pmap.keySet();
    }

    static public void main(String[] args) throws Exception
    {
        ModelChecker checker = new base_014();
        Main.main("base_014", checker, VERSION, args);
    }
}
