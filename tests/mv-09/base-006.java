//
// Generated by JSON Model Compiler version 2
// see https://github.com/clairey-zx81/json-model
//

import json_model.*;
import java.util.Map;
import java.util.Set;
import java.util.HashMap;
import java.util.HashSet;
import java.util.Iterator;
import java.util.regex.Pattern;
import java.util.regex.Matcher;

@SuppressWarnings("unchecked")
public class base_006 extends ModelChecker
{
    static public final String VERSION = "2";

    public Map<String, Checker> base_006_map_pmap;

    // check $ (.)
    public boolean json_model_1(Object val, Path path, Report rep)
    {
        // .
        boolean res = json.isArray(val) && json.arrayLength(val) == 4;
        if (res)
        {
            Path lpath_0 = new Path(0, path);
            // .0
            res = json.isString(json.arrayItem(val, 0));
            if (! res)
            {
                if (rep != null) rep.addEntry("unexpected string [.0]", (path != null ? lpath_0 : null));
            }
            if (res)
            {
                lpath_0 = new Path(1, path);
                // .1
                res = json.isInteger(json.arrayItem(val, 1)) && json.asLong(json.arrayItem(val, 1)) >= 0;
                if (! res)
                {
                    if (rep != null) rep.addEntry("not a 0 strict int [.1]", (path != null ? lpath_0 : null));
                }
                if (res)
                {
                    lpath_0 = new Path(2, path);
                    // .2
                    res = json.isBoolean(json.arrayItem(val, 2));
                    if (! res)
                    {
                        if (rep != null) rep.addEntry("not a bool [.2]", (path != null ? lpath_0 : null));
                    }
                    if (res)
                    {
                        lpath_0 = new Path(3, path);
                        // .3
                        res = json.isDouble(json.arrayItem(val, 3)) && json.asDouble(json.arrayItem(val, 3)) >= 0.0;
                        if (! res)
                        {
                            if (rep != null) rep.addEntry("not a 0.0 strict float [.3]", (path != null ? lpath_0 : null));
                        }
                    }
                }
            }
        }
        if (! res)
        {
            if (rep != null) rep.addEntry("not array or unexpected array [.]", path);
        }
        return res;
    }


    public void init(JSON json)
    {
        if (!initialized)
        {
            try {
            base_006_map_pmap = new HashMap<String, Checker>();
            base_006_map_pmap.put("", new Checker() { public boolean call(Object o, Path p, Report r) { return json_model_1(o, p, r);} });
                super.init(json);
            }
            catch (Exception e) {
                throw new Error("cannot initialized model checker: " + e);
            }
        }
    }

    public void free()
    {
        if (initialized)
        {
            super.free();
            base_006_map_pmap = null;
        }
    }

    public Checker get(String name)
    {
        return base_006_map_pmap.get(name);
    }

    public Set<String> models()
    {
        return base_006_map_pmap.keySet();
    }

    static public void main(String[] args) throws Exception
    {
        ModelChecker checker = new base_006();
        Main.main("base_006", checker, VERSION, args);
    }
}
