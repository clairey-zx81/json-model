//
// Generated by JSON Model Compiler version 2
// see https://github.com/clairey-zx81/json-model
//

import json_model.*;
import java.util.Map;
import java.util.Set;
import java.util.HashMap;
import java.util.HashSet;
import java.util.Iterator;
import java.util.regex.Pattern;
import java.util.regex.Matcher;

@SuppressWarnings("unchecked")
public class loose extends ModelChecker
{
    static public final String VERSION = "2";

    public Map<String, Checker> loose_map_pmap;

    // check $ (.)
    public boolean json_model_1(Object val, Path path, Report rep)
    {
        // JSON_MODEL_LOOSE_INT
        // .
        // check close must only props
        if (! json.isObject(val))
        {
            if (rep != null) rep.addEntry("not an object [.]", path);
            return false;
        }
        if (json.objectSize(val) != 1)
        {
            if (rep != null) rep.addEntry("bad property count [.]", path);
            return false;
        }
        Path lpath;
        Object pval;
        boolean res;
        if (! ((pval = json.objectValue(val, "li")) != null))
        {
            if (rep != null) rep.addEntry("missing mandatory prop <li> [.]", path);
            return false;
        }
        lpath = new Path("li", path);
        // .li
        res = ((json.isInteger(pval) || (json.isDouble(pval) && json.asDouble(pval) == ((long) json.asDouble(pval))))) && json.asNumber(pval) >= 0;
        if (! res)
        {
            if (rep != null) rep.addEntry("not a 0 loose int [.li]", (path != null ? lpath : null));
            if (rep != null) rep.addEntry("unexpected value for mandatory prop <li> [.]", (path != null ? lpath : null));
            return false;
        }
        return true;
    }


    public void init(JSON json)
    {
        if (!initialized)
        {
            try {
            loose_map_pmap = new HashMap<String, Checker>();
            loose_map_pmap.put("", new Checker() { public boolean call(Object o, Path p, Report r) { return json_model_1(o, p, r);} });
                super.init(json);
            }
            catch (Exception e) {
                throw new Error("cannot initialized model checker: " + e);
            }
        }
    }

    public void free()
    {
        if (initialized)
        {
            super.free();
            loose_map_pmap = null;
        }
    }

    public Checker get(String name)
    {
        return loose_map_pmap.get(name);
    }

    public Set<String> models()
    {
        return loose_map_pmap.keySet();
    }

    static public void main(String[] args) throws Exception
    {
        ModelChecker checker = new loose();
        Main.main("loose", checker, VERSION, args);
    }
}
