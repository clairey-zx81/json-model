//
// Generated by JSON Model Compiler version 2
// see https://github.com/clairey-zx81/json-model
//

import json_model.*;
import java.util.Map;
import java.util.Set;
import java.util.HashMap;
import java.util.HashSet;
import java.util.Iterator;
import java.util.regex.Pattern;
import java.util.regex.Matcher;

@SuppressWarnings("unchecked")
public class comments extends ModelChecker
{
    static public final String VERSION = "2";

    public Map<String, Checker> comments_map_pmap;

    // check $ (.)
    public boolean json_model_1(Object val, Path path, Report rep)
    {
        // This is a
        // multiline
        // comment.
        // .
        // check close must only props
        if (! json.isObject(val))
        {
            if (rep != null) rep.addEntry("not an object [.]", path);
            return false;
        }
        if (json.objectSize(val) != 2)
        {
            if (rep != null) rep.addEntry("bad property count [.]", path);
            return false;
        }
        Path lpath;
        Object pval;
        boolean res;
        if (! ((pval = json.objectValue(val, "hello")) != null))
        {
            if (rep != null) rep.addEntry("missing mandatory prop <hello> [.]", path);
            return false;
        }
        lpath = new Path("hello", path);
        // .hello
        res = json.isString(pval);
        if (! res)
        {
            if (rep != null) rep.addEntry("unexpected value for model \"\" [.hello]", (path != null ? lpath : null));
            if (rep != null) rep.addEntry("unexpected value for mandatory prop <hello> [.]", (path != null ? lpath : null));
            return false;
        }
        if (! ((pval = json.objectValue(val, "world")) != null))
        {
            if (rep != null) rep.addEntry("missing mandatory prop <world> [.]", path);
            return false;
        }
        lpath = new Path("world", path);
        // .world
        // "/^!/"
        res = json.isString(pval) && json.asString(pval).startsWith("!");
        if (! res)
        {
            if (rep != null) rep.addEntry("unexpected value for model \"/^!/\" [.world]", (path != null ? lpath : null));
            if (rep != null) rep.addEntry("unexpected value for mandatory prop <world> [.]", (path != null ? lpath : null));
            return false;
        }
        return true;
    }


    public void init(JSON json)
    {
        if (!initialized)
        {
            try {
            comments_map_pmap = new HashMap<String, Checker>();
            comments_map_pmap.put("", new Checker() { public boolean call(Object o, Path p, Report r) { return json_model_1(o, p, r);} });
                super.init(json);
            }
            catch (Exception e) {
                throw new Error("cannot initialized model checker: " + e);
            }
        }
    }

    public void free()
    {
        if (initialized)
        {
            super.free();
            comments_map_pmap = null;
        }
    }

    public Checker get(String name)
    {
        return comments_map_pmap.get(name);
    }

    public Set<String> models()
    {
        return comments_map_pmap.keySet();
    }

    static public void main(String[] args) throws Exception
    {
        ModelChecker checker = new comments();
        Main.main("comments", checker, VERSION, args);
    }
}
