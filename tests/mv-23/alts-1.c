//
// Generated by JSON Model Compiler version 2
// see https://github.com/clairey-zx81/json-model
//

// regular expression engine
#include <stddef.h>
#include <cre2.h>

#include <json-model.h>
#define JSON_MODEL_VERSION "2"

static bool json_model_2(const json_t *val, jm_path_t *path, jm_report_t *rep);
static jm_constant_t _jm_cst_0[2];
static bool json_model_3(const json_t *val, jm_path_t *path, jm_report_t *rep);
static jm_constmap_t _jm_map_0_tab[3];
static bool json_model_4(const json_t *val, jm_path_t *path, jm_report_t *rep);
static bool json_model_5(const json_t *val, jm_path_t *path, jm_report_t *rep);
static jm_constant_t _jm_cst_1[2];
static bool json_model_6(const json_t *val, jm_path_t *path, jm_report_t *rep);
static bool json_model_1(const json_t *val, jm_path_t *path, jm_report_t *rep);
jm_propmap_t check_model_map_tab[6];
const size_t check_model_map_size = 6;

// check $oA (.'$oA')
static bool json_model_2(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    // .'$oA'
    // check open must/may only props
    if (! json_is_object(val))
    {
        if (rep) jm_report_add_entry(rep, "not an object [.'$oA']", path);
        return false;
    }
    jm_path_t lpath;
    json_t * pval;
    bool res;
    if (! ((pval = json_object_get(val, "v")) != NULL))
    {
        if (rep) jm_report_add_entry(rep, "missing mandatory prop <v> [.'$oA']", path);
        return false;
    }
    lpath = (jm_path_t) { "v", 0, path, NULL };
    // .'$oA'.v
    res = json_is_integer(pval) && json_integer_value(pval) >= 1;
    if (! res)
    {
        if (rep) jm_report_add_entry(rep, "not a 1 strict int [.'$oA'.v]", (path ? &lpath : NULL));
        if (rep) jm_report_add_entry(rep, "unexpected value for mandatory prop <v> [.'$oA']", (path ? &lpath : NULL));
        return false;
    }
    if (! ((pval = json_object_get(val, "t")) != NULL))
    {
        if (rep) jm_report_add_entry(rep, "missing mandatory prop <t> [.'$oA']", path);
        return false;
    }
    lpath = (jm_path_t) { "t", 0, path, NULL };
    // .'$oA'.t
    res = json_is_string(pval) && strcmp(json_string_value(pval), "a") == 0;
    if (! res)
    {
        if (rep) jm_report_add_entry(rep, "unexpected _a [.'$oA'.t]", (path ? &lpath : NULL));
        if (rep) jm_report_add_entry(rep, "unexpected value for mandatory prop <t> [.'$oA']", (path ? &lpath : NULL));
        return false;
    }
    return true;
}


// check $oB (.'$oB')
static bool json_model_3(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    // .'$oB'
    // check open must/may only props
    if (! json_is_object(val))
    {
        if (rep) jm_report_add_entry(rep, "not an object [.'$oB']", path);
        return false;
    }
    jm_path_t lpath;
    json_t * pval;
    bool res;
    if (! ((pval = json_object_get(val, "v")) != NULL))
    {
        if (rep) jm_report_add_entry(rep, "missing mandatory prop <v> [.'$oB']", path);
        return false;
    }
    lpath = (jm_path_t) { "v", 0, path, NULL };
    // .'$oB'.v
    res = json_is_integer(pval) && json_integer_value(pval) >= 1;
    if (! res)
    {
        if (rep) jm_report_add_entry(rep, "not a 1 strict int [.'$oB'.v]", (path ? &lpath : NULL));
        if (rep) jm_report_add_entry(rep, "unexpected value for mandatory prop <v> [.'$oB']", (path ? &lpath : NULL));
        return false;
    }
    if (! ((pval = json_object_get(val, "t")) != NULL))
    {
        if (rep) jm_report_add_entry(rep, "missing mandatory prop <t> [.'$oB']", path);
        return false;
    }
    lpath = (jm_path_t) { "t", 0, path, NULL };
    // .'$oB'.t
    res = jm_json_is_scalar(pval) && json_is_string(pval) && jm_search_cst(&(jm_constant_t) { cst_is_string, { .s = json_string_value(pval) } }, _jm_cst_0, 2);;
    if (! res)
    {
        if (rep) jm_report_add_entry(rep, "value not in enum [.'$oB'.t.'|']", (path ? &lpath : NULL));
        if (rep) jm_report_add_entry(rep, "unexpected value for mandatory prop <t> [.'$oB']", (path ? &lpath : NULL));
        return false;
    }
    return true;
}

static INLINE jm_check_fun_t _jm_map_0(json_t *val)
{
    jm_constant_t cst;
    jm_set_cst(&cst, val);
    return jm_search_constmap(&cst, _jm_map_0_tab, 3);
}

// check $oC (.'$oC')
static bool json_model_4(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    bool res;
    // .'$oC'
    bool iso_0 = json_is_object(val);
    res = iso_0;
    if (res)
    {
        if (json_object_get(val, "t") != NULL)
        {
            json_t * tag_0 = json_object_get(val, "t");
            jm_check_fun_t fun_0 = _jm_map_0(tag_0);
            if (fun_0 != NULL)
            {
                res = fun_0(val, path, rep);
            }
            else
            {
                res = false;
                if (rep) jm_report_add_entry(rep, "tag <t> value not found [.'$oC'.'|']", path);
            }
        }
        else
        {
            res = false;
            if (rep) jm_report_add_entry(rep, "tag prop <t> is missing [.'$oC'.'|']", path);
        }
    }
    else
    {
        if (rep) jm_report_add_entry(rep, "value is not an object [.'$oC'.'|']", path);
    }
    return res;
}

// check $oL (.'$oL')
static bool json_model_5(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    // .'$oL'
    // check open must/may only props
    if (! json_is_object(val))
    {
        if (rep) jm_report_add_entry(rep, "not an object [.'$oL']", path);
        return false;
    }
    jm_path_t lpath;
    json_t * pval;
    bool res;
    if (! ((pval = json_object_get(val, "v")) != NULL))
    {
        if (rep) jm_report_add_entry(rep, "missing mandatory prop <v> [.'$oL']", path);
        return false;
    }
    lpath = (jm_path_t) { "v", 0, path, NULL };
    // .'$oL'.v
    res = json_is_integer(pval) && json_integer_value(pval) >= 1;
    if (! res)
    {
        if (rep) jm_report_add_entry(rep, "not a 1 strict int [.'$oL'.v]", (path ? &lpath : NULL));
        if (rep) jm_report_add_entry(rep, "unexpected value for mandatory prop <v> [.'$oL']", (path ? &lpath : NULL));
        return false;
    }
    if (! ((pval = json_object_get(val, "t")) != NULL))
    {
        if (rep) jm_report_add_entry(rep, "missing mandatory prop <t> [.'$oL']", path);
        return false;
    }
    lpath = (jm_path_t) { "t", 0, path, NULL };
    // .'$oL'.t
    res = json_is_string(pval) && strcmp(json_string_value(pval), "d") == 0;
    if (! res)
    {
        if (rep) jm_report_add_entry(rep, "unexpected _d [.'$oL'.t]", (path ? &lpath : NULL));
        if (rep) jm_report_add_entry(rep, "unexpected value for mandatory prop <t> [.'$oL']", (path ? &lpath : NULL));
        return false;
    }
    return true;
}


// check $oX (.'$oX')
static bool json_model_6(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    // .'$oX'
    // check open must/may only props
    if (! json_is_object(val))
    {
        if (rep) jm_report_add_entry(rep, "not an object [.'$oX']", path);
        return false;
    }
    jm_path_t lpath;
    json_t * pval;
    bool res;
    if (! ((pval = json_object_get(val, "v")) != NULL))
    {
        if (rep) jm_report_add_entry(rep, "missing mandatory prop <v> [.'$oX']", path);
        return false;
    }
    lpath = (jm_path_t) { "v", 0, path, NULL };
    // .'$oX'.v
    res = json_is_integer(pval) && json_integer_value(pval) >= 1;
    if (! res)
    {
        if (rep) jm_report_add_entry(rep, "not a 1 strict int [.'$oX'.v]", (path ? &lpath : NULL));
        if (rep) jm_report_add_entry(rep, "unexpected value for mandatory prop <v> [.'$oX']", (path ? &lpath : NULL));
        return false;
    }
    if (! ((pval = json_object_get(val, "t")) != NULL))
    {
        if (rep) jm_report_add_entry(rep, "missing mandatory prop <t> [.'$oX']", path);
        return false;
    }
    lpath = (jm_path_t) { "t", 0, path, NULL };
    // .'$oX'.t
    res = jm_json_is_scalar(pval) && json_is_string(pval) && jm_search_cst(&(jm_constant_t) { cst_is_string, { .s = json_string_value(pval) } }, _jm_cst_1, 2);;
    if (! res)
    {
        if (rep) jm_report_add_entry(rep, "value not in enum [.'$oX'.t.'|']", (path ? &lpath : NULL));
        if (rep) jm_report_add_entry(rep, "unexpected value for mandatory prop <t> [.'$oX']", (path ? &lpath : NULL));
        return false;
    }
    return true;
}

// check $ (.)
static bool json_model_1(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    bool res;
    // .
    // generic xor list
    int64_t xc_0 = 0;
    bool xr_0;
    // .'^'.0
    xr_0 = json_model_2(val, path, rep);
    if (xr_0)
    {
        xc_0 += 1;
    }
    else
    {
        if (rep) jm_report_add_entry(rep, "unexpected $oA [.'^'.0]", path);
    }
    // .'^'.1
    xr_0 = json_model_3(val, path, rep);
    if (xr_0)
    {
        xc_0 += 1;
    }
    else
    {
        if (rep) jm_report_add_entry(rep, "unexpected $oB [.'^'.1]", path);
    }
    if (xc_0 <= 1)
    {
        // .'^'.2
        xr_0 = json_model_4(val, path, rep);
        if (xr_0)
        {
            xc_0 += 1;
        }
        else
        {
            if (rep) jm_report_add_entry(rep, "unexpected $oC [.'^'.2]", path);
        }
    }
    res = xc_0 == 1;
    if (res)
    {
        if (rep) jm_report_free_entries(rep);
    }
    else
    {
        if (rep) jm_report_add_entry(rep, "not one model match [.'^']", path);
    }
    return res;
}

jm_check_fun_t check_model_map(const char *pname)
{
    return jm_search_propmap(pname, check_model_map_tab, 6);
}

static bool initialized = false;

const char *check_model_init(void)
{
    if (!initialized)
    {
        initialized = true;
        jm_version_string = JSON_MODEL_VERSION;
        // initialize sorted set _jm_cst_0
        _jm_cst_0[0] = (jm_constant_t) { cst_is_string, { .s = "b" } };
        _jm_cst_0[1] = (jm_constant_t) { cst_is_string, { .s = "c" } };
        jm_sort_cst(_jm_cst_0, 2);
        _jm_map_0_tab[0] = (jm_constmap_t) { (jm_constant_t) { cst_is_string, { .s = "e" } }, json_model_6 };
        _jm_map_0_tab[1] = (jm_constmap_t) { (jm_constant_t) { cst_is_string, { .s = "f" } }, json_model_6 };
        _jm_map_0_tab[2] = (jm_constmap_t) { (jm_constant_t) { cst_is_string, { .s = "d" } }, json_model_5 };
        jm_sort_constmap(_jm_map_0_tab, 3);
        // initialize sorted set _jm_cst_1
        _jm_cst_1[0] = (jm_constant_t) { cst_is_string, { .s = "e" } };
        _jm_cst_1[1] = (jm_constant_t) { cst_is_string, { .s = "f" } };
        jm_sort_cst(_jm_cst_1, 2);
        check_model_map_tab[0] = (jm_propmap_t) { "", json_model_1 };
        check_model_map_tab[1] = (jm_propmap_t) { "oA", json_model_2 };
        check_model_map_tab[2] = (jm_propmap_t) { "oB", json_model_3 };
        check_model_map_tab[3] = (jm_propmap_t) { "oC", json_model_4 };
        check_model_map_tab[4] = (jm_propmap_t) { "oL", json_model_5 };
        check_model_map_tab[5] = (jm_propmap_t) { "oX", json_model_6 };
        jm_sort_propmap(check_model_map_tab, 6);
    }
    return NULL;
}

void check_model_free(void)
{
    if (initialized)
    {
        initialized = false;

        // cleanup code
    }
}

/*
 * # API
 *
 * ```c
 * bool error;
 * char *message;
 *
 * bool valid = check(json_value, "model-name", &error, &message);
 *
 * if (message)
 *    free(message), message = NULL;
 * ```
 *
 * - `error` is set of there was an error during initialization or if the model does not exists.
 * - `message` is provided if a non NULL pointer is passed, and must be freed by the caller.
 */
bool
check_model(const json_t *val, const char *name, bool *error, char **reasons)
{
    return jm_generic_entry(check_model_init, check_model_map, val, name, error, reasons);
}
