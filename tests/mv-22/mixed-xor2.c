//
// Generated by JSON Model Compiler version 2
// see https://github.com/clairey-zx81/json-model
//

// regular expression engine
#include <stddef.h>
#include <cre2.h>

#include <json-model.h>
#define JSON_MODEL_VERSION "2"

static bool _jm_obj_0(const json_t *val, jm_path_t *path, jm_report_t *rep);
static bool _jm_obj_1(const json_t *val, jm_path_t *path, jm_report_t *rep);
static jm_constmap_t _jm_map_0_tab[2];
static bool json_model_2(const json_t *val, jm_path_t *path, jm_report_t *rep);
static bool _jm_obj_2(const json_t *val, jm_path_t *path, jm_report_t *rep);
static bool _jm_obj_3(const json_t *val, jm_path_t *path, jm_report_t *rep);
static jm_constmap_t _jm_map_1_tab[2];
static bool json_model_1(const json_t *val, jm_path_t *path, jm_report_t *rep);
jm_propmap_t check_model_map_tab[2];
const size_t check_model_map_size = 2;

// object .'$cs'.'|'.0
static INLINE bool _jm_obj_0(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    if (unlikely(! json_is_object(val)))
    {
        if (rep) jm_report_add_entry(rep, "not an object [.'$cs'.'|'.0]", path);
        return false;
    }
    bool res;
    int64_t must_count = 0;
    const char *prop;
    json_t *pval;
    json_object_foreach((json_t *) val, prop, pval)
    {
        jm_path_t lpath_0 = (jm_path_t) { prop, 0, path, NULL };
        if (jm_str_eq_2(prop, 0x00000074))
        {
            // handle must t property
            must_count += 1;
            // .'$cs'.'|'.0.t
            res = json_is_string(pval) && jm_str_eq_2(json_string_value(pval), 0x00000063);
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "unexpected c [.'$cs'.'|'.0.t]", (path ? &lpath_0 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'$cs'.'|'.0.t]", (path ? &lpath_0 : NULL));
                return false;
            }
            continue;
        }
        else if (likely(jm_str_eq_4(prop, 0x006c6163)))
        {
            // handle must cal property
            must_count += 1;
            // .'$cs'.'|'.0.cal
            res = json_is_integer(pval) && json_integer_value(pval) >= 1;
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "not a 1 strict int [.'$cs'.'|'.0.cal]", (path ? &lpath_0 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'$cs'.'|'.0.cal]", (path ? &lpath_0 : NULL));
                return false;
            }
            continue;
        }
        if (rep) jm_report_add_entry(rep, "unexpected prop [.'$cs'.'|'.0]", (path ? &lpath_0 : NULL));
        return false;
    }
    if (unlikely(must_count != 2))
    {
        if (likely(rep != NULL))
        {
            if (! (json_object_get(val, "cal") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <cal> [.'$cs'.'|'.0]", path);
            }
            if (! (json_object_get(val, "t") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <t> [.'$cs'.'|'.0]", path);
            }
        }
        return false;
    }
    return true;
}

// object .'$cs'.'|'.1
static INLINE bool _jm_obj_1(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    if (unlikely(! json_is_object(val)))
    {
        if (rep) jm_report_add_entry(rep, "not an object [.'$cs'.'|'.1]", path);
        return false;
    }
    bool res;
    int64_t must_count = 0;
    const char *prop;
    json_t *pval;
    json_object_foreach((json_t *) val, prop, pval)
    {
        jm_path_t lpath_1 = (jm_path_t) { prop, 0, path, NULL };
        if (jm_str_eq_2(prop, 0x00000074))
        {
            // handle must t property
            must_count += 1;
            // .'$cs'.'|'.1.t
            res = json_is_string(pval) && jm_str_eq_2(json_string_value(pval), 0x00000073);
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "unexpected s [.'$cs'.'|'.1.t]", (path ? &lpath_1 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'$cs'.'|'.1.t]", (path ? &lpath_1 : NULL));
                return false;
            }
            continue;
        }
        else if (likely(jm_str_eq_4(prop, 0x00737573)))
        {
            // handle must sus property
            must_count += 1;
            // .'$cs'.'|'.1.sus
            res = json_is_integer(pval) && json_integer_value(pval) >= 1;
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "not a 1 strict int [.'$cs'.'|'.1.sus]", (path ? &lpath_1 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'$cs'.'|'.1.sus]", (path ? &lpath_1 : NULL));
                return false;
            }
            continue;
        }
        if (rep) jm_report_add_entry(rep, "unexpected prop [.'$cs'.'|'.1]", (path ? &lpath_1 : NULL));
        return false;
    }
    if (unlikely(must_count != 2))
    {
        if (likely(rep != NULL))
        {
            if (! (json_object_get(val, "sus") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <sus> [.'$cs'.'|'.1]", path);
            }
            if (! (json_object_get(val, "t") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <t> [.'$cs'.'|'.1]", path);
            }
        }
        return false;
    }
    return true;
}

static INLINE jm_check_fun_t _jm_map_0(json_t *val)
{
    jm_constant_t cst;
    jm_set_cst(&cst, val);
    return jm_search_constmap(&cst, _jm_map_0_tab, 2);
}

// check $cs (.'$cs')
static bool json_model_2(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    bool res;
    // .'$cs'
    bool iso_0 = json_is_object(val);
    res = iso_0;
    if (likely(res))
    {
        if (likely(json_object_get(val, "t") != NULL))
        {
            json_t * tag_0 = json_object_get(val, "t");
            jm_check_fun_t fun_0 = _jm_map_0(tag_0);
            if (likely(fun_0 != NULL))
                res = fun_0(val, path, rep);
            else
            {
                res = false;
                if (rep) jm_report_add_entry(rep, "tag <t> value not found [.'$cs'.'|']", path);
            }
        }
        else
        {
            res = false;
            if (rep) jm_report_add_entry(rep, "tag prop <t> is missing [.'$cs'.'|']", path);
        }
    }
    else
    {
        if (rep) jm_report_add_entry(rep, "value is not an object [.'$cs'.'|']", path);
    }
    return res;
}

// object .'|'.0
static INLINE bool _jm_obj_2(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    if (unlikely(! json_is_object(val)))
    {
        if (rep) jm_report_add_entry(rep, "not an object [.'|'.0]", path);
        return false;
    }
    bool res;
    int64_t must_count = 0;
    const char *prop;
    json_t *pval;
    json_object_foreach((json_t *) val, prop, pval)
    {
        jm_path_t lpath_2 = (jm_path_t) { prop, 0, path, NULL };
        if (jm_str_eq_2(prop, 0x00000074))
        {
            // handle must t property
            must_count += 1;
            // .'|'.0.t
            res = json_is_string(pval) && jm_str_eq_2(json_string_value(pval), 0x0000006d);
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "unexpected m [.'|'.0.t]", (path ? &lpath_2 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'|'.0.t]", (path ? &lpath_2 : NULL));
                return false;
            }
            continue;
        }
        else if (likely(jm_str_eq_4(prop, 0x00656f6d)))
        {
            // handle must moe property
            must_count += 1;
            // .'|'.0.moe
            res = json_is_integer(pval) && json_integer_value(pval) >= 1;
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "not a 1 strict int [.'|'.0.moe]", (path ? &lpath_2 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'|'.0.moe]", (path ? &lpath_2 : NULL));
                return false;
            }
            continue;
        }
        if (rep) jm_report_add_entry(rep, "unexpected prop [.'|'.0]", (path ? &lpath_2 : NULL));
        return false;
    }
    if (unlikely(must_count != 2))
    {
        if (likely(rep != NULL))
        {
            if (! (json_object_get(val, "moe") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <moe> [.'|'.0]", path);
            }
            if (! (json_object_get(val, "t") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <t> [.'|'.0]", path);
            }
        }
        return false;
    }
    return true;
}

// object .'|'.1
static INLINE bool _jm_obj_3(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    if (unlikely(! json_is_object(val)))
    {
        if (rep) jm_report_add_entry(rep, "not an object [.'|'.1]", path);
        return false;
    }
    bool res;
    int64_t must_count = 0;
    const char *prop;
    json_t *pval;
    json_object_foreach((json_t *) val, prop, pval)
    {
        jm_path_t lpath_3 = (jm_path_t) { prop, 0, path, NULL };
        if (jm_str_eq_2(prop, 0x00000074))
        {
            // handle must t property
            must_count += 1;
            // .'|'.1.t
            res = json_is_string(pval) && jm_str_eq_2(json_string_value(pval), 0x00000068);
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "unexpected h [.'|'.1.t]", (path ? &lpath_3 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'|'.1.t]", (path ? &lpath_3 : NULL));
                return false;
            }
            continue;
        }
        else if (likely(jm_str_eq_4(prop, 0x00626f68)))
        {
            // handle must hob property
            must_count += 1;
            // .'|'.1.hob
            res = json_is_integer(pval) && json_integer_value(pval) >= 1;
            if (unlikely(! res))
            {
                if (rep) jm_report_add_entry(rep, "not a 1 strict int [.'|'.1.hob]", (path ? &lpath_3 : NULL));
                if (rep) jm_report_add_entry(rep, "invalid mandatory prop value [.'|'.1.hob]", (path ? &lpath_3 : NULL));
                return false;
            }
            continue;
        }
        if (rep) jm_report_add_entry(rep, "unexpected prop [.'|'.1]", (path ? &lpath_3 : NULL));
        return false;
    }
    if (unlikely(must_count != 2))
    {
        if (likely(rep != NULL))
        {
            if (! (json_object_get(val, "hob") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <hob> [.'|'.1]", path);
            }
            if (! (json_object_get(val, "t") != NULL))
            {
                if (rep) jm_report_add_entry(rep, "missing mandatory prop <t> [.'|'.1]", path);
            }
        }
        return false;
    }
    return true;
}

static INLINE jm_check_fun_t _jm_map_1(json_t *val)
{
    jm_constant_t cst;
    jm_set_cst(&cst, val);
    return jm_search_constmap(&cst, _jm_map_1_tab, 2);
}

// check $ (.)
static bool json_model_1(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    bool res;
    // .
    bool iso_1 = json_is_object(val);
    res = iso_1;
    if (likely(res))
    {
        if (likely(json_object_get(val, "t") != NULL))
        {
            json_t * tag_1 = json_object_get(val, "t");
            jm_check_fun_t fun_1 = _jm_map_1(tag_1);
            if (likely(fun_1 != NULL))
                res = fun_1(val, path, rep);
            else
            {
                res = false;
                if (rep) jm_report_add_entry(rep, "tag <t> value not found [.'|']", path);
            }
        }
        else
        {
            res = false;
            if (rep) jm_report_add_entry(rep, "tag prop <t> is missing [.'|']", path);
        }
    }
    else
    {
        if (rep) jm_report_add_entry(rep, "value is not an object [.'|']", path);
    }
    return res || json_model_2(val, path, rep);
}

jm_check_fun_t check_model_map(const char *pname)
{
    return jm_search_propmap(pname, check_model_map_tab, 2);
}

static bool initialized = false;

const char *check_model_init(void)
{
    if (!initialized)
    {
        initialized = true;
        jm_version_string = JSON_MODEL_VERSION;
        _jm_map_0_tab[0] = (jm_constmap_t) { (jm_constant_t) { 2, { .s = "c" } }, _jm_obj_0 };
        _jm_map_0_tab[1] = (jm_constmap_t) { (jm_constant_t) { 2, { .s = "s" } }, _jm_obj_1 };
        jm_sort_constmap(_jm_map_0_tab, 2);
        _jm_map_1_tab[0] = (jm_constmap_t) { (jm_constant_t) { 2, { .s = "m" } }, _jm_obj_2 };
        _jm_map_1_tab[1] = (jm_constmap_t) { (jm_constant_t) { 2, { .s = "h" } }, _jm_obj_3 };
        jm_sort_constmap(_jm_map_1_tab, 2);
        check_model_map_tab[0] = (jm_propmap_t) { "", json_model_1 };
        check_model_map_tab[1] = (jm_propmap_t) { "cs", json_model_2 };
        jm_sort_propmap(check_model_map_tab, 2);
    }
    return NULL;
}

void check_model_free(void)
{
    if (initialized)
        initialized = false;

        // cleanup code
}

/*
 * # API
 *
 * ```c
 * bool error;
 * char *message;
 *
 * bool valid = check(json_value, "model-name", &error, &message);
 *
 * if (message)
 *    free(message), message = NULL;
 * ```
 *
 * - `error` is set of there was an error during initialization or if the model does not exists.
 * - `message` is provided if a non NULL pointer is passed, and must be freed by the caller.
 */
bool
check_model(const json_t *val, const char *name, bool *error, char **reasons)
{
    return jm_generic_entry(check_model_init, check_model_map, val, name, error, reasons);
}
