#! /bin/env python
#
# Generated by JSON Model Compiler version 2
# see https://github.com/clairey-zx81/json-model
#
import math
from typing import Callable
import re2 as re
from json_model.runtime import *
__version__ = "2"

def check_model_fun(name: str) -> CheckFun:
    """Return check function for JSON model name."""
    return check_model_map[name]

# entry point for generated checkers
def check_model(val: Jsonable, name: str = "", rep: Report = None) -> bool:
    """Check val validity against JSON Model name."""
    if name not in check_model_map:
        raise Exception(f"unexpected model name: {name}")
    checker = check_model_map[name]
    return checker(val, [], rep)

check_model_map: PropMap

# object .'$Ax'.'^'.0
def _jm_obj_0(val: Jsonable, path: Path, rep: Report) -> bool:
    # check open must/may only props
    if not isinstance(val, dict):
        rep is None or rep.append(("not an object [.'$Ax'.'^'.0]", path))
        return False
    if not "a" in val:
        rep is None or rep.append(("missing mandatory prop <a> [.'$Ax'.'^'.0]", path))
        return False
    return True

# object .'$Ax'.'^'.1
def _jm_obj_1(val: Jsonable, path: Path, rep: Report) -> bool:
    # check open must/may only props
    if not isinstance(val, dict):
        rep is None or rep.append(("not an object [.'$Ax'.'^'.1]", path))
        return False
    if not "b" in val:
        rep is None or rep.append(("missing mandatory prop <b> [.'$Ax'.'^'.1]", path))
        return False
    return True

# check $Ax (.'$Ax')
def json_model_2(val: Jsonable, path: Path, rep: Report) -> bool:
    # .'$Ax'
    # generic xor list
    xc_0: int = 0
    # .'$Ax'.'^'.0
    xr_0: bool = _jm_obj_0(val, path, rep)
    if xr_0:
        xc_0 += 1
    else:
        rep is None or rep.append(("unexpected element [.'$Ax'.'^'.0]", path))
    # .'$Ax'.'^'.1
    xr_0 = _jm_obj_1(val, path, rep)
    if xr_0:
        xc_0 += 1
    else:
        rep is None or rep.append(("unexpected element [.'$Ax'.'^'.1]", path))
    res: bool = xc_0 == 1
    if res:
        rep is None or rep.clear()
    else:
        rep is None or rep.append(("not one model match [.'$Ax'.'^']", path))
    return res

# check $Bx (.'$Bx')
def json_model_3(val: Jsonable, path: Path, rep: Report) -> bool:
    # .'$Bx'
    # .'$Bx'.'|'.0
    res: bool = val is None
    if not res:
        rep is None or rep.append(("not null [.'$Bx'.'|'.0]", path))
        # .'$Bx'.'|'.1
        res = isinstance(val, dict)
    if res:
        rep is None or rep.clear()
    else:
        rep is None or rep.append(("no model matched [.'$Bx'.'|']", path))
    return res

# check $ (.)
def json_model_1(val: Jsonable, path: Path, rep: Report) -> bool:
    # .
    res: bool = json_model_2(val, path, rep)
    if not res:
        rep is None or rep.append(("unexpected value for model \"$Ax\" [.]", path))
    return res


# initialization guard
initialized: bool = False

# differed module initializations
def check_model_init():
    global initialized
    if not initialized:
        initialized = True
        global check_model_map
        check_model_map = {
            "": json_model_2,
            "Ax": json_model_2,
            "Bx": json_model_3,
        }

# differed module cleanup
def check_model_free():
    global initialized
    if initialized:
        initialized = False

if __name__ == "__main__":
    check_model_init()
    main(check_model_fun, check_model_map, __version__)
    check_model_free()
