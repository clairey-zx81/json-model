#! /bin/env node
// we may need require to load re2
import { createRequire } from 'node:module';
const require = createRequire(import.meta.url);
//
// Generated by JSON Model Compiler version 2
// see https://github.com/clairey-zx81/json-model
//
import * as runtime from "json_model_runtime"
const JSON_MODEL_VERSION = "2";

export var check_model_map = new Map()

// object .'$Ax'.'^'.0
function _jm_obj_0(val, path, rep)
{
    // check open must/may only props
    if (! (val !== null && typeof val == 'object' && !Array.isArray(val)))
    {
        rep !== null && rep.push(["not an object [.'$Ax'.'^'.0]", path])
        return false
    }
    if (! val.hasOwnProperty("a"))
    {
        rep !== null && rep.push(["missing mandatory prop <a> [.'$Ax'.'^'.0]", path])
        return false
    }
    return true
}

// object .'$Ax'.'^'.1
function _jm_obj_1(val, path, rep)
{
    // check open must/may only props
    if (! (val !== null && typeof val == 'object' && !Array.isArray(val)))
    {
        rep !== null && rep.push(["not an object [.'$Ax'.'^'.1]", path])
        return false
    }
    if (! val.hasOwnProperty("b"))
    {
        rep !== null && rep.push(["missing mandatory prop <b> [.'$Ax'.'^'.1]", path])
        return false
    }
    return true
}

// check $Ax (.'$Ax')
function json_model_2(val, path, rep)
{
    // .'$Ax'
    // generic xor list
    let xc_0 = 0
    // .'$Ax'.'^'.0
    let xr_0 = _jm_obj_0(val, path, rep)
    if (xr_0)
        xc_0 += 1
    else
        rep !== null && rep.push(["unexpected element [.'$Ax'.'^'.0]", path])
    // .'$Ax'.'^'.1
    xr_0 = _jm_obj_1(val, path, rep)
    if (xr_0)
        xc_0 += 1
    else
        rep !== null && rep.push(["unexpected element [.'$Ax'.'^'.1]", path])
    let res = xc_0 == 1
    if (res)
    {
        if (rep !== null) rep.length = 0
    }
    else
        rep !== null && rep.push(["not one model match [.'$Ax'.'^']", path])
    return res
}

// check $Bx (.'$Bx')
function json_model_3(val, path, rep)
{
    // .'$Bx'
    // .'$Bx'.'|'.0
    let res = val === null
    if (! res)
    {
        rep !== null && rep.push(["not null [.'$Bx'.'|'.0]", path])
        // .'$Bx'.'|'.1
        res = val !== null && typeof val == 'object' && !Array.isArray(val)
    }
    if (res)
    {
        if (rep !== null) rep.length = 0
    }
    else
        rep !== null && rep.push(["no model matched [.'$Bx'.'|']", path])
    return res
}

// check $ (.)
function json_model_1(val, path, rep)
{
    // .
    let res = json_model_2(val, path, rep)
    if (! res)
        rep !== null && rep.push(["unexpected value for model \"$Ax\" [.]", path])
    return res
}


var initialized = false

// differed module initializations
export function check_model_init()
{
    if (! initialized)
    {
        initialized = true;
        runtime.jm_set_rx(RegExp)
        check_model_map.set("", json_model_2)
        check_model_map.set("Ax", json_model_2)
        check_model_map.set("Bx", json_model_3)
    }
}

// differed module cleanup
export function check_model_free()
{
    if (initialized)
        initialized = false;
}

export function check_model(val, name, rep)
{
    let checker = check_model_map.get(name)
    if (checker === undefined)
        throw `no checker for "${name}"`
    let path = rep !== null ? [] : null

    return checker(val, path, rep)
}

// possibly run as main based on a guess
import main from "json_model_runtime/main.js"

if (import.meta.url.endsWith(process.argv[1]))
    main(check_model_init, check_model, check_model_free)
