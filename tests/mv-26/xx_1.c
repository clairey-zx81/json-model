//
// Generated by JSON Model Compiler version 2
// see https://github.com/clairey-zx81/json-model
//

// regular expression engine
#include <stddef.h>
#include <cre2.h>

#include <json-model.h>
#define JSON_MODEL_VERSION "2"

static bool json_model_1(const json_t *val, jm_path_t *path, jm_report_t *rep);
jm_propmap_t check_model_map_tab[1];
const size_t check_model_map_size = 1;

// check $ (.)
static bool json_model_1(const json_t *val, jm_path_t *path, jm_report_t *rep)
{
    bool res;
    // not hello, world or !
    // .
    // generic xor list
    int64_t xc_0 = 0;
    // .'^'.0
    bool xr_0 = json_is_string(val);
    if (unlikely(xr_0))
        xc_0 += 1;
    else
    {
        if (rep) jm_report_add_entry(rep, "unexpected string [.'^'.0]", path);
    }
    // .'^'.1
    xr_0 = json_is_string(val) && jm_str_eq_6(json_string_value(val), 0x000000646c726f77LL);
    if (unlikely(xr_0))
        xc_0 += 1;
    else
    {
        if (rep) jm_report_add_entry(rep, "unexpected _world [.'^'.1]", path);
    }
    if (xc_0 <= 1)
    {
        // .'^'.2
        xr_0 = json_is_string(val) && jm_str_eq_2(json_string_value(val), 0x00000021);
        if (unlikely(xr_0))
            xc_0 += 1;
        else
        {
            if (rep) jm_report_add_entry(rep, "unexpected _! [.'^'.2]", path);
        }
    }
    if (xc_0 <= 1)
    {
        // .'^'.3
        xr_0 = json_is_string(val) && jm_str_eq_6(json_string_value(val), 0x0000006f6c6c6568LL);
        if (unlikely(xr_0))
            xc_0 += 1;
        else
        {
            if (rep) jm_report_add_entry(rep, "unexpected _hello [.'^'.3]", path);
        }
    }
    res = xc_0 == 1;
    if (likely(res))
    {
        if (rep) jm_report_free_entries(rep);
    }
    else
    {
        if (rep) jm_report_add_entry(rep, "not one model match [.'^']", path);
    }
    return res;
}

jm_check_fun_t check_model_map(const char *pname)
{
    return jm_search_propmap(pname, check_model_map_tab, 1);
}

static bool initialized = false;

const char *check_model_init(void)
{
    if (!initialized)
    {
        initialized = true;
        jm_version_string = JSON_MODEL_VERSION;
        check_model_map_tab[0] = (jm_propmap_t) { "", json_model_1 };
        jm_sort_propmap(check_model_map_tab, 1);
    }
    return NULL;
}

void check_model_free(void)
{
    if (initialized)
        initialized = false;

        // cleanup code
}

/*
 * # API
 *
 * ```c
 * bool error;
 * char *message;
 *
 * bool valid = check(json_value, "model-name", &error, &message);
 *
 * if (message)
 *    free(message), message = NULL;
 * ```
 *
 * - `error` is set of there was an error during initialization or if the model does not exists.
 * - `message` is provided if a non NULL pointer is passed, and must be freed by the caller.
 */
bool
check_model(const json_t *val, const char *name, bool *error, char **reasons)
{
    return jm_generic_entry(check_model_init, check_model_map, val, name, error, reasons);
}
