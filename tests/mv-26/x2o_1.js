#! /bin/env node
// we may need require to load re2
import { createRequire } from 'node:module';
const require = createRequire(import.meta.url);
//
// Generated by JSON Model Compiler version 2
// see https://github.com/clairey-zx81/json-model
//
import * as runtime from "json_model_runtime"
const JSON_MODEL_VERSION = "2";

let _jm_cst_0 = new Set()
let _jm_map_0 = new Map()
var check_model_map = new Map()

// check $a (.'$a')
function json_model_2(val, path, rep)
{
    // .'$a'
    // check close must only props
    if (! (Object.prototype.toString.call(val) === '[object Object]'))
    {
        rep !== null && rep.push(["not an object [.'$a']", path])
        return false;
    }
    if (Object.keys(val).length != 1)
    {
        rep !== null && rep.push(["bad property count [.'$a']", path])
        return false;
    }
    let lpath;
    let pval;
    let res;
    if (! val.hasOwnProperty("t"))
    {
        rep !== null && rep.push(["missing mandatory prop <t> [.'$a']", path])
        return false;
    }
    lpath = path ? path.concat(["t"]) : null;
    pval = val["t"];
    // .'$a'.t
    res = ((typeof pval === 'string' || pval instanceof String)) && pval == "a";
    if (! res)
    {
        rep !== null && rep.push(["unexpected a [.'$a'.t]", (path ? lpath : null)])
        rep !== null && rep.push(["unexpected value for mandatory prop <t> [.'$a']", (path ? lpath : null)])
        return false;
    }
    return true;
}


// check $bc (.'$bc')
function json_model_3(val, path, rep)
{
    // .'$bc'
    // check close must only props
    if (! (Object.prototype.toString.call(val) === '[object Object]'))
    {
        rep !== null && rep.push(["not an object [.'$bc']", path])
        return false;
    }
    if (Object.keys(val).length != 1)
    {
        rep !== null && rep.push(["bad property count [.'$bc']", path])
        return false;
    }
    let lpath;
    let pval;
    let res;
    if (! val.hasOwnProperty("t"))
    {
        rep !== null && rep.push(["missing mandatory prop <t> [.'$bc']", path])
        return false;
    }
    lpath = path ? path.concat(["t"]) : null;
    pval = val["t"];
    // .'$bc'.t
    res = ((pval === null || (typeof pval === 'number' || pval instanceof Number) || (typeof pval === 'boolean' || pval instanceof Boolean) || (typeof pval === 'string' || pval instanceof String))) && _jm_cst_0.has(pval);
    if (! res)
    {
        rep !== null && rep.push(["value not in enum [.'$bc'.t.'|']", (path ? lpath : null)])
        rep !== null && rep.push(["unexpected value for mandatory prop <t> [.'$bc']", (path ? lpath : null)])
        return false;
    }
    return true;
}

// check $Nabc (.'$Nabc')
function json_model_4(val, path, rep)
{
    // .'$Nabc'
    // check close must only props
    if (! (Object.prototype.toString.call(val) === '[object Object]'))
    {
        rep !== null && rep.push(["not an object [.'$Nabc']", path])
        return false;
    }
    if (Object.keys(val).length != 1)
    {
        rep !== null && rep.push(["bad property count [.'$Nabc']", path])
        return false;
    }
    let lpath;
    let pval;
    let res;
    if (! val.hasOwnProperty("t"))
    {
        rep !== null && rep.push(["missing mandatory prop <t> [.'$Nabc']", path])
        return false;
    }
    lpath = path ? path.concat(["t"]) : null;
    pval = val["t"];
    // .'$Nabc'.t
    // generic xor list
    let xc_0 = 0;
    let xr_0;
    // .'$Nabc'.t.'^'.0
    xr_0 = (typeof pval === 'string' || pval instanceof String);
    if (xr_0)
    {
        xc_0 += 1;
    }
    else
    {
        rep !== null && rep.push(["unexpected string [.'$Nabc'.t.'^'.0]", (path ? lpath : null)])
    }
    // .'$Nabc'.t.'^'.1
    xr_0 = ((typeof pval === 'string' || pval instanceof String)) && pval == "a";
    if (xr_0)
    {
        xc_0 += 1;
    }
    else
    {
        rep !== null && rep.push(["unexpected _a [.'$Nabc'.t.'^'.1]", (path ? lpath : null)])
    }
    if (xc_0 <= 1)
    {
        // .'$Nabc'.t.'^'.2
        xr_0 = ((typeof pval === 'string' || pval instanceof String)) && pval == "b";
        if (xr_0)
        {
            xc_0 += 1;
        }
        else
        {
            rep !== null && rep.push(["unexpected _b [.'$Nabc'.t.'^'.2]", (path ? lpath : null)])
        }
    }
    if (xc_0 <= 1)
    {
        // .'$Nabc'.t.'^'.3
        xr_0 = ((typeof pval === 'string' || pval instanceof String)) && pval == "c";
        if (xr_0)
        {
            xc_0 += 1;
        }
        else
        {
            rep !== null && rep.push(["unexpected _c [.'$Nabc'.t.'^'.3]", (path ? lpath : null)])
        }
    }
    res = xc_0 == 1;
    if (res)
    {
        if (rep !== null) rep.length = 0
    }
    else
    {
        rep !== null && rep.push(["not one model match [.'$Nabc'.t.'^']", (path ? lpath : null)])
        rep !== null && rep.push(["unexpected value for mandatory prop <t> [.'$Nabc']", (path ? lpath : null)])
        return false;
    }
    return true;
}


// check $ (.)
function json_model_1(val, path, rep)
{
    let res;
    // xor-to-or with not constant exclusion behind references
    // .
    let iso_0 = Object.prototype.toString.call(val) === '[object Object]';
    res = iso_0;
    if (res)
    {
        if (val.hasOwnProperty("t"))
        {
            let tag_0 = val["t"];
            let fun_0 = _jm_map_0.get(tag_0);
            if (fun_0 !== undefined)
            {
                res = fun_0(val, path, rep);
            }
            else
            {
                res = false;
                rep !== null && rep.push(["tag <t> value not found [.'|']", path])
            }
        }
        else
        {
            res = false;
            rep !== null && rep.push(["tag prop <t> is missing [.'|']", path])
        }
    }
    else
    {
        rep !== null && rep.push(["value is not an object [.'|']", path])
    }
    if (! res)
    {
        res = json_model_4(val, path, rep);
    }
    return res;
}


var initialized = false

// differed module initializations
export function check_model_init()
{
    if (! initialized)
    {
        initialized = true;
        runtime.jm_set_rx(RegExp)
        _jm_cst_0.add("b")
        _jm_cst_0.add("c")
        _jm_map_0.set("a", json_model_2)
        _jm_map_0.set("b", json_model_3)
        _jm_map_0.set("c", json_model_3)
        check_model_map.set("", json_model_1)
        check_model_map.set("a", json_model_2)
        check_model_map.set("bc", json_model_3)
        check_model_map.set("Nabc", json_model_4)
    }
}

// differed module cleanup
export function check_model_free()
{
    if (initialized)
    {
        initialized = false;
    }
}

export function check_model(val, name, rep)
{
    let checker = check_model_map.get(name)
    if (checker === undefined)
        throw `no checker for "${name}"`
    let path = rep !== null ? [] : null

    return checker(val, path, rep)
}

// possibly run as main based on a guess
import main from "json_model_runtime/main.js"

if (import.meta.url.endsWith(process.argv[1]))
    main(check_model_init, check_model, check_model_free)
